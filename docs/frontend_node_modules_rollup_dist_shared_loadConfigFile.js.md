# `loadConfigFile.js`

> **Path:** `frontend/node_modules/rollup/dist/shared/loadConfigFile.js`  
> **Last documented:** 2026-06-29 18:03

---

## Functions

### `batchWarnings(command)` — line 20

### `defaultBody(log)` — line 205

### `title(string_)` — line 216

### `info(url)` — line 219

### `nest(array, property)` — line 222

### `showTruncatedWarnings(warnings)` — line 238

### `generateLogFilter(command)` — line 252

### `formatLocation(log)` — line 259

### `stdinPlugin(argument)` — line 268

### `readStdin()` — line 284

### `waitForInputPlugin()` — line 300

### `addCommandPluginsToInputOptions(inputOptions, command)` — line 325

### `addPluginsFromCommandOption(commandPlugin, inputOptions)` — line 334

### `loadAndRegisterPlugin(inputOptions, pluginText)` — line 353

### `getCamelizedPluginBaseName(pluginText)` — line 410

### `requireOrImport(pluginPath)` — line 419

### `getConfigFileExport(fileName, commandOptions, watchMode)` — line 446

### `getDefaultFromCjs(namespace)` — line 487

### `getConfigImportAttributesKey(input)` — line 490

### `loadTranspiledConfigFile(fileName, commandOptions)` — line 495

### `loadConfigFromWrittenFile(bundledFileName, bundledCode)` — line 549

### `getConfigList(configFileExport, commandOptions)` — line 558

### `add(warning)` — line 26

### `loadConfigFile(fileName, commandOptions = {}, watchMode = false)` — line 429

### `handleWarning(warning)` — line 459
