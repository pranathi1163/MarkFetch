# `util.js`

> **Path:** `frontend/node_modules/source-map-js/lib/util.js`  
> **Last documented:** 2026-06-29 18:03

---

## Functions

### `getArg(aArgs, aName, aDefaultValue)` — line 18

### `urlParse(aUrl)` — line 32

### `urlGenerate(aParsedUrl)` — line 47

### `lruMemoize(f)` — line 78

### `normalize(aPath)` — line 117

### `join(aRoot, aPath)` — line 195

### `relative(aRoot, aPath)` — line 248

### `identity(s)` — line 287

### `toSetString(aStr)` — line 300

### `fromSetString(aStr)` — line 309

### `isProtoString(s)` — line 318

### `compareByOriginalPositions(mappingA, mappingB, onlyCompareOriginal)` — line 358

### `compareByOriginalPositionsNoSource(mappingA, mappingB, onlyCompareOriginal)` — line 388

### `compareByGeneratedPositionsDeflated(mappingA, mappingB, onlyCompareGenerated)` — line 424

### `compareByGeneratedPositionsDeflatedNoLine(mappingA, mappingB, onlyCompareGenerated)` — line 454

### `strcmp(aStr1, aStr2)` — line 479

### `compareByGeneratedPositionsInflated(mappingA, mappingB)` — line 503

### `parseSourceMapInput(str)` — line 538

### `computeSourceURL(sourceRoot, sourceURL, sourceMapURL)` — line 547
