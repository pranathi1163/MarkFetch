# `dep-CV-fz3CQ.js`

> **Path:** `frontend/node_modules/vite/dist/node/chunks/dep-CV-fz3CQ.js`  
> **Last documented:** 2026-06-29 18:03

---

## Dependencies / Imports

- `./dep-3RmXg9uo.js`
- `./dep-Dm0c1Wj2.js`
- `node:module`
- `path`

## Functions

### `_mergeNamespaces(n, m)` — line 8

### `formatImportPrelude(layer, media, supports)` — line 20

### `base64EncodedConditionalImport(prelude, conditions)` — line 50

### `applyConditions(bundle, atRule)` — line 74

### `applyRaws(bundle)` — line 156

### `applyStyles(bundle, styles)` — line 170

### `isValid(url)` — line 191

### `contents(url)` — line 195

### `parseStatements(result, styles, conditions, from)` — line 221

### `parseCharset(result, atRule, conditions, from)` — line 260

### `parseImport(result, atRule, conditions, from)` — line 274

### `processContent(result, content, filename, options, postcss)` — line 455

### `runPostcss(postcss, content, filename, plugins, parsers, index)` — line 492

### `handleCharset(stmt)` — line 544

### `resolveImportId(result, stmt, options, state, postcss)` — line 579

### `loadImportContent(result, stmt, filename, options, state, postcss)` — line 640

### `isProcessableURL(uri)` — line 725

### `AtImport(options)` — line 756

### `loadContent()` — line 752

### `resolveId(id)` — line 754
