# `parseAst.js`

> **Path:** `frontend/node_modules/rollup/dist/es/shared/parseAst.js`  
> **Last documented:** 2026-06-29 18:03

---

## Dependencies / Imports

- `../../native.js`
- `node:path`

## Functions

### `relative(from, to)` — line 14

### `rangeContains(range, index)` — line 74

### `getLocator(source, options = {})` — line 82

### `locator(search, index)` — line 103

### `locate(source, search, options)` — line 137

### `spaces(index)` — line 141

### `tabsToSpaces(value)` — line 147

### `getCodeFrame(source, line, column)` — line 153

### `isAbsolute(path)` — line 202

### `isRelative(path)` — line 205

### `normalize(path)` — line 209

### `printQuotedStringList(list, verbs)` — line 213

### `getAliasName(id)` — line 225

### `relativeId(id)` — line 229

### `isPathFragment(name)` — line 234

### `getImportPath(importerId, targetPath, stripJsExtension, ensureFileName)` — line 239

### `isValidUrl(url)` — line 258

### `getRollupUrl(snippet)` — line 267

### `addTrailingSlashIfMissed(url)` — line 270

### `error(base)` — line 312

### `getRollupError(base)` — line 315

### `augmentCodeLocation(properties, pos, source, id)` — line 324

### `augmentLogMessage(log)` — line 344

### `logAddonNotGenerated(message, hook, plugin)` — line 366

### `logAlreadyClosed()` — line 373

### `logAmbiguousExternalNamespaces(binding, reexportingModule, usedModule, sources)` — line 379

### `logAnonymousPluginCache()` — line 388

### `logAssetNotFinalisedForFileName(name)` — line 394

### `logAssetReferenceIdNotFoundForSetSource(assetReferenceId)` — line 400

### `logAssetSourceAlreadySet(name)` — line 406

### `logNoAssetSourceSet(assetName)` — line 412

### `logBadLoader(id)` — line 418

### `logCannotCallNamespace(name)` — line 424

### `logCannotEmitFromOptionsHook()` — line 430

### `logChunkNotGeneratedForFileName(name)` — line 436

### `logChunkInvalid({ fileName, code }, { pos, message })` — line 442

### `logCircularDependency(cyclePath)` — line 450

### `logCircularChunk(cyclePath, isManualChunkConflict)` — line 457

### `logCircularReexport(exportName, exporter)` — line 466

### `logCyclicCrossChunkReexport(exportName, exporter, reexporter, importer, preserveModules)` — line 473

### `logDeprecation(deprecation, urlSnippet, plugin)` — line 482

### `logConstVariableReassignError()` — line 490

### `logDuplicateArgumentNameError(name)` — line 496

### `logDuplicateExportError(name)` — line 502

### `logDuplicatePluginName(plugin)` — line 505

### `logEmptyChunk(chunkName)` — line 511

### `logEval(id)` — line 518

### `logExternalSyntheticExports(id, importer)` — line 526

### `logFileNameConflict(fileName)` — line 533

### `logFileNameOutsideOutputDirectory(fileName)` — line 539

### `logFileReferenceIdNotFoundForFilename(assetReferenceId)` — line 545

### `logFirstSideEffect(source, id, { line, column })` — line 551

### `logIllegalIdentifierAsName(name)` — line 557

### `logIllegalImportReassignment(name, importingId)` — line 564

### `logInconsistentImportAttributes(existingAttributes, newAttributes, source, importer)` — line 570

### `logInvalidAnnotation(comment, id, type)` — line 582

### `logInputHookInOutputPlugin(pluginName, hookName)` — line 590

### `logCannotAssignModuleToChunk(moduleId, assignToAlias, currentAlias)` — line 596

### `tweakStackMessage(error, oldMessage)` — line 602

### `logInvalidExportOptionValue(optionValue)` — line 609

### `logIncompatibleExportOptionValue(optionValue, keys, entryModule)` — line 616

### `logInternalIdCannotBeExternal(source, importer)` — line 623

### `logImportOptionsAreInvalid(importer)` — line 629

### `logImportAttributeIsInvalid(importer)` — line 635

### `logInvalidLogPosition(plugin)` — line 641

### `logInvalidOption(option, urlSnippet, explanation, value)` — line 647

### `logInvalidAddonPluginHook(hook, plugin)` — line 654

### `logInvalidFunctionPluginHook(hook, plugin)` — line 662

### `logInvalidRollupPhaseForChunkEmission()` — line 670

### `logInvalidSetAssetSourceCall()` — line 676

### `logInvalidFormatForTopLevelAwait(id, format)` — line 682

### `logMissingEntryExport(binding, exporter)` — line 689

### `logMissingExport(binding, importingModule, exporter, missingButExportExists)` — line 698

### `logMissingGlobalName(externalId, guess)` — line 718

### `logImplicitDependantCannotBeExternal(unresolvedId, implicitlyLoadedBefore)` — line 727

### `logUnresolvedImplicitDependant(unresolvedId, implicitlyLoadedBefore)` — line 733

### `logImplicitDependantIsNotIncluded(module)` — line 739

### `logMissingJsxExport(name, exporter, importer)` — line 748

### `logMissingNameOptionForIifeExport()` — line 758

### `logMissingNameOptionForUmdExport()` — line 765

### `logMissingNodeBuiltins(externalBuiltins)` — line 772

### `logMissingFileOrDirOption()` — line 779

### `logMixedExport(facadeModuleId, name)` — line 786

### `logModuleLevelDirective(directive, id)` — line 794

### `logNamespaceConflict(binding, reexportingModuleId, sources)` — line 801

### `logNonExternalSourcePhaseImport(source, importer)` — line 810

### `logNoTransformMapOrAstWithoutCode(pluginName)` — line 817

### `logOptimizeChunkStatus(chunks, smallChunks, pointInTime)` — line 824

### `logParseError(message, pos)` — line 832

### `logRedeclarationError(name)` — line 835

### `logReservedNamespace(namespace)` — line 841

### `logModuleParseError(error, moduleId)` — line 847

### `logPluginError(error, plugin, { hook, id } = {})` — line 863

### `logShimmedExport(id, binding)` — line 880

### `logSourcePhaseFormatUnsupported(outputFormat, chunkId, dependencyId)` — line 888

### `logSourcemapBroken(plugin)` — line 895

### `logConflictingSourcemapSources(filename)` — line 903

### `logInvalidSourcemapForError(error, id, column, line, pos)` — line 909

### `logSyntheticNamedExportsNeedNamespaceExport(id, syntheticNamedExportsOption)` — line 923

### `logThisIsUndefined()` — line 932

### `logUnexpectedNamedImport(id, imported, isReexport)` — line 939

### `logUnexpectedNamespaceReexport(id)` — line 948

### `logUnknownOption(optionType, unknownOptions, validOptions)` — line 956

### `logEntryCannotBeExternal(unresolvedId)` — line 962

### `logExternalModulesCannotBeIncludedInManualChunks(source)` — line 968

### `logExternalModulesCannotBeTransformedToModules(source)` — line 974

### `logUnresolvedEntry(unresolvedId)` — line 980

### `logUnresolvedImport(source, importer)` — line 986

### `logUnresolvedImportTreatedAsExternal(source, importer)` — line 994

### `logUnusedExternalImports(externalId, names, importers)` — line 1003

### `logFailedValidation(message)` — line 1015

### `warnDeprecation(deprecation, urlSnippet, activeDeprecation, options, plugin)` — line 1021

### `warnDeprecationWithOptions(deprecation, urlSnippet, activeDeprecation, log, strictDeprecations, plugin)` — line 1024

### `convertProgram(buffer)` — line 1125

### `panicError(position, buffer)` — line 1141

### `parseError(position, buffer)` — line 1149

### `arrayExpression(position, buffer)` — line 1157

### `arrayPattern(position, buffer)` — line 1165

### `arrowFunctionExpression(position, buffer)` — line 1173

### `assignmentExpression(position, buffer)` — line 1189

### `assignmentPattern(position, buffer)` — line 1199

### `awaitExpression(position, buffer)` — line 1208

### `binaryExpression(position, buffer)` — line 1216

### `blockStatement(position, buffer)` — line 1226

### `breakStatement(position, buffer)` — line 1234

### `callExpression(position, buffer)` — line 1243

### `catchClause(position, buffer)` — line 1256

### `chainExpression(position, buffer)` — line 1266

### `classBody(position, buffer)` — line 1274

### `classDeclaration(position, buffer)` — line 1282

### `classExpression(position, buffer)` — line 1295

### `conditionalExpression(position, buffer)` — line 1308

### `continueStatement(position, buffer)` — line 1318

### `debuggerStatement(position, buffer)` — line 1327

### `decorator(position, buffer)` — line 1334

### `directive(position, buffer)` — line 1342

### `doWhileStatement(position, buffer)` — line 1351

### `emptyStatement(position, buffer)` — line 1360

### `exportAllDeclaration(position, buffer)` — line 1367

### `exportDefaultDeclaration(position, buffer)` — line 1378

### `exportNamedDeclaration(position, buffer)` — line 1386

### `exportSpecifier(position, buffer)` — line 1399

### `expressionStatement(position, buffer)` — line 1410

### `forInStatement(position, buffer)` — line 1418

### `forOfStatement(position, buffer)` — line 1428

### `forStatement(position, buffer)` — line 1440

### `functionDeclaration(position, buffer)` — line 1454

### `functionExpression(position, buffer)` — line 1471

### `identifier(position, buffer)` — line 1488

### `ifStatement(position, buffer)` — line 1496

### `importAttribute(position, buffer)` — line 1507

### `importDeclaration(position, buffer)` — line 1516

### `importDefaultSpecifier(position, buffer)` — line 1528

### `importExpression(position, buffer)` — line 1536

### `importNamespaceSpecifier(position, buffer)` — line 1548

### `importSpecifier(position, buffer)` — line 1556

### `jsxAttribute(position, buffer)` — line 1567

### `jsxClosingElement(position, buffer)` — line 1577

### `jsxClosingFragment(position, buffer)` — line 1585

### `jsxElement(position, buffer)` — line 1592

### `jsxEmptyExpression(position, buffer)` — line 1603

### `jsxExpressionContainer(position, buffer)` — line 1610

### `jsxFragment(position, buffer)` — line 1618

### `jsxIdentifier(position, buffer)` — line 1628

### `jsxMemberExpression(position, buffer)` — line 1636

### `jsxNamespacedName(position, buffer)` — line 1645

### `jsxOpeningElement(position, buffer)` — line 1654

### `jsxOpeningFragment(position, buffer)` — line 1665

### `jsxSpreadAttribute(position, buffer)` — line 1674

### `jsxSpreadChild(position, buffer)` — line 1682

### `jsxText(position, buffer)` — line 1690

### `labeledStatement(position, buffer)` — line 1699

### `literalBigInt(position, buffer)` — line 1708

### `literalBoolean(position, buffer)` — line 1719

### `literalNull(position, buffer)` — line 1730

### `literalNumber(position, buffer)` — line 1739

### `literalRegExp(position, buffer)` — line 1749

### `literalString(position, buffer)` — line 1761

### `logicalExpression(position, buffer)` — line 1771

### `memberExpression(position, buffer)` — line 1781

### `metaProperty(position, buffer)` — line 1793

### `methodDefinition(position, buffer)` — line 1802

### `newExpression(position, buffer)` — line 1816

### `objectExpression(position, buffer)` — line 1827

### `objectPattern(position, buffer)` — line 1835

### `privateIdentifier(position, buffer)` — line 1843

### `program(position, buffer)` — line 1851

### `property(position, buffer)` — line 1862

### `propertyDefinition(position, buffer)` — line 1878

### `restElement(position, buffer)` — line 1892

### `returnStatement(position, buffer)` — line 1900

### `sequenceExpression(position, buffer)` — line 1909

### `spreadElement(position, buffer)` — line 1917

### `staticBlock(position, buffer)` — line 1925

### `superElement(position, buffer)` — line 1933

### `switchCase(position, buffer)` — line 1940

### `switchStatement(position, buffer)` — line 1950

### `taggedTemplateExpression(position, buffer)` — line 1959

### `templateElement(position, buffer)` — line 1968

### `templateLiteral(position, buffer)` — line 1981

### `thisExpression(position, buffer)` — line 1990

### `throwStatement(position, buffer)` — line 1997

### `tryStatement(position, buffer)` — line 2005

### `unaryExpression(position, buffer)` — line 2017

### `updateExpression(position, buffer)` — line 2027

### `variableDeclaration(position, buffer)` — line 2038

### `variableDeclarator(position, buffer)` — line 2047

### `whileStatement(position, buffer)` — line 2057

### `yieldExpression(position, buffer)` — line 2066

### `convertNode(position, buffer)` — line 2078

### `convertNodeList(position, buffer)` — line 2088

### `getAstBuffer(astBuffer)` — line 2100

### `formatAttributes(attributes)` — line 576

### `convertAnnotations(position, buffer)` — line 1106

### `convertAnnotation(position, buffer)` — line 1116

### `parseAst(input, { allowReturnOutsideFunction = false, jsx = false } = {})` — line 2121

### `parseAstAsync(input, { allowReturnOutsideFunction = false, jsx = false, signal } = {})` — line 2122
