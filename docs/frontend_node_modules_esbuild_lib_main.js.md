# `main.js`

> **Path:** `frontend/node_modules/esbuild/lib/main.js`  
> **Last documented:** 2026-06-29 18:02

---

## Functions

### `encodePacket(packet)` — line 50

### `decodePacket(bytes)` — line 89

### `readUInt32LE(buffer, offset)` — line 207

### `writeUInt32LE(buffer, value, offset)` — line 210

### `validateAndJoinStringArray(values, what)` — line 221

### `getFlag(object, keys, key, mustBeFn)` — line 248

### `checkForInvalidFlags(object, keys, where)` — line 256

### `validateInitializeOptions(options)` — line 263

### `validateMangleCache(mangleCache)` — line 275

### `pushLogFlags(flags, options, keys, isTTY2, logLevelDefault)` — line 290

### `validateStringValue(value, what, key)` — line 299

### `pushCommonFlags(flags, options, keys)` — line 305

### `flagsForBuildOptions(callName, options, isTTY2, logLevelDefault, writeDefault)` — line 390

### `flagsForTransformOptions(callName, options, isTTY2, logLevelDefault)` — line 535

### `createChannel(streamIn)` — line 557

### `buildOrContextImpl(callName, buildKey, sendRequest, sendResponse, refs, streamIn, requestCallbacks, options, isTTY2, defaultWD2, callback)` — line 829

### `buildOrContextContinue(requestPlugins, runOnEndCallbacks, scheduleOnDisposeCallbacks)` — line 883

### `createObjectStash()` — line 1362

### `extractCallerV8(e, streamIn, ident)` — line 1380

### `extractErrorMessageV8(e, streamIn, stash, note, pluginName)` — line 1398

### `parseStackLinesV8(streamIn, lines, ident)` — line 1411

### `failureErrorWithLog(text, errors, warnings)` — line 1456

### `replaceDetailsInMessages(messages, stash)` — line 1482

### `sanitizeLocation(location, where, terminalWidth)` — line 1488

### `sanitizeMessages(messages, property, stash, fallbackPluginName, terminalWidth)` — line 1518

### `sanitizeStringArray(values, property)` — line 1556

### `sanitizeStringMap(map, property)` — line 1564

### `convertOutputFiles({ path: path3, contents, hash })` — line 1573

### `jsRegExpToGoRegExp(regexp)` — line 1589

### `pkgAndSubpathForCurrentPlatform()` — line 1635

### `pkgForSomeOtherPlatform()` — line 1655

### `downloadedBinPath(pkg, subpath)` — line 1676

### `generateBinPath()` — line 1680

### `__export(target, all)` — line 8

### `__copyProps(to, from, except, desc)` — line 12

### `__toESM(mod, isNodeMode, target)` — line 20

### `__toCommonJS(mod)` — line 28

### `visit(value)` — line 51

### `canBeAnything()` — line 230

### `mustBeBoolean(value)` — line 231

### `mustBeString(value)` — line 232

### `mustBeRegExp(value)` — line 233

### `mustBeInteger(value)` — line 234

### `mustBeValidPortNumber(value)` — line 235

### `mustBeFunction(value)` — line 236

### `mustBeArray(value)` — line 237

### `mustBeArrayOfStrings(value)` — line 238

### `mustBeObject(value)` — line 239

### `mustBeEntryPoints(value)` — line 240

### `mustBeWebAssemblyModule(value)` — line 241

### `mustBeObjectOrNull(value)` — line 242

### `mustBeStringOrBoolean(value)` — line 243

### `mustBeStringOrObject(value)` — line 244

### `mustBeStringOrArrayOfStrings(value)` — line 245

### `mustBeStringOrUint8Array(value)` — line 246

### `mustBeStringOrURL(value)` — line 247

### `readFromStdout(chunk)` — line 565

### `afterClose(error)` — line 589

### `sendRequest(refs, value, callback)` — line 598

### `sendResponse(id, value)` — line 611

### `handleRequest(id, request)` — line 615

### `handleIncomingPacket(bytes)` — line 642

### `buildOrContext({ callName, refs, options, isTTY: isTTY2, defaultWD: defaultWD2, callback })` — line 661

### `transform2({ callName, refs, input, options, isTTY: isTTY2, fs: fs3, callback })` — line 700

### `start(inputPath)` — line 702

### `next()` — line 722

### `formatMessages2({ callName, refs, messages, options, callback })` — line 780

### `analyzeMetafile2({ callName, refs, metafile, options, callback })` — line 801

### `handleError(e, pluginName)` — line 832

### `buildResponseToResult(response, callback2)` — line 910

### `triggerAnotherBuild()` — line 967

### `handlePlugins(buildKey, sendRequest, sendResponse, refs, streamIn, requestCallbacks, initialOptions, plugins, details)` — line 1071

### `resolve(path3, options = {})` — line 1099

### `runOnEndCallbacks(result, done)` — line 1307

### `scheduleOnDisposeCallbacks()` — line 1349

### `isValidBinaryPath(x)` — line 1600

### `esbuildCommandAndArgs()` — line 1809

### `isTTY()` — line 1828

### `build(options)` — line 1876

### `context(buildOptions)` — line 1877

### `transform(input, options)` — line 1878

### `formatMessages(messages, options)` — line 1879

### `analyzeMetafile(messages, options)` — line 1880

### `buildSync(options)` — line 1881

### `transformSync(input, options)` — line 1900

### `formatMessagesSync(messages, options)` — line 1920

### `analyzeMetafileSync(metafile, options)` — line 1938

### `stop()` — line 1956

### `initialize(options)` — line 1962

### `ensureServiceIsRunning()` — line 1975

### `runServiceSync(callback)` — line 2069

### `randomFileName()` — line 2095

### `startWorkerThreadService(worker_threads2)` — line 2099

### `fakeBuildError(text)` — line 2116

### `validateBuildSyncOptions(options)` — line 2124

### `applyProperties(object, properties)` — line 2129

### `runCallSync(command, args)` — line 2134

### `startSyncServiceWorker()` — line 2171

### `extractProperties(object)` — line 2174
