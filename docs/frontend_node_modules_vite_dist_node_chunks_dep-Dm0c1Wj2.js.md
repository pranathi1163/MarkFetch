# `dep-Dm0c1Wj2.js`

> **Path:** `frontend/node_modules/vite/dist/node/chunks/dep-Dm0c1Wj2.js`  
> **Last documented:** 2026-06-29 18:03

---

## Dependencies / Imports

- `${HELPERS_ID}`
- `${clientPublicPath}`
- `${dynamicImportHelperId}`
- `${id}`
- `${normalizePath$3(
              path$b.relative(process.cwd(), importerFile)
            )}`
- `${preloadHelperId}`
- `${url}`
- `${wasmHelperId}`
- `../constants.js`
- `assert`
- `buffer`
- `child_process`
- `crypto`
- `esbuild`
- `events`
- `fdir`
- `foo`
- `fs`
- `http`
- `https`
- `module`
- `net`
- `node:assert`
- `node:buffer`
- `node:child_process`
- `node:crypto`
- `node:dns`
- `node:events`
- `node:fs`
- `node:fs/promises`
- `node:http`
- `node:https`
- `node:module`
- `node:net`
- `node:os`
- `node:path`
- `node:perf_hooks`
- `node:process`
- `node:querystring`
- `node:readline`
- `node:url`
- `node:util`
- `node:v8`
- `node:worker_threads`
- `node:zlib`
- `os`
- `path`
- `picomatch`
- `rollup/parseAst`
- `stream`
- `tinyglobby`
- `tls`
- `tty`
- `url`
- `util`
- `vite/module-runner`
- `worker_threads`
- `zlib`

## Functions

### `getDefaultExportFromCjs(x)` — line 56

### `cleanUrl(url)` — line 160

### `splitFileAndPostfix(path)` — line 163

### `withTrailingSlash(path)` — line 167

### `promiseWithResolvers()` — line 173

### `extractAssignedNames(param)` — line 412

### `attachScopes(ast, propertyName = 'scope')` — line 451

### `foo()` — line 456

### `isArray(arg)` — line 524

### `ensureArray(thing)` — line 527

### `normalizePath(filename)` — line 536

### `createFilter(include, exclude, options)` — line 554

### `result(id)` — line 571

### `makeLegalIdentifier(str)` — line 601

### `serializeArray(arr, indent, baseIndent)` — line 614

### `serializeObject(obj, indent, baseIndent)` — line 623

### `serialize(obj, indent, baseIndent)` — line 634

### `isWellFormedString(input)` — line 666

### `dataToEsm(data, options = {})` — line 673

### `decodeInteger(reader, relative)` — line 766

### `encodeInteger(builder, num, relative)` — line 783

### `hasMoreVlq(reader, max)` — line 795

### `decode(mappings)` — line 858

### `sort(line)` — line 904

### `getBtoa()` — line 1134

### `guessIndent(code)` — line 1173

### `getRelativePath(from, to)` — line 1199

### `getLocator(source)` — line 1224

### `locate(index)` — line 1233

### `getReplacement(match, str)` — line 2122

### `matchAll(re, str)` — line 2136

### `isReference(node, parent)` — line 2215

### `tryParse(parse, code, id)` — line 2250

### `hasCjsKeywords(code, ignoreGlobal)` — line 2263

### `analyzeTopLevelStatements(parse, code, id)` — line 2271

### `deconflict(scopes, globals, identifier)` — line 2318

### `getName(id)` — line 2336

### `normalizePathSlashes(path)` — line 2344

### `capitalize(name)` — line 2351

### `getStrictRequiresFilter({ strictRequires })` — line 2355

### `getPackageEntryPoint(dirPath)` — line 2378

### `getDynamicRequireModules(patterns, dynamicRequireRoot)` — line 2403

### `getDynamicModuleRegistry(isDynamicRequireModulesEnabled, dynamicRequireModules, commonDir, ignoreDynamicRequires)` — line 2444

### `getDynamicModules()` — line 2475

### `handleRequire(path)` — line 2482

### `commonjsResolve(path, originalModuleDir)` — line 2499

### `isPossibleNodeModulesPath(modulePath)` — line 2533

### `normalize(path)` — line 2543

### `getDefaultExportFromNamespaceIfPresent(n)` — line 2601

### `getDefaultExportFromNamespaceIfNotNamed(n)` — line 2605

### `getAugmentedNamespace(n)` — line 2609

### `a()` — line 2613

### `getHelpersModule()` — line 2635

### `getUnknownRequireProxy(id, requireReturnsDefault)` — line 2639

### `getStaticRequireProxy(id, requireReturnsDefault, loadModule)` — line 2655

### `getEntryProxy(id, defaultIsModuleExports, getModuleInfo, shebang)` — line 2682

### `getEsImportProxy(id, defaultIsModuleExports, moduleSideEffects)` — line 2702

### `getCandidatesForExtension(resolved, extension)` — line 2727

### `getCandidates(resolved, extensions)` — line 2731

### `resolveExtensions(importee, importer, extensions)` — line 2738

### `getResolveId(extensions, isPossibleCjsId)` — line 2757

### `getRequireResolver(extensions, detectCyclesAndConditional, currentlyResolving)` — line 2872

### `validateVersion(actualVersion, peerDependencyVersion, name)` — line 3075

### `not(value)` — line 3122

### `equals(a, b, strict)` — line 3126

### `isTruthy(node)` — line 3133

### `isFalsy(node)` — line 3141

### `getKeypath(node)` — line 3145

### `getDefineCompiledEsmType(node)` — line 3166

### `getDefinePropertyCallName(node, targetName)` — line 3180

### `isShorthandProperty(parent)` — line 3215

### `wrapCode(magicString, uses, moduleName, exportsName, indentExclusionRanges)` — line 3219

### `rewriteExportsAndGetExportsBlock(magicString, moduleName, exportsName, exportedExportsName, wrapped, moduleExportsAssignments, firstTopLevelModuleExportsAssignment, exportsAssignmentsByName, topLevelAssignments, defineCompiledEsmExpressions, deconflictedExportNames, code, HELPERS_NAME, exportMode, defaultIsModuleExports, usesRequireWrapper, requireName)` — line 3239

### `getExportsWhenUsingRequireWrapper(magicString, wrapped, exportMode, exports, moduleExportsAssignments, exportsAssignmentsByName, moduleName, exportsName, requireName, defineCompiledEsmExpressions)` — line 3322

### `getExportsForReplacedModuleExports(magicString, exports, exportDeclarations, moduleExportsAssignments, firstTopLevelModuleExportsAssignment, exportsName, defaultIsModuleExports, HELPERS_NAME)` — line 3362

### `getDefaultExportDeclaration(exportedExportsName, defaultIsModuleExports, HELPERS_NAME)` — line 3382

### `getExports(magicString, exports, exportDeclarations, moduleExportsAssignments, exportsAssignmentsByName, deconflictedExportNames, topLevelAssignments, moduleName, exportsName, exportedExportsName, defineCompiledEsmExpressions, HELPERS_NAME, defaultIsModuleExports, exportMode)` — line 3392

### `rewriteModuleExportsAssignments(magicString, moduleExportsAssignments, exportsName)` — line 3468

### `replaceDefineCompiledEsmExpressionsAndGetIfRestorable(defineCompiledEsmExpressions, magicString, exportMode, moduleName, exportsName)` — line 3474

### `isRequireExpression(node, scope)` — line 3495

### `isRequire(node, scope)` — line 3505

### `isModuleRequire({ object, property }, scope)` — line 3512

### `hasDynamicArguments(node)` — line 3522

### `isNodeRequirePropertyAccess(parent)` — line 3532

### `getRequireStringArg(node)` — line 3536

### `getRequireHandlers()` — line 3542

### `addRequireExpression(sourceId, node, scope, usesReturnValue, isInsideTryBlock, isInsideConditional, toBeRemoved)` — line 3545

### `rewriteRequireExpressionsAndGetImportBlock(magicString, topLevelDeclarations, reassignedNames, helpersName, dynamicRequireName, moduleName, exportsName, id, exportMode, resolveRequireSourcesAndUpdateMeta, needsRequireWrapper, isEsModule, isDynamicRequireModulesEnabled, getIgnoreTryCatchRequireStatementMode, commonjsMeta)` — line 3565

### `collectSources(requireExpressions)` — line 3629

### `processRequireExpressions(imports, requireTargets, requiresBySource, getIgnoreTryCatchRequireStatementMode, magicString)` — line 3642

### `getGenerateRequireName()` — line 3692

### `transformCommonjs(parse, code, id, isEsModule, ignoreGlobal, ignoreRequire, ignoreDynamicRequires, getIgnoreTryCatchRequireStatementMode, sourceMap, isDynamicRequireModulesEnabled, dynamicRequireModules, commonDir, astCache, defaultIsModuleExports, needsRequireWrapper, resolveRequireSourcesAndUpdateMeta, isRequired, checkDynamicRequire, commonjsMeta)` — line 3720

### `commonjs(options = {})` — line 4270

### `transformAndCheckExports(code, id)` — line 4346

### `perEnvironmentState(initial)` — line 4560

### `isAbsoluteUrl(input)` — line 4596

### `isSchemeRelativeUrl(input)` — line 4599

### `isAbsolutePath(input)` — line 4602

### `isFileUrl(input)` — line 4605

### `isRelative(input)` — line 4608

### `parseAbsoluteUrl(input)` — line 4611

### `parseFileUrl(input)` — line 4615

### `makeUrl(scheme, user, host, port, path, query, hash)` — line 4620

### `stripPathFilename(path)` — line 4662

### `mergePaths(url, base)` — line 4670

### `stripFilename(path)` — line 4815

### `maybeSort(mappings, owned)` — line 4828

### `nextUnsortedSegmentLine(mappings, start)` — line 4841

### `isSorted(line)` — line 4848

### `sortSegments(line, owned)` — line 4856

### `sortComparator(a, b)` — line 4861

### `binarySearch(haystack, needle, low, high)` — line 4882

### `upperBound(haystack, needle, index)` — line 4900

### `lowerBound(haystack, needle, index)` — line 4907

### `memoizedState()` — line 4914

### `memoizedBinarySearch(haystack, needle, state, key)` — line 4925

### `encodedMappings(map)` — line 4991

### `decodedMappings(map)` — line 4999

### `traceSegment(map, line, column)` — line 5007

### `decodedMap(map)` — line 5048

### `encodedMap(map)` — line 5055

### `clone(map, mappings)` — line 5058

### `OMapping(source, line, column, name)` — line 5070

### `traceSegmentInternal(segments, memo, line, column, bias)` — line 5073

### `get(setarr, key)` — line 5109

### `put(setarr, key)` — line 5116

### `remove(setarr, key)` — line 5128

### `cast(map)` — line 5167

### `setSourceContent(map, source, content)` — line 5181

### `setIgnore(map, source, ignore = true)` — line 5186

### `toDecodedMap(map)` — line 5200

### `toEncodedMap(map)` — line 5218

### `addSegmentInternal(skipable, map, genLine, genColumn, source, sourceLine, sourceColumn, name, content)` — line 5223

### `getLine(mappings, index)` — line 5243

### `getColumnIndex(line, genColumn)` — line 5249

### `insert(array, index, value)` — line 5258

### `removeEmptyFinalLines(mappings)` — line 5264

### `skipSourceless(line, index)` — line 5274

### `skipSource(line, index, sourcesIndex, sourceLine, sourceColumn, namesIndex)` — line 5285

### `SegmentObject(source, line, column, name, content, ignore)` — line 5303

### `Source(map, sources, source, content, ignore)` — line 5306

### `MapSource(map, sources)` — line 5319

### `OriginalSource(source, content, ignore)` — line 5326

### `traceMappings(tree)` — line 5333

### `originalPositionFor(source, line, column, name)` — line 5370

### `asArray(value)` — line 5385

### `buildSourceMapTree(input, loader)` — line 5401

### `remapping(input, loader, options)` — line 5487

### `requireMs()` — line 5502

### `parse(str)` — line 5548

### `fmtShort(ms)` — line 5613

### `fmtLong(ms)` — line 5638

### `plural(ms, msAbs, n, name)` — line 5659

### `requireCommon()` — line 5669

### `setup(env)` — line 5677

### `selectColor(namespace)` — line 5711

### `createDebug(namespace)` — line 5730

### `debug(...args)` — line 5736

### `extend(namespace, delimiter)` — line 5819

### `enable(namespaces)` — line 5832

### `matchesTemplate(search, template)` — line 5862

### `disable()` — line 5903

### `enabled(name)` — line 5919

### `coerce(val)` — line 5942

### `destroy()` — line 5953

### `useColors()` — line 6121

### `formatArgs(args)` — line 6133

### `getDate()` — line 6148

### `log(...args)` — line 6159

### `save(namespaces)` — line 6169

### `load()` — line 6186

### `init(debug)` — line 6197

### `invalidatePackageData(packageCache, pkgPath)` — line 6242

### `resolvePackageData(pkgName, basedir, preserveSymlinks = false, packageCache)` — line 6250

### `findNearestPackageData(basedir, packageCache)` — line 6303

### `findNearestMainPackageData(basedir, packageCache)` — line 6327

### `loadPackageData(pkgPath)` — line 6334

### `getResolveCacheKey(key, options)` — line 6372

### `findNearestNodeModules(basedir)` — line 6381

### `watchPackageDataPlugin(packageCache)` — line 6393

### `getRpdCache(packageCache, pkgName, basedir, originalBasedir, preserveSymlinks)` — line 6425

### `setRpdCache(packageCache, pkgData, pkgName, basedir, originalBasedir, preserveSymlinks)` — line 6435

### `getRpdCacheKey(pkgName, basedir, preserveSymlinks)` — line 6441

### `getFnpdCache(packageCache, basedir, originalBasedir)` — line 6444

### `setFnpdCache(packageCache, pkgData, basedir, originalBasedir)` — line 6454

### `getFnpdCacheKey(basedir)` — line 6460

### `traverseBetweenDirs(longerDir, shorterDir, cb)` — line 6463

### `isBuiltin(builtins, id)` — line 6495

### `createIsBuiltin(builtins)` — line 6503

### `isNodeLikeBuiltin(id)` — line 6518

### `isNodeBuiltin(id)` — line 6521

### `moduleListContains(moduleList, id)` — line 6528

### `isOptimizable(id, optimizeDeps)` — line 6533

### `createDebugger(namespace, options = {})` — line 6544

### `testCaseInsensitiveFS()` — line 6563

### `fsPathFromId(id)` — line 6593

### `fsPathFromUrl(url)` — line 6599

### `isParentDirectory(dir, file)` — line 6602

### `isSameFileUri(file1, file2)` — line 6606

### `removeImportQuery(url)` — line 6638

### `removeDirectQuery(url)` — line 6641

### `removeUrlQuery(url)` — line 6646

### `injectQuery(url, queryToInject)` — line 6649

### `removeTimestampQuery(url)` — line 6658

### `asyncReplace(input, re, replacer)` — line 6661

### `timeFrom(start, subtract = 0)` — line 6673

### `prettifyUrl(url, root)` — line 6684

### `isDefined(value)` — line 6700

### `tryStatSync(file)` — line 6703

### `lookupFile(dir, fileNames)` — line 6709

### `isFilePathESM(filePath, packageCache)` — line 6720

### `posToNumber(source, pos)` — line 6741

### `numberToPos(source, offset)` — line 6751

### `generateCodeFrame(source, start = 0, end)` — line 6772

### `isFileReadable(filename)` — line 6812

### `emptyDir(dir, skip)` — line 6824

### `copyDir(srcDir, destDir)` — line 6859

### `recursiveReaddir(dir)` — line 6876

### `windowsMappedRealpathSync(path2)` — line 6906

### `windowsSafeRealPathSync(path2)` — line 6917

### `optimizeSafeRealPathSync()` — line 6924

### `ensureWatchedFile(watcher, file, root)` — line 6952

### `joinSrcset(ret)` — line 6959

### `parseSrcset(string)` — line 6964

### `processSrcSet(srcs, replacer)` — line 6971

### `processSrcSetSync(srcs, replacer)` — line 6979

### `escapeToLinuxLikePath(path2)` — line 6990

### `unescapeToLinuxLikePath(path2)` — line 7000

### `combineSourcemaps(filename, sourcemapList)` — line 7015

### `loader(sourcefile)` — line 7036

### `unique(arr)` — line 7053

### `getLocalhostAddressIfDiffersFromDNS()` — line 7056

### `diffDnsOrderChange(oldUrls, newUrls)` — line 7064

### `resolveHostname(optionsHost)` — line 7067

### `resolveServerUrls(server, options, httpsOptions, config)` — line 7085

### `extractHostnamesFromSubjectAltName(subjectAltName)` — line 7135

### `arraify(target)` — line 7167

### `getHash(text, length = 8)` — line 7179

### `emptyCssComments(raw)` — line 7193

### `backwardCompatibleWorkerPlugins(plugins)` — line 7196

### `deepClone(value)` — line 7205

### `mergeWithDefaultsRecursively(defaults, values)` — line 7227

### `mergeWithDefaults(defaults, values)` — line 7245

### `mergeConfigRecursively(defaults, overrides, rootPath)` — line 7249

### `mergeConfig(defaults, overrides, isRoot = true)` — line 7296

### `mergeAlias(a, b)` — line 7302

### `normalizeAlias(o = [])` — line 7310

### `normalizeSingleAlias({
  find, replacement, customResolver
})` — line 7318

### `transformStableResult(s, id, config)` — line 7336

### `asyncFlatten(arr)` — line 7342

### `stripBomTag(content)` — line 7348

### `shouldServeFile(filePath, root)` — line 7359

### `hasCorrectCase(file, assets)` — line 7363

### `joinUrlSegments(a, b)` — line 7371

### `removeLeadingSlash(str)` — line 7383

### `stripBase(path2, base)` — line 7386

### `arrayEqual(a, b)` — line 7393

### `evalValue(rawValue)` — line 7401

### `getNpmPackageName(importPath)` — line 7410

### `getPkgName(name)` — line 7419

### `escapeRegex(str)` — line 7423

### `getPackageManagerCommand(type = "install")` — line 7426

### `isDevServer(server)` — line 7439

### `createSerialPromiseQueue()` — line 7442

### `sortObjectKeys(obj)` — line 7457

### `displayTime(time)` — line 7464

### `encodeURIPath(uri)` — line 7479

### `partialEncodeURIPath(uri)` — line 7485

### `getServerUrlByHost(resolvedUrls, host)` — line 7513

### `clearScreen()` — line 7535

### `getTimeFormatter()` — line 7543

### `createLogger(level = "info", options = {})` — line 7551

### `format(type, msg, options2 = {})` — line 7565

### `output(type, msg, options2 = {})` — line 7581

### `printServerUrls(urls, optionsHost, info)` — line 7640

### `buildReporterPlugin(config)` — line 7661

### `getCompressedSize(code)` — line 7713

### `writeLine(output)` — line 7910

### `throttle(fn)` — line 7922

### `makePromise()` — line 7951

### `resolveTSConfigJson(filename, cache)` — line 7965

### `resolveReferencedTSConfigFiles(result, options)` — line 8043

### `resolveSolutionTSConfig(filename, result)` — line 8058

### `isIncluded(filename, result)` — line 8082

### `isGlobMatch(filename, dir, patterns, allowJs)` — line 8112

### `pattern2regex(resolvedPattern, allowJs)` — line 8211

### `replaceTokens(result)` — line 8247

### `find(filename, options)` — line 8264

### `findUp(dir, { resolve, reject, promise }, options)` — line 8288

### `toJson(tsconfigJson)` — line 8338

### `stripDanglingComma(pseudoJson)` — line 8355

### `isEscaped(jsonString, quotePosition)` — line 8396

### `strip(string, start, end)` — line 8414

### `stripJsonComments(jsonString)` — line 8425

### `stripBom(string)` — line 8483

### `getParsedDeep(filename, cache, options)` — line 8548

### `normalizeTSConfig(tsconfig, dir)` — line 8613

### `parseReferences(result, options)` — line 8628

### `parseExtends(result, cache)` — line 8649

### `resolveExtends(extended, from)` — line 8719

### `extendTSConfig(extending, extended)` — line 8765

### `rebaseRelative(key, value, prependPath)` — line 8830

### `rebasePath(value, prependPath)` — line 8847

### `applyDefaults(tsconfig, tsconfigFile)` — line 8896

### `isJSConfig(configFileName)` — line 8916

### `transformWithEsbuild(code, filename, options, inMap, config, watcher)` — line 9058

### `esbuildPlugin(config)` — line 9169

### `resolveEsbuildTranspileOptions(config, format)` — line 9270

### `prettifyMessage(m, code)` — line 9337

### `getTSConfckCache(config)` — line 9348

### `loadTsconfigJsonForFile(filename, config)` — line 9359

### `reloadOnTsconfigChange(server, changedFile)` — line 9364

### `createParentFunctionResponder(parentFunctions)` — line 9536

### `genWorkerCode(fn, isModule, parentFunctions)` — line 9589

### `genFakeWorkerArgsAndCode(fn, declareRequire, parentFunctions)` — line 9706

### `terserPlugin(config)` — line 9764

### `resolveEnvironmentPlugins(environment)` — line 9812

### `perEnvironmentPlugin(name, applyToEnvironment)` — line 9833

### `lookup(extn)` — line 10281

### `initPublicFiles(config)` — line 10288

### `getPublicFiles(config)` — line 10304

### `checkPublicFile(url, config)` — line 10307

### `registerCustomMime()` — line 10331

### `renderAssetUrlInJS(pluginContext, chunk, opts, code)` — line 10337

### `assetPlugin(config)` — line 10384

### `fileToDevUrl(environment, id, skipBase = false)` — line 10489

### `getPublicAssetFilename(hash, config)` — line 10519

### `publicFileToBuiltUrl(url, config)` — line 10524

### `isGitLfsPlaceholder(content)` — line 10540

### `fileToBuiltUrl(pluginContext, id, skipPublicCheck = false, forceInline)` — line 10544

### `urlToBuiltUrl(pluginContext, url, importer, forceInline)` — line 10586

### `shouldInline(environment, file, id, content, buildPluginContext, forceInline)` — line 10600

### `assetToDataURL(environment, file, content)` — line 10621

### `svgToDataURL(content)` — line 10635

### `manifestPlugin()` — line 10645

### `getChunkName(chunk)` — line 10670

### `getInternalImports(imports)` — line 10673

### `createChunk(chunk)` — line 10683

### `createAsset(asset, src, isEntry)` — line 10717

### `getChunkOriginalFileName(chunk, root, format)` — line 10767

### `dataURIPlugin()` — line 10782

### `k(A)` — line 10821

### `Q(A, Q)` — line 10821

### `B(A, Q)` — line 10821

### `getCommentRegex()` — line 10828

### `getMapFileCommentRegex()` — line 10836

### `decodeBase64WithBufferFrom(base64)` — line 10853

### `decodeBase64WithNewBuffer(base64)` — line 10857

### `decodeBase64WithAtob(base64)` — line 10864

### `stripComment(sm)` — line 10868

### `readFromFileMap(sm, read)` — line 10872

### `throwError(e)` — line 10888

### `Converter(sm, opts)` — line 10893

### `encodeBase64WithBufferFrom()` — line 10927

### `encodeBase64WithNewBuffer()` — line 10932

### `encodeBase64WithBtoa()` — line 10940

### `makeConverter(sm)` — line 11008

### `computeSourceRoute(map, file)` — line 11066

### `injectSourcesContent(map, file, logger)` — line 11076

### `genSourceMapUrl(map)` — line 11109

### `getCodeWithSourcemap(type, code, map)` — line 11115

### `applySourcemapIgnoreList(map, sourcemapPath, sourcemapIgnoreList, logger)` — line 11130

### `extractSourcemapFromFile(code, filePath)` — line 11153

### `createConvertSourceMapReadMap(originalFileName)` — line 11165

### `getDefaultSearchPlaces(name, sync)` — line 11185

### `parentDir(p)` — line 11212

### `getOptions(name, options, sync)` — line 11271

### `getPackageProp(props, obj)` — line 11304

### `validateFilePath(filepath)` — line 11315

### `validateLoader(loader, ext)` — line 11320

### `lilconfig(name, options)` — line 11332

### `lilconfigSync(name, options)` — line 11487

### `options(config, file)` — line 11713

### `plugins(config, file)` — line 11792

### `processResult(ctx, result)` — line 11859

### `createContext(ctx)` — line 11889

### `yamlLoader(_, content)` — line 11916

### `rc(ctx, path, options)` — line 11981

### `getEntries({ entries, customResolver })` — line 12033

### `getHookFunction(hook)` — line 12051

### `resolveCustomResolver(customResolver)` — line 12060

### `jsonPlugin(options, isBuild)` — line 12111

### `serializeValue(value)` — line 12185

### `extractJsonErrorPosition(errorMessage, inputLength)` — line 12192

### `e(e, n, r)` — line 12200

### `n(n, i, o, f)` — line 12200

### `r(e, n, r)` — line 12200

### `t(e, n, r)` — line 12200

### `o(e, r, t)` — line 12200

### `f(e, r, t)` — line 12200

### `encode(text)` — line 12211

### `encodeQueryValue(input)` — line 12214

### `encodeQueryKey(text)` — line 12217

### `encodeQueryItem(key, value)` — line 12220

### `stringifyQuery(query)` — line 12234

### `clearImports(imports)` — line 12239

### `getImportNames(cleanedImports)` — line 12242

### `formatList(array, type = 'and')` — line 12292

### `createError(sym, value, constructor)` — line 12570

### `makeNodeErrorWithCode(Base, key)` — line 12583

### `NodeError(...parameters)` — line 12589

### `isErrorStackTraceLimitWritable()` — line 12626

### `hideStackFrames(wrappedFunction)` — line 12651

### `getMessage(key, parameters, self)` — line 12687

### `determineSpecificType(value)` — line 12719

### `parseStaticImport(matched)` — line 12747

### `hasESMSyntax(code, opts = {})` — line 12770

### `esbuildDepPlugin(environment, qualified, external)` — line 12805

### `resolveEntry(id)` — line 12910

### `esbuildCjsExternalPlugin(externals, platform)` — line 13001

### `getDefaultResolvedEnvironmentOptions(config)` — line 13042

### `stripLiteralJsTokens(code, options)` — line 13556

### `stripLiteral(code, options)` — line 13625

### `stripLiteralDetailed(code, options)` — line 13628

### `importGlobPlugin(config)` — line 13632

### `parseGlobOptions(rawOpts, optsStartIndex, logger)` — line 13703

### `parseImportGlob(code, importer, root, resolveId, logger)` — line 13768

### `findCorrespondingCloseParenthesisPosition(cleanCode, openPos)` — line 13866

### `transformGlobImport(code, id, root, resolveId, restoreQueryExtension = false, logger)` — line 13890

### `globSafePath(path)` — line 14006

### `lastNthChar(str, n)` — line 14009

### `globSafeResolvedPath(resolved, glob2)` — line 14012

### `toAbsoluteGlob(glob2, root, importer, resolveId)` — line 14023

### `getCommonBase(globsResolved)` — line 14048

### `isVirtualModule(id)` — line 14066

### `devToScanEnvironment(environment)` — line 14097

### `scanImports(environment)` — line 14129

### `disposeContext()` — line 14164

### `computeEntries(environment)` — line 14218

### `prepareEsbuildScanner(environment, entries, deps, missing, scanContext)` — line 14253

### `orderedDependencies(deps)` — line 14281

### `globEntries(pattern, environment)` — line 14286

### `esbuildScanPlugin(environment, depImports, missing, entries)` — line 14311

### `resolveId(id, importer)` — line 14313

### `extractImportPaths(code)` — line 14580

### `shouldExternalizeDep(resolvedId, rawId)` — line 14591

### `isScannable(id, extensions)` — line 14600

### `createOptimizeDepsIncludeResolver(environment)` — line 14604

### `expandGlobIds(id, config)` — line 14630

### `getFirstExportStringValue(obj)` — line 14703

### `nestedResolveBasedir(id, basedir, preserveSymlinks = false)` — line 14714

### `isDepOptimizationDisabled(optimizeDeps2)` — line 14725

### `optimizeDeps(config, force = config.optimizeDeps.force, asCommand = false)` — line 14728

### `optimizeExplicitEnvironmentDeps(environment)` — line 14755

### `initDepsOptimizerMetadata(environment, timestamp)` — line 14771

### `addOptimizedDepInfo(metadata, type, depInfo)` — line 14784

### `loadCachedDepOptimizationMetadata(environment, force = environment.config.optimizeDeps.force ?? false, asCommand = false)` — line 14790

### `discoverProjectDependencies(environment)` — line 14842

### `toDiscoveredDependencies(environment, deps, timestamp)` — line 14866

### `depsLogString(qualifiedIds)` — line 14885

### `runOptimizeDeps(environment, depsInfo)` — line 14888

### `prepareEsbuildOptimizerRun(environment, depsInfo, processingCacheDir, optimizerContext)` — line 15084

### `addManuallyIncludedOptimizeDeps(environment, deps)` — line 15151

### `depsFromOptimizedDepInfo(depsInfo)` — line 15190

### `getOptimizedDepPath(environment, id)` — line 15197

### `getDepsCacheSuffix(environment)` — line 15202

### `getDepsCacheDir(environment)` — line 15205

### `getProcessingDepsCacheDir(environment)` — line 15208

### `getTempSuffix()` — line 15211

### `getDepsCacheDirPrefix(environment)` — line 15216

### `createIsOptimizedDepFile(environment)` — line 15219

### `createIsOptimizedDepUrl(environment)` — line 15223

### `isOptimizedDepUrl(url)` — line 15236

### `parseDepsOptimizerMetadata(jsonMetadata, depsCacheDir)` — line 15240

### `stringifyDepsOptimizerMetadata(metadata, depsCacheDir)` — line 15277

### `esbuildOutputFromId(outputs, id, cacheDirOutputPath)` — line 15311

### `extractExportsData(environment, filePath)` — line 15327

### `needsInterop(environment, id, exportsData, output)` — line 15373

### `isSingleDefaultExport(exports)` — line 15389

### `getConfigHash(environment)` — line 15442

### `getLockfileHash(environment)` — line 15470

### `getDepHash(environment)` — line 15492

### `getOptimizedBrowserHash(hash, deps, timestamp = "")` — line 15502

### `optimizedDepInfoFromId(metadata, id)` — line 15505

### `optimizedDepInfoFromFile(metadata, file)` — line 15508

### `findOptimizedDepInfoInRecord(dependenciesInfo, callbackFn)` — line 15511

### `optimizedDepNeedsInterop(environment, metadata, file)` — line 15519

### `cleanupDepsCacheStaleDirs(config)` — line 15532

### `gracefulRename(from, to, cb)` — line 15553

### `CB(er)` — line 15556

### `shouldExternalize(environment, id, importer)` — line 15598

### `createIsConfiguredAsExternal(environment)` — line 15606

### `createIsExternal(environment)` — line 15675

### `canExternalizeFile(filePath)` — line 15690

### `resolvePlugin(resolveOptions)` — line 15705

### `resolveSubpathImports(id, importer, options)` — line 15903

### `ensureVersionQuery(resolved, id, options, depsOptimizer)` — line 15924

### `tryFsResolve(fsPath, options, tryIndex = true, skipPackageJson = false)` — line 15936

### `tryCleanFsResolve(file, options, tryIndex = true, skipPackageJson = false)` — line 15952

### `tryNodeResolve(id, importer, options, depsOptimizer, externalize)` — line 16023

### `tryOptimizedResolve(depsOptimizer, id, importer, preserveSymlinks, packageCache)` — line 16108

### `resolvePackageEntry(id, { dir, data, setResolvedCache, getResolvedCache }, options)` — line 16138

### `packageEntryFailure(id, details)` — line 16197

### `resolveExportsOrImports(pkg, key, options, type)` — line 16204

### `resolveDeepImport(id, { setResolvedCache, getResolvedCache, dir, data }, options)` — line 16220

### `tryResolveBrowserMapping(id, importer, options, isFilePath, externalize)` — line 16270

### `tryResolveBrowserEntry(dir, data, options)` — line 16308

### `mapWithBrowserField(relativePathInPkgDir, map)` — line 16329

### `equalWithoutSuffix(path2, key, suffix)` — line 16338

### `tryResolveRealFile(file, preserveSymlinks)` — line 16341

### `tryResolveRealFileWithExtensions(filePath, extensions, preserveSymlinks)` — line 16345

### `tryResolveRealFileOrType(file, preserveSymlinks)` — line 16351

### `getRealPath(resolved, preserveSymlinks)` — line 16361

### `isDirectory(path2)` — line 16367

### `optimizedDepsPlugin()` — line 16373

### `throwProcessingError(id)` — line 16428

### `throwOutdatedRequest(id)` — line 16435

### `throwFileNotFoundInOptimizedDep(id)` — line 16442

### `_parseVault(options)` — line 16505

### `_warn(message)` — line 16547

### `_debug(message)` — line 16551

### `_dotenvKey(options)` — line 16555

### `_instructions(result, dotenvKey)` — line 16570

### `_vaultPath(options)` — line 16613

### `_resolveHome(envPath)` — line 16637

### `_configVault(options)` — line 16641

### `configDotenv(options)` — line 16659

### `config(options)` — line 16717

### `decrypt(encrypted, keyStr)` — line 16735

### `populate(processEnv, parsed, options = {})` — line 16767

### `_resolveEscapeSequences(value)` — line 16817

### `expandValue(value, processEnv, runningParsed)` — line 16821

### `getEnvFilesForMode(mode, envDir)` — line 16917

### `loadEnv(mode, envDir, prefixes = "VITE_")` — line 16932

### `resolveEnvPrefix({
  envPrefix = "VITE_"
})` — line 16975

### `warnFutureDeprecation(config, type, extraMessage, stacktrace = true)` — line 17005

### `ignoreDeprecationWarnings(fn)` — line 17031

### `prepareError(err)` — line 17039

### `buildErrorMessage(err, args = [], includeStack = true)` — line 17050

### `cleanStack(stack)` — line 17058

### `logError(server, err)` — line 17061

### `errorMiddleware(server, allowNext = false)` — line 17075

### `viteErrorMiddleware(err, _req, res, next)` — line 17076

### `createWritableStdioStream(fd)` — line 17647

### `callback()` — line 17923

### `cleanup()` — line 17928

### `thunk(fn)` — line 17936

### `listener(event, done)` — line 17950

### `onevent(arg1)` — line 17951

### `attachFinishedListener(msg, callback)` — line 18054

### `onFinish(error)` — line 18059

### `onSocket(socket)` — line 18070

### `attachListener(msg, listener)` — line 18104

### `createListener(msg)` — line 18124

### `patchAssignSocket(res, callback)` — line 18150

### `_assignSocket(socket)` — line 18156

### `parseurl(req)` — line 18198

### `originalurl(req)` — line 18227

### `fastparse(str)` — line 18256

### `fresh(url, parsedUrl)` — line 18314

### `populateStatusesMap(statuses, codes)` — line 18447

### `forEachCode(code)` — line 18450

### `status(code)` — line 18480

### `hasPipeDataListeners(stream)` — line 18520

### `createHtmlDocument(message)` — line 18610

### `getErrorHeaders(err)` — line 18712

### `getErrorMessage(err, status, env)` — line 18738

### `getErrorStatusCode(err)` — line 18762

### `getResourceName(req)` — line 18787

### `getResponseStatusCode(res)` — line 18803

### `headersSent(res)` — line 18822

### `write()` — line 18840

### `setHeaders(res, headers)` — line 18888

### `app(req, res, next)` — line 18978

### `use(route, fn)` — line 19003

### `handle(req, res, out)` — line 19046

### `next(err)` — line 19062

### `listen()` — line 19142

### `call(handle, route, err, req, res, next)` — line 19152

### `logerror(err)` — line 19185

### `getProtohost(url)` — line 19196

### `toObject(val)` — line 19222

### `shouldUseNative()` — line 19230

### `vary(res, field)` — line 19433

### `isString(s)` — line 19465

### `isOriginAllowed(origin, allowedOrigin)` — line 19469

### `configureOrigin(options, req)` — line 19486

### `configureMethods(options)` — line 19523

### `configureCredentials(options)` — line 19534

### `configureAllowedHeaders(options, req)` — line 19544

### `configureExposedHeaders(options)` — line 19567

### `configureMaxAge(options)` — line 19583

### `applyHeaders(headers, res)` — line 19594

### `cors(options, req, res, next)` — line 19609

### `middlewareWrapper(o)` — line 19642

### `corsMiddleware(req, res, next)` — line 19653

### `isExtglob(str)` — line 20133

### `isGlob(str, options)` — line 20279

### `globParent(str, opts)` — line 20314

### `collatePatterns(neg, pos, options)` — line 20597

### `splitToRanges(min, max)` — line 20605

### `rangeToPattern(start, stop, options)` — line 20638

### `splitToPatterns(min, max, tok, options)` — line 20669

### `filterPatterns(arr, comparison, prefix, intersection, options)` — line 20704

### `zip(a, b)` — line 20727

### `compare(a, b)` — line 20733

### `contains(arr, key, val)` — line 20737

### `countNines(min, len)` — line 20741

### `countZeros(integer, zeros)` — line 20745

### `toQuantifier(digits)` — line 20749

### `toCharacterClass(a, b, options)` — line 20757

### `hasPadding(str)` — line 20761

### `padZeros(value, tok, options)` — line 20765

### `createFsWatchInstance(path, options, listener, errHandler, emitRaw)` — line 22181

### `setFSEventsListener(path, realPath, listener, rawEmitter)` — line 22828

### `quote(xs)` — line 24249

### `getVar(env, pre, key)` — line 24337

### `parseInternal(string, env, opts)` — line 24351

### `parseEnvVar()` — line 24400

### `getEditorFromMacProcesses(output)` — line 24635

### `getEditorFromWindowsProcesses(output)` — line 24664

### `getEditorFromLinuxProcesses(output)` — line 24677

### `getArgumentsForPosition(editor, fileName, lineNumber, columnNumber = 1)` — line 24769

### `wrapErrorCallback(cb)` — line 24857

### `isTerminalEditor(editor)` — line 24872

### `parseFile(file)` — line 24883

### `launchEditor(file, specifiedEditor, onErrorCallback)` — line 24902

### `escapeCmdArgs(cmdArgs)` — line 24983

### `doubleQuoteIfNeeded(str)` — line 24992

### `launchEditorMiddleware(req, res)` — line 25046

### `resolveHttpServer({ proxy }, app, httpsOptions)` — line 25073

### `resolveHttpsConfig(https)` — line 25096

### `readFileIfExists(value)` — line 25106

### `httpServerStart(httpServer, serverOptions)` — line 25112

### `setClientErrorHandler(server, logger)` — line 25136

### `calculateOffsetOnce()` — line 25157

### `ssrRewriteStacktrace(stack, moduleGraph)` — line 25168

### `rebindErrorStacktrace(e, stacktrace)` — line 25201

### `ssrFixStacktrace(e, moduleGraph)` — line 25218

### `createHMROptions(environment, options)` — line 25226

### `resolveSourceMapOptions(options)` — line 25242

### `createServerModuleRunner(environment, options = {})` — line 25294

### `ssrLoadModule(url, server, fixStacktrace)` — line 25310

### `instantiateModule(url, runner, environment, fixStacktrace)` — line 25321

### `extract_names(param)` — line 25384

### `extract_identifiers(param, nodes = [])` — line 25393

### `isNode(value)` — line 25644

### `ssrTransform(code, inMap, url, originalCode, options)` — line 25675

### `ssrTransformJSON(code, inMap)` — line 25681

### `ssrTransformScript(code, inMap, url, originalCode)` — line 25690

### `defineImport(index, importNode, metadata)` — line 25718

### `defineExport(name, local = name)` — line 25746

### `walk(root, { onIdentifier, onImportMeta, onDynamicImport, onStatements })` — line 25967

### `isInScope(name, parents)` — line 25983

### `handlePattern(p, parentScope)` — line 25986

### `isRefIdentifier(id, parent, parentStack)` — line 26099

### `isBlock(node)` — line 26141

### `findParentScope(parentStack, isVar = false)` — line 26144

### `isInDestructuringAssignment(parent, parentStack)` — line 26147

### `hasDockerEnv()` — line 26156

### `hasDockerCGroup()` — line 26165

### `isDocker()` — line 26173

### `isInsideContainer()` — line 26194

### `defineLazyProperty(object, propertyName, valueGetter)` — line 26226

### `defaultBrowserId()` — line 26247

### `runAppleScript(script, {humanReadableOutput = true} = {})` — line 26262

### `bundleName(bundleId)` — line 26273

### `defaultBrowser()` — line 26323

### `detectArchBinary(binary)` — line 26616

### `detectPlatformBinary({[platform]: platformBinary}, {wsl})` — line 26630

### `requireWindows()` — line 26680

### `checkPathExt(path, options)` — line 26688

### `checkStat(stat, path, options)` — line 26709

### `isexe(path, options, cb)` — line 26716

### `sync(path, options)` — line 26722

### `requireMode()` — line 26731

### `checkMode(stat, options)` — line 26753

### `resolveCommandAttempt(parsed, withoutPathExt)` — line 26984

### `escapeCommand(arg)` — line 27036

### `escapeArgument(arg, doubleEscapeMetaChars)` — line 27043

### `detectShebang(parsed)` — line 27131

### `parseNonShell(parsed)` — line 27146

### `notFoundError(original, syscall)` — line 27214

### `hookChildProcess(cp, parsed)` — line 27224

### `verifyENOENT(status, parsed)` — line 27247

### `verifyENOENTSync(status, parsed)` — line 27255

### `spawn(command, args, options)` — line 27274

### `spawnSync(command, args, options)` — line 27288

### `openBrowser(url, opt, logger)` — line 27311

### `executeNodeScript(scriptPath, url, logger)` — line 27320

### `startBrowserProcess(browser, browserArgs, url, logger)` — line 27351

### `execAsync(command, options)` — line 27387

### `bindCLIShortcuts(server, opts)` — line 27399

### `getResolvedOutDirs(root, outDir, outputOptions)` — line 27509

### `resolveEmptyOutDir(emptyOutDir, root, outDirs, logger)` — line 27518

### `resolveChokidarOptions(options, resolvedOutDirs, emptyOutDir, cacheDir)` — line 27537

### `createNoopWatcher(options)` — line 27583

### `_mask(source, mask, output, offset, length)` — line 27648

### `_unmask(buffer, mask)` — line 27661

### `deflateOnData(chunk)` — line 28260

### `inflateOnData(chunk)` — line 28271

### `inflateOnError(err)` — line 28295

### `_isValidUTF8(buf)` — line 28361

### `callCallbacks(sender, err, cb)` — line 29740

### `onError(sender, err, cb)` — line 29759

### `onMessage(data, isBinary)` — line 29964

### `onClose(code, message)` — line 29973

### `onOpen()` — line 29994

### `callListener(listener, thisArg, event)` — line 30042

### `push(dest, name, elem)` — line 30062

### `initAsClient(websocket, address, protocols, options)` — line 30902

### `emitErrorAndClose(websocket, err)` — line 31282

### `netConnect(options)` — line 31300

### `tlsConnect(options)` — line 31312

### `sendAfterClose(websocket, data, cb)` — line 31367

### `receiverOnConclude(code, reason)` — line 31397

### `receiverOnDrain()` — line 31418

### `receiverOnError(err)` — line 31430

### `receiverOnFinish()` — line 31456

### `receiverOnMessage(data, isBinary)` — line 31467

### `receiverOnPing(data)` — line 31477

### `receiverOnPong(data)` — line 31490

### `resume(stream)` — line 31500

### `senderOnError(err)` — line 31510

### `setCloseTimer(websocket)` — line 31538

### `socketOnClose()` — line 31550

### `socketOnData(chunk)` — line 31602

### `socketOnEnd()` — line 31613

### `addListeners(server, map)` — line 32147

### `removeListeners()` — line 32150

### `emitClose(server)` — line 32163

### `socketOnError()` — line 32173

### `abortHandshake(socket, code, message, headers)` — line 32186

### `abortHandshakeOrEmitwsClientError(server, req, socket, code, message)` — line 32226

### `getAdditionalAllowedHosts(resolvedServerOptions, resolvedPreviewOptions)` — line 32242

### `isHostAllowedWithoutCache(allowedHosts, additionalAllowedHosts, host)` — line 32262

### `isHostAllowed(config, isPreview, host)` — line 32297

### `hostCheckMiddleware(config, isPreview)` — line 32320

### `viteHostCheckMiddleware(req, res, next)` — line 32321

### `hasValidToken(config, url)` — line 32355

### `createWebSocketServer(server, config, httpsOptions)` — line 32368

### `getSocketClient(socket)` — line 32531

### `baseMiddleware(rawBase, middlewareMode)` — line 32637

### `viteBaseMiddleware(req, res, next)` — line 32638

### `Events()` — line 32693

### `EE(fn, context, once)` — line 32721

### `addListener(emitter, event, fn, context, once)` — line 32738

### `clearEvent(emitter, evt)` — line 32760

### `EventEmitter()` — line 32772

### `eventNames()` — line 32784

### `listeners(event)` — line 32809

### `listenerCount(event)` — line 32830

### `emit(event, a1, a2, a3, a4, a5)` — line 32846

### `on(event, fn, context)` — line 32907

### `once(event, fn, context)` — line 32920

### `removeListener(event, fn, context, once)` — line 32934

### `removeAllListeners(event)` — line 32981

### `required(port, protocol)` — line 33032

### `rewriteCookieProperty(header, config, property)` — line 33272

### `hasPort(host)` — line 33305

### `removeChunked(req, res, proxyRes)` — line 33334

### `setConnection(req, res, proxyRes)` — line 33350

### `setRedirectHostRewrite(req, res, proxyRes, options)` — line 33358

### `writeHeaders(req, res, proxyRes, options)` — line 33393

### `writeStatusCode(req, res, proxyRes)` — line 33445

### `detectUnsupportedEnvironment()` — line 33485

### `RedirectableRequest(options, responseCallback)` — line 33555

### `destroyOnTimeout(socket)` — line 33683

### `startTimer(socket)` — line 33690

### `clearTimer()` — line 33702

### `writeNext(error)` — line 33832

### `wrap(protocols)` — line 33972

### `request(input, options, callback)` — line 33987

### `parseUrl(input)` — line 34038

### `resolveUrl(relative, base)` — line 34054

### `validateUrl(input)` — line 34059

### `spreadUrlObject(urlObject, target)` — line 34069

### `removeMatchingHeaders(regex, headers)` — line 34089

### `createErrorType(code, message, baseClass)` — line 34101

### `CustomError(properties)` — line 34103

### `destroyRequest(request, error)` — line 34128

### `isSubdomain(subdomain, domain)` — line 34136

### `isFunction(value)` — line 34146

### `isBuffer(value)` — line 34150

### `isURL(value)` — line 34154

### `deleteLength(req, res, options)` — line 34197

### `timeout(req, res, options)` — line 34215

### `XHeaders(req, res, options)` — line 34231

### `stream(req, res, options, _, server, clb)` — line 34263

### `createErrorHandler(proxyReq, url)` — line 34318

### `proxyError(err)` — line 34319

### `checkMethodAndHeader(req, socket)` — line 34388

### `onOutgoingError(err)` — line 34511

### `createRightProxy(type)` — line 34549

### `ProxyServer(options)` — line 34611

### `done()` — line 34669

### `createProxyServer(options)` — line 34730

### `proxyMiddleware(httpServer, options, config)` — line 34810

### `viteProxyMiddleware(req, res, next)` — line 34924

### `doesProxyContextMatchUrl(context, url)` — line 34962

### `htmlFallbackMiddleware(root, spaFallback)` — line 34967

### `viteHtmlFallbackMiddleware(req, _res, next)` — line 34968

### `entitytag(entity)` — line 35049

### `etag(entity, options)` — line 35080

### `isstats(obj)` — line 35114

### `stattag(stat)` — line 35136

### `totalist(dir, callback, pre='')` — line 35204

### `isMatch(uri, arr)` — line 35258

### `toAssume(uri, extns)` — line 35264

### `viaCache(cache, uri, extns)` — line 35280

### `viaLocal(dir, isEtag, uri, extns, shouldServe)` — line 35287

### `is404(req, res)` — line 35306

### `send(req, res, file, stats, headers)` — line 35310

### `toHeaders(name, stats, isEtag)` — line 35353

### `sirv(dir, opts={})` — line 35371

### `servePublicMiddleware(server, publicFiles)` — line 35486

### `viteServePublicMiddleware(req, res, next)` — line 35506

### `serveStaticMiddleware(server)` — line 35514

### `viteServeStaticMiddleware(req, res, next)` — line 35523

### `serveRawFsMiddleware(server)` — line 35565

### `viteServeRawFsMiddleware(req, res, next)` — line 35573

### `isFileServingAllowed(configOrUrl, urlOrServer)` — line 35595

### `isUriInFilePath(uri, filePath)` — line 35602

### `isFileLoadingAllowed(config, filePath)` — line 35606

### `checkLoadingAccess(config, path2)` — line 35623

### `checkServingAccess(url, server)` — line 35632

### `respondWithAccessDenied(url, server, res)` — line 35641

### `renderRestrictedErrorHTML(msg)` — line 35653

### `transformRequest(environment, url, options = {})` — line 35674

### `doTransform(environment, url, options, timestamp)` — line 35708

### `getCachedTransformResult(environment, url, module, timestamp)` — line 35749

### `loadAndTransform(environment, id, url, options, timestamp, mod, resolved)` — line 35766

### `handleModuleSoftInvalidation(environment, mod, timestamp)` — line 35909

### `getNodeAssetAttributes(node)` — line 36037

### `handleAttributeKey(key, type)` — line 36056

### `getAttrKey(attr)` — line 36067

### `modulePreloadPolyfillPlugin(config)` — line 36073

### `polyfill()` — line 36099

### `getFetchOpts(link)` — line 36118

### `processPreload(link)` — line 36128

### `htmlInlineProxyPlugin(config)` — line 36154

### `addToHTMLProxyCache(config, filePath, index, result)` — line 36183

### `addToHTMLProxyTransformResult(hash, code)` — line 36192

### `nodeIsElement(node)` — line 36202

### `traverseNodes(node, visitor)` — line 36205

### `traverseHtml(html, filePath, visitor)` — line 36214

### `getScriptInfo(node)` — line 36226

### `overwriteAttrValue(s, sourceCodeLocation, newValue)` — line 36250

### `removeViteIgnoreAttr(s, sourceCodeLocation)` — line 36270

### `formatParseError(parserError, id, html)` — line 36277

### `handleParseError(parserError, html, filePath)` — line 36294

### `buildHtmlPlugin(config)` — line 36314

### `parseRelAttr(attr)` — line 36833

### `findNeedTransformStyleAttribute(node)` — line 36836

### `extractImportExpressionFromClassicScript(scriptTextNode)` — line 36845

### `preImportMapHook(config)` — line 36863

### `postImportMapHook()` — line 36883

### `injectCspNonceMetaTagHook(config)` — line 36901

### `htmlEnvHook(config)` — line 36915

### `injectNonceAttributeTagHook(config)` — line 36956

### `resolveHtmlTransforms(plugins, logger)` — line 36981

### `headTagInsertCheck(tags, ctx)` — line 37028

### `applyHtmlTransforms(html, hooks, ctx)` — line 37047

### `isEntirelyImport(code)` — line 37093

### `getBaseInHTML(urlRelativePath, config)` — line 37096

### `injectToHead(html, tags, prepend = false)` — line 37109

### `injectToBody(html, tags, prepend = false)` — line 37136

### `prependInjectFallback(html, tags)` — line 37169

### `serializeTag({ tag, attrs, children }, indent = "")` — line 37181

### `serializeTags(tags, indent = "")` — line 37191

### `serializeAttrs(attrs)` — line 37200

### `incrementIndent(indent = "")` — line 37211

### `decodeURIIfPossible(input)` — line 37214

### `deniedServingAccessForTransform(url, server, res, next)` — line 37229

### `isServerAccessDeniedForTransform(config, id)` — line 37243

### `cachedTransformMiddleware(server)` — line 37249

### `viteCachedTransformMiddleware(req, res, next)` — line 37250

### `transformMiddleware(server)` — line 37267

### `viteTransformMiddleware(req, res, next)` — line 37271

### `warnAboutExplicitPublicPathInUrl(url)` — line 37446

### `createDevHtmlTransformFn(config)` — line 37474

### `getHtmlFilename(url, server)` — line 37499

### `shouldPreTransform(url, config)` — line 37508

### `isBareRelative(url)` — line 37512

### `indexHtmlMiddleware(root, server)` — line 37750

### `viteIndexHtmlMiddleware(req, res, next)` — line 37752

### `preTransformRequest(server, decodedUrl, decodedBase)` — line 37795

### `timeMiddleware(root)` — line 37802

### `viteTimeMiddleware(req, res, next)` — line 37803

### `mapIterator(iterable, transform)` — line 38000

### `createBackwardCompatibleModuleSet(moduleGraph, prop, module)` — line 38231

### `createBackwardCompatibleModuleMap(moduleGraph, prop, getModuleMap)` — line 38271

### `createBackwardCompatibleFileToModulesMap(moduleGraph)` — line 38323

### `notFoundMiddleware()` — line 38410

### `vite404Middleware(_, res)` — line 38411

### `hasWorkspacePackageJSON(root)` — line 38429

### `hasRootFile(root)` — line 38441

### `hasPackageJSON(root)` — line 38444

### `searchForPackageRoot(current, root = current)` — line 38448

### `searchForWorkspaceRoot(current, root = searchForPackageRoot(current)` — line 38454

### `rejectInvalidRequestMiddleware()` — line 38462

### `viteRejectInvalidRequestMiddleware(req, res, next)` — line 38463

### `createServer(inlineConfig = {})` — line 38474

### `_createServer(inlineConfig = {}, options)` — line 38477

### `viteHMRPingMiddleware(req, res, next)` — line 38839

### `startServer(server, inlinePort)` — line 38894

### `createServerCloseFn(server)` — line 38912

### `resolvedAllowDir(root, dir)` — line 38942

### `resolveServerOptions(root, raw, logger)` — line 38974

### `restartServer(server)` — line 39029

### `restartServerWithUrls(server)` — line 39078

### `getShortName(file, root)` — line 39109

### `getSortedPluginsByHotUpdateHook(plugins)` — line 39236

### `getSortedHotUpdatePlugins(environment)` — line 39258

### `handleHMRUpdate(type, file, server)` — line 39266

### `hmr(environment)` — line 39424

### `updateModules(environment, file, modules, timestamp, firstInvalidatedBy)` — line 39467

### `areAllImportsAccepted(importedBindings, acceptedExports)` — line 39534

### `propagateUpdate(node, traversedModules, boundaries, currentChain = [node])` — line 39542

### `isNodeWithinCircularImports(node, nodeChain, currentChain = [node], traversedModules = /* @__PURE__ */ new Set()` — line 39611

### `handlePrunedModules(mods, { hot })` — line 39645

### `lexAcceptedHmrDeps(code, start, urls)` — line 39657

### `addDep(index)` — line 39661

### `lexAcceptedHmrExports(code, start, exportNames)` — line 39747

### `normalizeHmrUrl(url)` — line 39755

### `error(pos)` — line 39761

### `readModifiedFile(file)` — line 39768

### `createServerHotChannel()` — line 39784

### `createDeprecatedHotBroadcaster(ws)` — line 39811

### `definePlugin(config)` — line 39842

### `generatePattern(environment)` — line 39870

### `getPattern(environment)` — line 39909

### `replaceDefine(environment, code, id, define)` — line 39966

### `serializeDefine(define)` — line 39997

### `handleDefineValue(value)` — line 40010

### `getImportMetaEnvKeyRe(marker)` — line 40015

### `saveEmitWorkerAsset(config, asset)` — line 40029

### `bundleWorkerEntry(config, id)` — line 40044

### `emitSourcemapForWorkerEntry(config, chunk)` — line 40114

### `encodeWorkerAssetFileName(fileName, workerCache2)` — line 40131

### `workerFileToUrl(config, id)` — line 40139

### `webWorkerPostPlugin()` — line 40155

### `webWorkerPlugin(config)` — line 40173

### `WorkerWrapper(options)` — line 40258

### `isSameContent(a, b)` — line 40380

### `preAliasPlugin(config)` — line 40390

### `optimizeAliasReplacementForSSR(id, optimizeDeps)` — line 40435

### `matches(pattern, importee)` — line 40441

### `getAliasPatterns(entries)` — line 40453

### `getAliasPatternMatcher(entries)` — line 40459

### `isExplicitImportRequired(url)` — line 40473

### `normalizeResolvedIdToUrl(environment, url, resolved)` — line 40476

### `extractImportedBindings(id, source, importSpec, importedBindings)` — line 40494

### `importAnalysisPlugin(config)` — line 40533

### `getEnv(ssr)` — line 40540

### `mergeAcceptedUrls(orderedUrls)` — line 40965

### `createParseErrorInfo(importer, source)` — line 40973

### `interopNamedImports(str, importSpecifier, rewrittenUrl, importIndex, importer, config)` — line 40991

### `getLineBreaks(str)` — line 41034

### `transformCjsImport(importExp, url, rawUrl, importIndex, importer, config)` — line 41037

### `getIdentifierNameOrLiteralValue(node)` — line 41114

### `__vite__injectQuery(url, queryToInject)` — line 41117

### `clientInjectionsPlugin(config)` — line 41128

### `escapeReplacement(value)` — line 41199

### `err(e, pos)` — line 41281

### `findClosingParen(input, fromIndex)` — line 41286

### `extractWorkerTypeFromAst(expression, optsStartIndex)` — line 41295

### `parseWorkerOptions(rawOpts, optsStartIndex)` — line 41330

### `getWorkerType(raw, clean, i)` — line 41356

### `isIncludeWorkerImportMetaUrl(code)` — line 41387

### `workerImportMetaUrlPlugin(config)` — line 41393

### `assetImportMetaUrlPlugin(config)` — line 41476

### `buildGlobPattern(ast)` — line 41587

### `getQueryDelimiterIndex(rawUrl)` — line 41603

### `metadataPlugin()` — line 41617

### `sanitizeString(str)` — line 41635

### `templateLiteralToGlob(node)` — line 41643

### `callExpressionToGlob(node)` — line 41656

### `binaryExpressionToGlob(node)` — line 41668

### `expressionToGlob(node)` — line 41676

### `shouldIgnore(glob)` — line 41695

### `dynamicImportToGlob(node, sourceString)` — line 41707

### `parseDynamicImportPattern(strings)` — line 41773

### `transformDynamicImport(importSource, importer, resolve, root)` — line 41806

### `dynamicImportVarsPlugin(config)` — line 41839

### `getMatcherString(glob, cwd)` — line 41937

### `patternToIdFilter(pattern, cwd)` — line 41944

### `patternToCodeFilter(pattern)` — line 41960

### `normalizeFilter(filter)` — line 41984

### `createIdFilter(filter, cwd = process.cwd()` — line 42000

### `createCodeFilter(filter)` — line 42007

### `createFilterForTransform(idFilter, codeFilter, cwd)` — line 42014

### `resolvePlugins(config, prePlugins, normalPlugins, postPlugins)` — line 42033

### `createPluginHookUtils(plugins)` — line 42085

### `getSortedPlugins(hookName)` — line 42087

### `getSortedPluginHooks(hookName)` — line 42094

### `getSortedPluginsByHook(hookName, plugins)` — line 42103

### `getHookHandler(hook)` — line 42124

### `getCachedFilterForPlugin(plugin, hookName)` — line 42128

### `throwClosedServerError()` — line 42184

### `createEnvironmentPluginContainer(environment, plugins, watcher, autoStart = true)` — line 42191

### `createPluginContainer(environments)` — line 42919

### `createBackCompatIdResolver(config, options)` — line 42923

### `createIdResolver(config, options)` — line 42934

### `resolve(environment, id, importer)` — line 42937

### `resolveAlias(environment, id, importer)` — line 42964

### `resolveCSSOptions(options)` — line 42997

### `encodePublicUrlsInCSS(config)` — line 43025

### `cssPlugin(config)` — line 43029

### `cssPostPlugin(config)` — line 43149

### `getCssBundleName()` — line 43177

### `ensureFileExt(name, ext)` — line 43364

### `cssAnalysisPlugin(config)` — line 43607

### `isCssScopeToRendered(cssScopeTo, renderedModules)` — line 43657

### `getEmptyChunkReplacer(pureCssChunkNames, outputFormat)` — line 43662

### `createCSSResolvers(config)` — line 43683

### `getCssResolversKeys(resolvers)` — line 43732

### `compileCSSPreprocessors(environment, id, lang, code, workerController)` — line 43735

### `getAtImportResolvers(config)` — line 43779

### `compileCSS(environment, id, code, workerController, urlResolver)` — line 43787

### `compilePostCSS(environment, id, code, deps, lang, workerController, urlResolver)` — line 43841

### `transformSugarSS(environment, id, code)` — line 43964

### `runPostCSS(id, code, plugins, options, deps, logger, enableSourcemap)` — line 43978

### `createCachedImport(imp)` — line 44062

### `preprocessCSS(code, filename, config)` — line 44079

### `formatPostcssSourceMap(rawMap, file)` — line 44091

### `combineSourcemapsIfExists(filename, map1, map2)` — line 44109

### `finalizeCss(css, minify, config)` — line 44125

### `resolvePostcssConfig(config)` — line 44135

### `rewriteCssUrls(css, replacer)` — line 44231

### `rewriteCssDataUris(css, replacer)` — line 44237

### `rewriteImportCss(css, replacer)` — line 44243

### `rewriteCssImageSet(css, replacer)` — line 44250

### `skipUrlReplacer(rawUrl)` — line 44265

### `doUrlReplace(rawUrl, matched, replacer, funcName = "url")` — line 44268

### `doImportCSSReplace(rawUrl, matched, replacer)` — line 44290

### `minifyCSS(css, config, inlined)` — line 44303

### `resolveMinifyCssEsbuildOptions(options)` — line 44368

### `hoistAtRules(css)` — line 44389

### `loadPreprocessorPath(lang, root)` — line 44410

### `loadSassPackage(root)` — line 44434

### `loadSss(root)` — line 44448

### `cleanScssBugUrl(url)` — line 44454

### `fixScssBugImportValue(data)` — line 44465

### `rebaseUrls(environment, file, rootFile, alias, variablePrefix, resolver)` — line 44779

### `formatStylusSourceMap(mapBefore, root)` — line 45069

### `getSource(source, filename, additionalData, enableSourcemap, sep = "")` — line 45079

### `isPreProcessor(lang)` — line 45141

### `compileLightningCSS(environment, id, src, deps, workerController, urlResolver)` — line 45145

### `getLightningCssErrorMessageForIeSyntaxes(code)` — line 45309

### `resolveLibCssFilename(libOptions, root, packageCache)` — line 45388

### `toRelativePath(filename, importer)` — line 45410

### `indexOfMatchInSlice(str, reg, pos = 0)` — line 45414

### `detectScriptRel()` — line 45419

### `preload(baseModule, deps, importerUrl)` — line 45423

### `handlePreloadError(err)` — line 45482

### `buildImportAnalysisPlugin(config)` — line 45500

### `ssrManifestPlugin()` — line 45906

### `buildLoadFallbackPlugin()` — line 45991

### `completeSystemWrapPlugin()` — line 46011

### `resolveBuildEnvironmentOptions(raw, logger, consumer)` — line 46066

### `resolveBuildPlugins(config)` — line 46112

### `build(inlineConfig = {})` — line 46139

### `resolveConfigToBuild(inlineConfig = {}, patchConfig, patchPlugins)` — line 46145

### `buildEnvironment(environment)` — line 46156

### `extractStack(e)` — line 46205

### `prepareOutDir(outDirs, emptyOutDir, environment)` — line 46391

### `resolveOutputJsExtension(format, type = "commonjs")` — line 46425

### `resolveLibFilename(libOptions, format, entryName, root, extension, packageCache)` — line 46432

### `resolveBuildOutputs(outputs, libOptions, logger)` — line 46448

### `clearLine()` — line 46489

### `onRollupLog(level, log, environment)` — line 46496

### `normalizeUserOnWarn(userOnWarn, defaultHandler)` — line 46557

### `resolveUserExternal(user, id, parentId, isResolved)` — line 46570

### `isExternal(id, test)` — line 46579

### `injectEnvironmentAndFilterToHooks(environment, plugin)` — line 46586

### `wrapEnvironmentAndFilterResolveId(environment, hook)` — line 46609

### `wrapEnvironmentAndFilterLoad(environment, hook)` — line 46634

### `wrapEnvironmentAndFilterTransform(environment, hook)` — line 46658

### `wrapEnvironmentHook(environment, hook)` — line 46683

### `injectEnvironmentInContext(context, environment)` — line 46699

### `injectSsrFlag(options, environment)` — line 46703

### `escapeId(id)` — line 46710

### `toOutputFilePathInJS(environment, filename, type, hostId, hostType, toRelative)` — line 46748

### `createToImportMetaURLBasedRelativeRuntime(format, isWorker)` — line 46776

### `toOutputFilePathWithoutRuntime(filename, type, hostId, hostType, config, toRelative)` — line 46785

### `areSeparateFolders(a, b)` — line 46816

### `defaultBuildApp(builder)` — line 46841

### `resolveBuilderOptions(options)` — line 46851

### `createBuilder(inlineConfig = {}, useLegacyBuilder = false)` — line 46858

### `setupEnvironment(name, config2)` — line 46880

### `fetchModule(environment, url, importer, options = {})` — line 46945

### `inlineSourceMap(mod, result, startOffset)` — line 47011

### `createDepsOptimizer(environment)` — line 47032

### `close()` — line 47101

### `startNextDiscoveredBatch()` — line 47178

### `prepareKnownDeps()` — line 47183

### `runOptimizer(preRunResult)` — line 47195

### `fullReload()` — line 47349

### `rerun()` — line 47356

### `getDiscoveredBrowserHash(hash, deps, missing)` — line 47362

### `registerMissingImport(id, resolved)` — line 47367

### `addMissingDep(id, resolved)` — line 47386

### `debouncedProcessing(timeout = debounceMs)` — line 47407

### `onCrawlEnd()` — line 47420

### `createExplicitDepsOptimizer(environment)` — line 47501

### `findInteropMismatches(discovered, optimized)` — line 47529

### `warmupFiles(server, environment)` — line 47889

### `warmupFile(server, environment, file)` — line 47897

### `htmlFileToUrl(file, root)` — line 47919

### `fileToUrl(file, root)` — line 47924

### `mapFiles(files, root)` — line 47931

### `invalidateModule(environment, m)` — line 48137

### `setupOnCrawlEnd()` — line 48156

### `cancel()` — line 48162

### `registerRequestProcessing(id, done)` — line 48165

### `waitForRequestsIdle(ignoredId)` — line 48173

### `markIdAsDone(id)` — line 48182

### `checkIfCrawlEndAfterTimeout()` — line 48186

### `callOnCrawlEndWhenIdle()` — line 48194

### `createRunnableDevEnvironment(name, config, context = {})` — line 48205

### `isRunnableDevEnvironment(environment)` — line 48214

### `getChunkSize(chunk, enc)` — line 48253

### `compression({ threshold = 1024, level = -1, brotli = false, gzip = true, mimes = MIMES } = {})` — line 48261

### `start()` — line 48283

### `resolvePreviewOptions(preview2, server)` — line 48357

### `preview(inlineConfig = {})` — line 48370

### `resolveSSROptions(ssr, preserveSymlinks)` — line 48515

### `runnerImport(moduleId, inlineConfig)` — line 48522

### `defineConfig(config)` — line 48580

### `defaultCreateClientDevEnvironment(name, config, context)` — line 48583

### `defaultCreateDevEnvironment(name, config)` — line 48590

### `resolveDevEnvironmentOptions(dev, environmentName, consumer, skipSsrTransform, preTransformRequest)` — line 48696

### `resolveEnvironmentOptions(options, alias, preserveSymlinks, forceOptimizeDeps, logger, environmentName, skipSsrTransform, isSsrTargetWebworkerSet, preTransformRequests)` — line 48713

### `getDefaultEnvironmentOptions(config)` — line 48766

### `checkBadCharactersInPath(name, path2, logger)` — line 48779

### `resolveEnvironmentResolveOptions(resolve, alias, preserveSymlinks, logger, consumer, isSsrTargetWebworkerEnvironment)` — line 48812

### `resolveResolveOptions(resolve, logger)` — line 48836

### `resolveDepOptimizationOptions(optimizeDeps, preserveSymlinks, forceOptimizeDeps, consumer)` — line 48856

### `isResolvedConfig(inlineConfig)` — line 48871

### `resolveConfig(inlineConfig, command, defaultMode = "development", defaultNodeEnv = "development", isPreview = false, patchConfig = void 0, patchPlugins = void 0)` — line 48874

### `resolveBaseUrl(base = configDefaults.base, isBuild, logger)` — line 49322

### `decodeBase(base)` — line 49349

### `sortUserPlugins(plugins)` — line 49358

### `loadConfigFromFile(configEnv, configFile, configRoot = process.cwd()` — line 49371

### `nativeImportConfigFile(resolvedPath)` — line 49416

### `runnerImportConfigFile(resolvedPath)` — line 49423

### `bundleAndLoadConfigFile(resolvedPath)` — line 49430

### `bundleConfigFile(fileName, isESM)` — line 49443

### `loadConfigFromBundledFile(fileName, bundledCode, isESM)` — line 49567

### `runConfigHook(config, plugins, configEnv)` — line 49613

### `runConfigEnvironmentHook(environments, plugins, configEnv, isSsrTargetWebworkerSet)` — line 49625

### `optimizeDepsDisabledBackwardCompatibility(resolved, optimizeDeps, optimizeDepsPath = "")` — line 49641

### `formatter(open, close, replace = open)` — line 67

### `replaceClose(string, close, replace, index)` — line 73

### `createColors(enabled = isColorSupported)` — line 83

### `getMatcher(id)` — line 556

### `replacer(match)` — line 1575

### `hasConflicts()` — line 2321

### `getVirtualPathForDynamicRequirePath(path, commonDir)` — line 2348

### `modifyMap(targetPath, resolvedPath)` — line 2408

### `isWrappedId(id, suffix)` — line 2566

### `wrapId(id, suffix)` — line 2567

### `unwrapId(wrappedId, suffix)` — line 2568

### `getDependencies(id)` — line 2877

### `isCyclic(id)` — line 2879

### `getTypeForFullyAnalyzedModule(id)` — line 2897

### `setInitialParentType(id, initialCommonJSType)` — line 2908

### `analyzeRequiredModule(parentId, resolved, isConditional, loadModule)` — line 2924

### `getTypeForImportedModule(resolved, loadModule)` — line 2941

### `hasNameConflict({ scope })` — line 3696

### `isPossibleCjsId(id)` — line 4280

### `getIgnoreTryCatchRequireStatementMode(id)` — line 4323

### `checkDynamicRequire(position)` — line 4374

### `maybeAddSegment(map, genLine, genColumn, source, sourceLine, sourceColumn, name, content)` — line 5175

### `watchFileStub(id)` — line 6396

### `flattenId(id)` — line 6475

### `limitFlattenIdLength(id, limit = FLATTEN_ID_MAX_FILE_LENGTH)` — line 6483

### `normalizeId(id)` — line 6489

### `urlCanParse(// eslint-disable-next-line n/no-unsupported-features/node-builtins
  URL$3.canParse ?? // URL.canParse is supported from Node.js 18.17.0+, 20.0.0+
  ((path2, base)` — line 6576

### `isExternalUrl(url)` — line 6610

### `isDataUrl(url)` — line 6612

### `isJSRequest(url)` — line 6616

### `isImportRequest(url)` — line 6636

### `isInternalRequest(url)` — line 6637

### `isAddressInfo(x)` — line 7087

### `blankReplacer(match)` — line 7174

### `requireResolveFromRootWithFallback(root, id)` — line 7184

### `isNonDriveRelativeAbsolutePath(p)` — line 7355

### `parentSigtermCallback(signal, exitCode)` — line 7492

### `setupSIGTERMListener(callback)` — line 7495

### `teardownSIGTERMListener(callback)` — line 7504

### `colorUrl(url)` — line 7641

### `displaySize(bytes)` — line 7667

### `Lib(()` — line 9226

### `buildEsbuildPlugin()` — line 9236

### `codeToDataUrl(code)` — line 9391

### `unlock()` — line 9538

### `syncResponse(data)` — line 9546

### `createLock(lockState)` — line 9590

### `createParentFunctionRequester(syncPort, asyncPort, receive, lock)` — line 9603

### `doWorkPromise(async ()` — line 9653

### `localImport(specifier)` — line 9685

### `loadTerserPath(root)` — line 9746

### `makeWorker()` — line 9766

### `E()` — line 10821

### `jsonLoader(_, content)` — line 11217

### `withLoaders(options = {})` — line 11931

### `isJSONRequest(request)` — line 12110

### `resolveResult(id, resolved)` — line 12833

### `matchesEntireLine(text)` — line 13000

### `err2(msg)` — line 13778

### `validateLiteral(element)` — line 13804

### `staticImports(await Promise.all(
    matches.map(
      async ({
        globsResolved, isRelative, options, index, start, end, onlyKeys, onlyValues
      })` — line 13905

### `resolvePaths(file)` — line 13927

### `resolvePath(p)` — line 14225

### `isUnlessEntry(path2)` — line 14337

### `externalUnlessEntry({ path: path2 })` — line 14338

### `doTransformGlobImport(contents, id, loader)` — line 14342

### `htmlTypeOnLoadCallback({ path: p })` — line 14389

### `setupExternalize(filter, doExternalize)` — line 14514

### `cleanUp()` — line 14909

### `unableToOptimize(id, msg)` — line 15156

### `isExternalizable(id, importer, configuredAsExternal)` — line 15619

### `isPossibleTsOutput(url)` — line 15951

### `isModuleBuiltin(id2)` — line 16036

### `getTime()` — line 16934

### `h(tag, text)` — line 17097

### `readdirpPromise(root, options = {})` — line 19965

### `createPattern(matcher, options)` — line 20040

### `matchPatterns(patterns, negPatterns, args, returnIndex)` — line 20061

### `stringify(node, parent = {})` — line 20468

### `pad(input, maxLength, toNumber)` — line 20837

### `toMaxLen(input, maxLength)` — line 20849

### `toSequence(parts, options, maxLen)` — line 20859

### `toRange(a, b, isNumbers, options)` — line 20889

### `toRegex(start, end, options)` — line 20901

### `rangeError(...args)` — line 20910

### `invalidRange(start, end, options)` — line 20914

### `invalidStep(step, options)` — line 20919

### `fillNumbers(start, end, step = 1, options = {})` — line 20926

### `fillLetters(start, end, step = 1, options = {})` — line 20978

### `append(queue = '', stash = '', enclose = false)` — line 21109

### `advance()` — line 21290

### `foreach(val, fn)` — line 22113

### `addAndConvert(main, prop, item)` — line 22121

### `delFromSet(main, prop, item)` — line 22138

### `isEmptySet(val)` — line 22147

### `handleEvent(rawEvent, evPath)` — line 22182

### `fsWatchBroadcast(fullPath, type, val1, val2, val3)` — line 22209

### `setFsWatchListener(path, fullPath, options, handlers)` — line 22225

### `setFsWatchFileListener(path, fullPath, options, handlers)` — line 22308

### `Depth(value)` — line 22774

### `createFSEventsInstance(path, callback)` — line 22814

### `filteredListener(fullPath, flags, info)` — line 22845

### `couldConsolidate(path)` — line 22903

### `canUse()` — line 22918

### `calcDepth(path, root)` — line 22921

### `sameTypes(info, stats)` — line 22929

### `watchCallback(fullPath, flags, info)` — line 23028

### `arrify(value = [])` — line 23327

### `flatten(list, result = [])` — line 23328

### `unifyPaths(paths_)` — line 23339

### `toUnix(string)` — line 23352

### `normalizePathToUnix(path)` — line 23369

### `normalizeIgnored(cwd = EMPTY_STR)` — line 23371

### `getAbsolutePath(path, cwd)` — line 23376

### `undef(opts, key)` — line 23386

### `awfEmit(err, stats)` — line 23844

### `clear()` — line 23924

### `awaitWriteFinish(prevStat)` — line 23960

### `watch(paths, options)` — line 24216

### `createServerModuleRunnerTransport(options)` — line 25257

### `setIsNodeInPattern(node)` — line 25965

### `isNodeInPattern(node)` — line 25966

### `setScope(node, name)` — line 25972

### `isStaticProperty(node)` — line 26134

### `isStaticPropertyKey(node, parent)` — line 26135

### `hasContainerEnv()` — line 26185

### `isWsl()` — line 26203

### `getWslDrivesMountPoint(()` — line 26356

### `pTryEach(array, mapper)` — line 26395

### `open(target, options)` — line 26605

### `getNotFoundError(cmd)` — line 26843

### `getPathInfo(cmd, opt)` — line 26846

### `subStep(p, i, ii)` — line 26903

### `whichSync(cmd, opt)` — line 26921

### `pathKey(options = {})` — line 26963

### `onInput(input)` — line 27416

### `shouldHandle(req)` — line 32402

### `handleUpgrade(req, socket, head, isPing)` — line 32418

### `route(_, res)` — line 32447

### `rewriteOriginHeader(proxyReq, options, config)` — line 34793

### `noop()` — line 35256

### `sirvOptions({
  config, getHeaders, disableFsServeCheck
})` — line 35451

### `toFilePath(url)` — line 35496

### `clearCache()` — line 35695

### `isHTMLProxy(id)` — line 36150

### `isHTMLRequest(request)` — line 36151

### `isExcludedUrl(url)` — line 36325

### `publicToRelative(filename)` — line 36336

### `toOutputPublicFilePath(url)` — line 36337

### `nodeStartWithLeadingWhitespace(node)` — line 36345

### `processAssetUrl(url, shouldInline)` — line 36373

### `toScriptTag(chunkOrUrl, toOutputPath, isAsync)` — line 36641

### `toPreloadTag(filename, toOutputPath)` — line 36656

### `toStyleSheetLinkTag(file, toOutputPath)` — line 36664

### `getCssTagsForChunk(chunk, toOutputPath)` — line 36699

### `toOutputFilePath(filename, type)` — line 36707

### `toOutputAssetFilePath(filename)` — line 36721

### `toOutputPublicAssetFilePath(filename)` — line 36722

### `processNodeUrl(url, useSrcSetReplacer, config, htmlPath, originalUrl, server, isClassicScriptLink)` — line 37515

### `devHtmlHook(html, { path: htmlPath, filename, server, originalUrl })` — line 37561

### `addInlineModule(node, ext)` — line 37585

### `getModuleMapUnion(prop)` — line 38032

### `getFileToModulesMap()` — line 38324

### `getBackwardCompatibleModules(modules)` — line 38350

### `closeServer()` — line 38556

### `closeServerAndExit(_, exitCode)` — line 38750

### `onHMRUpdate(type, file)` — line 38761

### `onFileAddUnlink(file, isUnlink)` — line 38766

### `initServer(onListen)` — line 38863

### `normalizeHotChannel(channel, enableHmr, normalizeClient = true)` — line 39112

### `handleInvoke(payload)` — line 39117

### `listenerWithNormalizedClient(data, client)` — line 39152

### `isNonJsRequest(request)` — line 39839

### `canSkipImportAnalysis(id)` — line 40467

### `str()` — line 40606

### `toAbsoluteUrl(url)` — line 40609

### `normalizeUrl(url, pos, forceSkipImportAnalysis = false)` — line 40610

### `interopHelper(m)` — line 40990

### `definesReplacement()` — line 41138

### `wasmHelper(opts = {}, url)` — line 41206

### `wasmHelperPlugin()` — line 41238

### `wasmFallbackPlugin()` — line 41265

### `dynamicImportHelper(glob, path, segs)` — line 41757

### `isCSSRequest(request)` — line 43017

### `isModuleCSSRequest(request)` — line 43018

### `isDirectCSSRequest(request)` — line 43019

### `isDirectRequest(request)` — line 43020

### `urlResolver(url, importer)` — line 43087

### `getCssAssetDirname(cssAssetName)` — line 43158

### `getContentWithSourcemap(content)` — line 43222

### `resolveAssetUrlsInCss(chunkCSS2, cssAssetName)` — line 43320

### `toRelative(filename)` — line 43324

### `UrlRewritePostcssPlugin(opts)` — line 44181

### `replacerForDeclaration(rawUrl)` — line 44199

### `makeScssWorker(environment, resolvers, alias, maxWorkers, packageName)` — line 44474

### `internalImporter(url, importer, filename)` — line 44475

### `_internalImporter(url, importer2, done)` — line 44503

### `makeModernScssWorker(environment, resolvers, alias, maxWorkers)` — line 44554

### `internalCanonicalize(url, importer)` — line 44555

### `internalLoad(file, rootFile)` — line 44560

### `makeModernCompilerScssWorker(environment, resolvers, alias, _maxWorkers)` — line 44640

### `scssProcessor(maxWorkers)` — line 44703

### `rebaseFn(url)` — line 44794

### `makeLessWorker(environment, resolvers, alias, maxWorkers)` — line 44821

### `viteLessResolve(filename, dir, rootFile, mime)` — line 44822

### `createViteLessPlugin(less, rootFile)` — line 44851

### `lessProcessor(maxWorkers)` — line 44920

### `makeStylWorker(maxWorkers)` — line 44979

### `stylProcessor(maxWorkers)` — line 45018

### `resolveFromRoot(p)` — line 45072

### `createPreprocessorWorkerController(maxWorkers)` — line 45102

### `sassProcess(environment, source, root, options, resolvers)` — line 45106

### `normalizeMaxWorkers(maxWorker)` — line 45129

### `convertTargets(esbuildTarget)` — line 45354

### `getInsertPreload(environment)` — line 45501

### `addFileDep(url, runtime = false)` — line 45715

### `addDeps(filename)` — line 45748

### `normalizeCodeFrame(frame)` — line 46217

### `enhanceRollupError(e)` — line 46223

### `outputBuildError(e)` — line 46247

### `buildOutputOptions(output = {})` — line 46256

### `viteLog(logLeveling, rawLogging)` — line 46498

### `normalizeLog(log)` — line 46569

### `getResolveUrl(path2, URL = "URL")` — line 46714

### `getRelativeUrlFromDocument(relativePath, umd = false)` — line 46715

### `getFileUrlFromFullPath(path2)` — line 46718

### `getFileUrlFromRelativePath(path2)` — line 46719

### `patchConfig(resolved)` — line 46859

### `patchConfig2(resolved)` — line 46891

### `patchPlugins(resolvedPlugins)` — line 46896

### `logNewlyDiscoveredDeps()` — line 47057

### `logDiscoveredDepsWhileScanning()` — line 47071

### `resolveEnqueuedProcessingPromises()` — line 47088

### `commitProcessing()` — line 47232

### `modPromise(async ()` — line 47784

### `NOOP()` — line 48245

### `viteAssetMiddleware(...args)` — line 48453

### `filterPlugin(p)` — line 48907

### `resolveByViteResolver(id, importer, isRequire)` — line 49473

### `commondir(basedir, relfiles)` — line 726

### `AsyncFunction()` — line 9389

### `ms(val, options)` — line 17145

### `strictCheck(str)` — line 20156

### `relaxedCheck(str)` — line 20247

### `debug_1()` — line 33461

### `createHttpHeader(line, headers)` — line 34439

### `viteAliasCustomResolver(id, importer, options)` — line 42163

### `collect2(chunk)` — line 43520

### `allSettled2(promises)` — line 45426

### `handler(id, importer, options)` — line 46614

### `createWorkerPlugins(bundleChain)` — line 49099
