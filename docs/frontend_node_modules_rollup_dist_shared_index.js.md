# `index.js`

> **Path:** `frontend/node_modules/rollup/dist/shared/index.js`  
> **Last documented:** 2026-06-29 18:03

---

## Functions

### `requireReaddirp()` — line 2446

### `requireScan()` — line 3010

### `requirePicomatch()` — line 5152

### `requireNormalizePath()` — line 5170

### `requireAnymatch()` — line 5208

### `requireIsExtglob()` — line 5327

### `isExtglob(str)` — line 5330

### `requireIsGlob()` — line 5356

### `isGlob(str, options)` — line 5484

### `requireGlobParent()` — line 5508

### `globParent(str, opts)` — line 5528

### `requireUtils()` — line 5559

### `requireStringify()` — line 5691

### `requireIsNumber()` — line 5737

### `requireToRegexRange()` — line 5763

### `collatePatterns(neg, pos, options)` — line 5845

### `splitToRanges(min, max)` — line 5853

### `rangeToPattern(start, stop, options)` — line 5886

### `splitToPatterns(min, max, tok, options)` — line 5917

### `filterPatterns(arr, comparison, prefix, intersection, options)` — line 5952

### `zip(a, b)` — line 5975

### `compare(a, b)` — line 5981

### `contains(arr, key, val)` — line 5985

### `countNines(min, len)` — line 5989

### `countZeros(integer, zeros)` — line 5993

### `toQuantifier(digits)` — line 5997

### `toCharacterClass(a, b, options)` — line 6005

### `hasPadding(str)` — line 6009

### `padZeros(value, tok, options)` — line 6013

### `requireFillRange()` — line 6059

### `requireCompile()` — line 6308

### `requireExpand()` — line 6376

### `requireParse()` — line 6562

### `requireBraces()` — line 6901

### `requireBinaryExtensions()` — line 7343

### `requireIsBinaryPath()` — line 7353

### `requireConstants()` — line 7369

### `requireNodefsHandler()` — line 7445

### `createFsWatchInstance(path, options, listener, errHandler, emitRaw)` — line 7551

### `requireFseventsHandler()` — line 8100

### `setFSEventsListener(path, realPath, listener, rawEmitter)` — line 8208

### `requireChokidar()` — line 8633

### `scan(input, options)` — line 343

### `eos()` — line 371

### `peek()` — line 372

### `advance()` — line 373

### `expandRange(args, options)` — line 715

### `syntaxError(type, char)` — line 730

### `parseRepeatedExtglob(pattern, requireEnd = true)` — line 850

### `analyzeRepeatedExtglob(body, options)` — line 969

### `parse(input, options)` — line 1012

### `remaining()` — line 1103

### `consume(value = '', num = 0)` — line 1104

### `negate()` — line 1114

### `extglobOpen(type, value)` — line 1180

### `picomatch(glob, options, returnState = false)` — line 2118

### `isIgnored()` — line 2146

### `matcher(input, returnObject = false)` — line 2152

### `readdirp(root, options = {})` — line 2704

### `readdirpPromise(root, options = {})` — line 2720

### `arrify(item)` — line 5224

### `createPattern(matcher, options)` — line 5231

### `matchPatterns(patterns, negPatterns, args, returnIndex)` — line 5252

### `stringify(node, parent = {})` — line 5698

### `toRegexRange(min, max, options)` — line 5769

### `pad(input, maxLength, toNumber)` — line 6094

### `toMaxLen(input, maxLength)` — line 6106

### `toSequence(parts, options, maxLen)` — line 6116

### `toRange(a, b, isNumbers, options)` — line 6146

### `toRegex(start, end, options)` — line 6158

### `rangeError(...args)` — line 6167

### `invalidRange(start, end, options)` — line 6171

### `invalidStep(step, options)` — line 6176

### `fillNumbers(start, end, step = 1, options = {})` — line 6183

### `fillLetters(start, end, step = 1, options = {})` — line 6235

### `fill(start, end, step, options = {})` — line 6268

### `compile(ast, options = {})` — line 6315

### `walk(node, parent = {})` — line 6316

### `append(queue = '', stash = '', enclose = false)` — line 6384

### `expand(ast, options = {})` — line 6410

### `braces(input, options = {})` — line 6924

### `foreach(val, fn)` — line 7483

### `addAndConvert(main, prop, item)` — line 7491

### `delFromSet(main, prop, item)` — line 7508

### `isEmptySet(val)` — line 7517

### `handleEvent(rawEvent, evPath)` — line 7552

### `fsWatchBroadcast(fullPath, type, val1, val2, val3)` — line 7579

### `setFsWatchListener(path, fullPath, options, handlers)` — line 7595

### `setFsWatchFileListener(path, fullPath, options, handlers)` — line 7678

### `listener(path, newStats)` — line 7796

### `Depth(value)` — line 8154

### `createFSEventsInstance(path, callback)` — line 8194

### `filteredListener(fullPath, flags, info)` — line 8225

### `couldConsolidate(path)` — line 8283

### `canUse()` — line 8298

### `calcDepth(path, root)` — line 8301

### `sameTypes(info, stats)` — line 8309

### `watchCallback(fullPath, flags, info)` — line 8408

### `flatten(list, result = [])` — line 8713

### `unifyPaths(paths_)` — line 8724

### `toUnix(string)` — line 8737

### `normalizePathToUnix(path)` — line 8754

### `normalizeIgnored(cwd = EMPTY_STR)` — line 8756

### `getAbsolutePath(path, cwd)` — line 8761

### `undef(opts, key)` — line 8771

### `awfEmit(err, stats)` — line 9229

### `clear()` — line 9309

### `awaitWriteFinish(prevStat)` — line 9345

### `watch(paths, options)` — line 9601

### `strictCheck(str)` — line 5361

### `relaxedCheck(str)` — line 5452
