# `watch.js`

> **Path:** `frontend/node_modules/rollup/dist/es/shared/watch.js`  
> **Last documented:** 2026-06-29 18:03

---

## Dependencies / Imports

- `./node-entry.js`
- `events`
- `fs`
- `node:os`
- `node:path`
- `node:process`
- `os`
- `path`
- `stream`
- `util`

## Functions

### `requireReaddirp()` — line 2450

### `requireScan()` — line 3014

### `requirePicomatch()` — line 5156

### `requireNormalizePath()` — line 5174

### `requireAnymatch()` — line 5212

### `requireIsExtglob()` — line 5331

### `isExtglob(str)` — line 5334

### `requireIsGlob()` — line 5360

### `isGlob(str, options)` — line 5488

### `requireGlobParent()` — line 5512

### `globParent(str, opts)` — line 5532

### `requireUtils()` — line 5563

### `requireStringify()` — line 5695

### `requireIsNumber()` — line 5741

### `requireToRegexRange()` — line 5767

### `collatePatterns(neg, pos, options)` — line 5849

### `splitToRanges(min, max)` — line 5857

### `rangeToPattern(start, stop, options)` — line 5890

### `splitToPatterns(min, max, tok, options)` — line 5921

### `filterPatterns(arr, comparison, prefix, intersection, options)` — line 5956

### `zip(a, b)` — line 5979

### `compare(a, b)` — line 5985

### `contains(arr, key, val)` — line 5989

### `countNines(min, len)` — line 5993

### `countZeros(integer, zeros)` — line 5997

### `toQuantifier(digits)` — line 6001

### `toCharacterClass(a, b, options)` — line 6009

### `hasPadding(str)` — line 6013

### `padZeros(value, tok, options)` — line 6017

### `requireFillRange()` — line 6063

### `requireCompile()` — line 6312

### `requireExpand()` — line 6380

### `requireParse()` — line 6566

### `requireBraces()` — line 6905

### `requireBinaryExtensions()` — line 7347

### `requireIsBinaryPath()` — line 7357

### `requireConstants()` — line 7373

### `requireNodefsHandler()` — line 7449

### `createFsWatchInstance(path, options, listener, errHandler, emitRaw)` — line 7555

### `requireFseventsHandler()` — line 8104

### `setFSEventsListener(path, realPath, listener, rawEmitter)` — line 8212

### `requireChokidar()` — line 8637

### `scan(input, options)` — line 347

### `eos()` — line 375

### `peek()` — line 376

### `advance()` — line 377

### `expandRange(args, options)` — line 719

### `syntaxError(type, char)` — line 734

### `parseRepeatedExtglob(pattern, requireEnd = true)` — line 854

### `analyzeRepeatedExtglob(body, options)` — line 973

### `parse(input, options)` — line 1016

### `remaining()` — line 1107

### `consume(value = '', num = 0)` — line 1108

### `negate()` — line 1118

### `extglobOpen(type, value)` — line 1184

### `picomatch(glob, options, returnState = false)` — line 2122

### `isIgnored()` — line 2150

### `matcher(input, returnObject = false)` — line 2156

### `readdirp(root, options = {})` — line 2708

### `readdirpPromise(root, options = {})` — line 2724

### `arrify(item)` — line 5228

### `createPattern(matcher, options)` — line 5235

### `matchPatterns(patterns, negPatterns, args, returnIndex)` — line 5256

### `stringify(node, parent = {})` — line 5702

### `toRegexRange(min, max, options)` — line 5773

### `pad(input, maxLength, toNumber)` — line 6098

### `toMaxLen(input, maxLength)` — line 6110

### `toSequence(parts, options, maxLen)` — line 6120

### `toRange(a, b, isNumbers, options)` — line 6150

### `toRegex(start, end, options)` — line 6162

### `rangeError(...args)` — line 6171

### `invalidRange(start, end, options)` — line 6175

### `invalidStep(step, options)` — line 6180

### `fillNumbers(start, end, step = 1, options = {})` — line 6187

### `fillLetters(start, end, step = 1, options = {})` — line 6239

### `fill(start, end, step, options = {})` — line 6272

### `compile(ast, options = {})` — line 6319

### `walk(node, parent = {})` — line 6320

### `append(queue = '', stash = '', enclose = false)` — line 6388

### `expand(ast, options = {})` — line 6414

### `braces(input, options = {})` — line 6928

### `foreach(val, fn)` — line 7487

### `addAndConvert(main, prop, item)` — line 7495

### `delFromSet(main, prop, item)` — line 7512

### `isEmptySet(val)` — line 7521

### `handleEvent(rawEvent, evPath)` — line 7556

### `fsWatchBroadcast(fullPath, type, val1, val2, val3)` — line 7583

### `setFsWatchListener(path, fullPath, options, handlers)` — line 7599

### `setFsWatchFileListener(path, fullPath, options, handlers)` — line 7682

### `listener(path, newStats)` — line 7800

### `Depth(value)` — line 8158

### `createFSEventsInstance(path, callback)` — line 8198

### `filteredListener(fullPath, flags, info)` — line 8229

### `couldConsolidate(path)` — line 8287

### `canUse()` — line 8302

### `calcDepth(path, root)` — line 8305

### `sameTypes(info, stats)` — line 8313

### `watchCallback(fullPath, flags, info)` — line 8412

### `flatten(list, result = [])` — line 8717

### `unifyPaths(paths_)` — line 8728

### `toUnix(string)` — line 8741

### `normalizePathToUnix(path)` — line 8758

### `normalizeIgnored(cwd = EMPTY_STR)` — line 8760

### `getAbsolutePath(path, cwd)` — line 8765

### `undef(opts, key)` — line 8775

### `awfEmit(err, stats)` — line 9233

### `clear()` — line 9313

### `awaitWriteFinish(prevStat)` — line 9349

### `watch(paths, options)` — line 9605

### `handleChange(id, event)` — line 9654

### `strictCheck(str)` — line 5365

### `relaxedCheck(str)` — line 5456
