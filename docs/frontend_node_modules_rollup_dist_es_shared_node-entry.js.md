# `node-entry.js`

> **Path:** `frontend/node_modules/rollup/dist/es/shared/node-entry.js`  
> **Last documented:** 2026-06-29 18:03

---

## Dependencies / Imports

- `../../native.js`
- `./parseAst.js`
- `estree`
- `node:fs/promises`
- `node:path`
- `node:perf_hooks`
- `node:process`
- `path`

## Functions

### `_mergeNamespaces(n, m)` — line 18

### `decodeInteger(reader, relative)` — line 45

### `encodeInteger(builder, num, relative)` — line 62

### `hasMoreVlq(reader, max)` — line 73

### `decode(mappings)` — line 132

### `sort(line)` — line 174

### `sortComparator(a, b)` — line 177

### `encode(decoded)` — line 180

### `getBtoa()` — line 399

### `guessIndent(code)` — line 438

### `getRelativePath(from, to)` — line 464

### `isObject(thing)` — line 485

### `getLocator(source)` — line 489

### `locate(index)` — line 498

### `getReplacement(match, str)` — line 1397

### `matchAll(re, str)` — line 1411

### `treeshakeNode(node, code, start, end)` — line 1790

### `findFirstOccurrenceOutsideComment(code, searchString, start = 0)` — line 1797

### `findNonWhiteSpace(code, index)` — line 1817

### `findLastWhiteSpaceReverse(code, start, end)` — line 1823

### `findFirstLineBreakOutsideComment(code)` — line 1838

### `renderStatementList(statements, code, start, end, options)` — line 1855

### `getCommaSeparatedNodesWithBoundaries(nodes, code, start, end)` — line 1895

### `removeLineBreaks(code, start, end)` — line 1934

### `getSystemExportStatement(exportedVariables, { exportNamesByVariable, snippets: { _, getObject, getPropertyAccess } }, modifier = '')` — line 1944

### `renderSystemExportExpression(exportedVariable, expressionStart, expressionEnd, code, { exportNamesByVariable, snippets: { _ } })` — line 1961

### `renderSystemExportFunction(exportedVariables, expressionStart, expressionEnd, needsParens, code, options)` — line 1965

### `renderSystemExportSequenceAfterExpression(exportedVariable, expressionStart, expressionEnd, needsParens, code, options)` — line 1970

### `renderSystemExportSequenceBeforeExpression(exportedVariable, expressionStart, expressionEnd, needsParens, code, options, modifier)` — line 1978

### `getOrCreate(map, key, init)` — line 1987

### `getNewSet()` — line 1996

### `getNewArray()` — line 1999

### `is_reference(node, parent)` — line 2180

### `createInclusionContext()` — line 2225

### `createHasEffectsContext()` — line 2234

### `isFlagSet(flags, flag)` — line 2255

### `setFlag(flags, flag, value)` — line 2258

### `cacheObjectGetters(object, getterProperties)` — line 2494

### `isLegal(value)` — line 2563

### `makeLegal(value)` — line 2569

### `stringifyObjectKeyIfNeeded(key)` — line 2579

### `stringifyIdentifierIfNeeded(key)` — line 2588

### `markModuleAndImpureDependenciesAsExecuted(baseModule)` — line 2684

### `createChildNodeKeysForNode(esTreeNode)` — line 3001

### `onlyIncludeSelf()` — line 3004

### `onlyIncludeSelfNoDeoptimize()` — line 3009

### `doNotDeoptimize()` — line 3012

### `isObjectExpressionNode(node)` — line 3016

### `isPropertyNode(node)` — line 3019

### `isArrowFunctionExpressionNode(node)` — line 3022

### `isFunctionExpressionNode(node)` — line 3025

### `isCallExpressionNode(node)` — line 3028

### `isMemberExpressionNode(node)` — line 3031

### `isAwaitExpressionNode(node)` — line 3034

### `isIdentifierNode(node)` — line 3037

### `isExpressionStatementNode(node)` — line 3040

### `assembleMemberDescriptions(memberDescriptions, inheritedDescriptions = null)` — line 3044

### `getLiteralMembersForValue(value)` — line 3217

### `hasMemberEffectWhenCalled(members, memberName, interaction, context)` — line 3234

### `getMemberReturnExpressionWhenCalled(members, memberName)` — line 3240

### `getGlobalAtPath(path)` — line 4989

### `closestParentFunctionOrProgram(node)` — line 5402

### `if(name === 'eval' &&
                renderedParentType === CallExpression$1 &&
                isCalleeOfRenderedParent)` — line 5521

### `toBase64(value)` — line 5533

### `getSafeName(baseName, usedNames, forbiddenNames)` — line 5543

### `checkEffectForNodes(nodes, context)` — line 5690

### `isStaticBlock(statement)` — line 5840

### `addArgumentToBeDeoptimized(argument)` — line 6037

### `getDeclarationStart(code, start)` — line 6936

### `getFunctionIdInsertPosition(code, start)` — line 6939

### `escapeId(id)` — line 7047

### `getToStringTagValue(getObject)` — line 7254

### `getChainElementLiteralValueAtPath(element, object, path, recursionTracker, origin)` — line 7314

### `getResolvablePropertyKey(memberExpression)` — line 7328

### `getResolvableComputedPropertyKey(propertyKey)` — line 7333

### `getPathIfNotComputed(memberExpression)` — line 7339

### `getStringFromPath(path)` — line 7356

### `resolveNamespaceVariables(baseVariable, path, astContext)` — line 7725

### `formatAttributes(attributes, { getObject })` — line 8213

### `removeJsExtension(name)` — line 8224

### `getCompleteAmdId(options, chunkId)` — line 8228

### `getSingleDefaultExport(exports, dependencies, interop, externalLiveBindings, getPropertyAccess)` — line 8322

### `getReexportedImportName(moduleVariableName, imported, depNamedExportsMode, isChunk, defaultVariableName, namespaceVariableName, interop, moduleId, externalLiveBindings, getPropertyAccess)` — line 8334

### `getEsModuleValue(getObject)` — line 8356

### `getNamespaceMarkers(hasNamedExports, addEsModule, addNamespaceToStringTag, { _, getObject })` — line 8361

### `getInteropBlock(dependencies, interop, externalLiveBindings, freeze, symbols, accessedGlobals, indent, snippets)` — line 8394

### `throwOnPhase(outputFormat, chunkId, dependencies)` — line 8449

### `addJsExtension(name)` — line 8456

### `updateExtensionForRelativeAmdId(id, forceJsExtensionForImports)` — line 8463

### `warnOnBuiltins(log, dependencies)` — line 8588

### `amd(magicString, { accessedGlobals, dependencies, exports, hasDefaultExport, hasExports, id, indent: t, intro, isEntryFacade, isModuleFacade, namedExportsMode, log, outro, snippets }, { amd, esModule, externalLiveBindings, freeze, generatedCode: { symbols }, interop, reexportProtoFromExternal, strict })` — line 8597

### `cjs(magicString, { accessedGlobals, dependencies, exports, hasDefaultExport, hasExports, id, indent: t, intro, isEntryFacade, isModuleFacade, namedExportsMode, outro, snippets }, { compact, esModule, externalLiveBindings, freeze, interop, generatedCode: { symbols }, reexportProtoFromExternal, strict })` — line 8637

### `es(magicString, { accessedGlobals, indent: t, intro, outro, dependencies, exports, snippets }, { externalLiveBindings, freeze, generatedCode: { symbols }, importAttributesKey })` — line 8674

### `getImportBlock(dependencies, importAttributesKey, { _ })` — line 8689

### `getExportBlock(exports, { _, cnst })` — line 8772

### `setupNamespace(name, root, globals, { _, getPropertyAccess, s }, compact, log)` — line 8793

### `assignToDeepVariable(deepName, root, globals, assignment, { _, getPropertyAccess }, log)` — line 8815

### `trimEmptyImports(dependencies)` — line 8843

### `iife(magicString, { accessedGlobals, dependencies, exports, hasDefaultExport, hasExports, id, indent: t, intro, namedExportsMode, log, outro, snippets }, { compact, esModule, extend, freeze, externalLiveBindings, reexportProtoFromExternal, globals, interop, name, generatedCode: { symbols }, strict })` — line 8854

### `system(magicString, { accessedGlobals, dependencies, exports, hasExports, id, indent: t, intro, snippets, outro, usesTopLevelAwait }, { externalLiveBindings, freeze, name, generatedCode: { symbols }, strict, systemNullSetters })` — line 8914

### `analyzeDependencies(dependencies, exports, t, { _, cnst, getObject, getPropertyAccess, n })` — line 8962

### `getExportsBlock(exports, t, { _, n })` — line 9044

### `globalProperty(name, globalVariable, getPropertyAccess)` — line 9064

### `safeAccess(name, globalVariable, { _, getPropertyAccess })` — line 9069

### `umd(magicString, { accessedGlobals, dependencies, exports, hasDefaultExport, hasExports, id, indent: t, intro, namedExportsMode, log, outro, snippets }, { amd, compact, esModule, extend, externalLiveBindings, freeze, interop, name, generatedCode: { symbols }, globals, noConflict, reexportProtoFromExternal, strict })` — line 9076

### `getDefaultExportFromCjs(x)` — line 9174

### `getAugmentedNamespace(n)` — line 9178

### `a()` — line 9182

### `requireConstants()` — line 9212

### `requireUtils()` — line 9405

### `requireScan()` — line 9486

### `requireParse()` — line 9885

### `requirePicomatch()` — line 11629

### `picomatch(glob, options, returnState = false)` — line 11636

### `extractAssignedNames(param)` — line 11683

### `isArray(arg)` — line 11690

### `normalizePath(filename)` — line 11702

### `createFilter(include, exclude, options)` — line 11720

### `result(id)` — line 11737

### `getRenderedLiteralValue(value)` — line 12227

### `getSimplifiedNumber(value)` — line 12242

### `renderCallArguments(code, options, node)` — line 12396

### `tryCastLiteralValueToBoolean(literalValue)` — line 12721

### `hasLoopBodyEffects(context, body)` — line 12984

### `includeLoopBody(context, body, includeChildrenRecursively)` — line 13000

### `getChunkInfoWithPath(chunk)` — line 13490

### `getInteropHelper(resolution, exportMode, interop)` — line 13793

### `getRenderedJsxChildren(children)` — line 14000

### `getAndIncludeFactoryVariable(factory, preserve, importSource, node, context)` — line 14011

### `findFunctionScope(scope, thisVariable)` — line 15487

### `isReassignedExportsMember(variable, exportNamesByVariable)` — line 15707

### `gatherSystemExportsAndGetSingleExport(separatedNodes, options, aggregatedSystemExports)` — line 15869

### `convertProgram(buffer, parent, parentScope)` — line 16025

### `panicError(node, position, buffer)` — line 16223

### `parseError(node, position, buffer)` — line 16226

### `arrayExpression(node, position, buffer)` — line 16229

### `arrayPattern(node, position, buffer)` — line 16233

### `arrowFunctionExpression(node, position, buffer)` — line 16237

### `assignmentExpression(node, position, buffer)` — line 16249

### `assignmentPattern(node, position, buffer)` — line 16255

### `awaitExpression(node, position, buffer)` — line 16260

### `binaryExpression(node, position, buffer)` — line 16264

### `blockStatement(node, position, buffer)` — line 16270

### `breakStatement(node, position, buffer)` — line 16274

### `callExpression(node, position, buffer)` — line 16279

### `catchClause(node, position, buffer)` — line 16287

### `chainExpression(node, position, buffer)` — line 16295

### `classBody(node, position, buffer)` — line 16299

### `classDeclaration(node, position, buffer)` — line 16317

### `classExpression(node, position, buffer)` — line 16328

### `conditionalExpression(node, position, buffer)` — line 16338

### `continueStatement(node, position, buffer)` — line 16344

### `debuggerStatement()` — line 16349

### `decorator(node, position, buffer)` — line 16350

### `directive(node, position, buffer)` — line 16354

### `doWhileStatement(node, position, buffer)` — line 16359

### `emptyStatement()` — line 16364

### `exportAllDeclaration(node, position, buffer)` — line 16365

### `exportDefaultDeclaration(node, position, buffer)` — line 16373

### `exportNamedDeclaration(node, position, buffer)` — line 16377

### `exportSpecifier(node, position, buffer)` — line 16387

### `expressionStatement(node, position, buffer)` — line 16394

### `forInStatement(node, position, buffer)` — line 16398

### `forOfStatement(node, position, buffer)` — line 16404

### `forStatement(node, position, buffer)` — line 16412

### `functionDeclaration(node, position, buffer)` — line 16422

### `functionExpression(node, position, buffer)` — line 16436

### `identifier(node, position, buffer)` — line 16449

### `ifStatement(node, position, buffer)` — line 16452

### `importAttribute(node, position, buffer)` — line 16462

### `importDeclaration(node, position, buffer)` — line 16467

### `importDefaultSpecifier(node, position, buffer)` — line 16475

### `importExpression(node, position, buffer)` — line 16479

### `importNamespaceSpecifier(node, position, buffer)` — line 16488

### `importSpecifier(node, position, buffer)` — line 16492

### `jsxAttribute(node, position, buffer)` — line 16499

### `jsxClosingElement(node, position, buffer)` — line 16505

### `jsxClosingFragment()` — line 16509

### `jsxElement(node, position, buffer)` — line 16510

### `jsxEmptyExpression()` — line 16520

### `jsxExpressionContainer(node, position, buffer)` — line 16521

### `jsxFragment(node, position, buffer)` — line 16525

### `jsxIdentifier(node, position, buffer)` — line 16531

### `jsxMemberExpression(node, position, buffer)` — line 16534

### `jsxNamespacedName(node, position, buffer)` — line 16539

### `jsxOpeningElement(node, position, buffer)` — line 16544

### `jsxOpeningFragment(node)` — line 16551

### `jsxSpreadAttribute(node, position, buffer)` — line 16555

### `jsxSpreadChild(node, position, buffer)` — line 16559

### `jsxText(node, position, buffer)` — line 16563

### `labeledStatement(node, position, buffer)` — line 16567

### `literalBigInt(node, position, buffer)` — line 16572

### `literalBoolean(node, position, buffer)` — line 16577

### `literalNull(node)` — line 16582

### `literalNumber(node, position, buffer)` — line 16585

### `literalRegExp(node, position, buffer)` — line 16590

### `literalString(node, position, buffer)` — line 16597

### `logicalExpression(node, position, buffer)` — line 16602

### `memberExpression(node, position, buffer)` — line 16608

### `metaProperty(node, position, buffer)` — line 16616

### `methodDefinition(node, position, buffer)` — line 16621

### `newExpression(node, position, buffer)` — line 16631

### `objectExpression(node, position, buffer)` — line 16637

### `objectPattern(node, position, buffer)` — line 16641

### `privateIdentifier(node, position, buffer)` — line 16645

### `program(node, position, buffer)` — line 16648

### `property(node, position, buffer)` — line 16653

### `propertyDefinition(node, position, buffer)` — line 16664

### `restElement(node, position, buffer)` — line 16674

### `returnStatement(node, position, buffer)` — line 16678

### `sequenceExpression(node, position, buffer)` — line 16684

### `spreadElement(node, position, buffer)` — line 16688

### `staticBlock(node, position, buffer)` — line 16692

### `superElement()` — line 16696

### `switchCase(node, position, buffer)` — line 16697

### `switchStatement(node, position, buffer)` — line 16703

### `taggedTemplateExpression(node, position, buffer)` — line 16708

### `templateElement(node, position, buffer)` — line 16713

### `templateLiteral(node, position, buffer)` — line 16721

### `thisExpression()` — line 16726

### `throwStatement(node, position, buffer)` — line 16727

### `tryStatement(node, position, buffer)` — line 16731

### `unaryExpression(node, position, buffer)` — line 16740

### `updateExpression(node, position, buffer)` — line 16745

### `variableDeclaration(node, position, buffer)` — line 16752

### `variableDeclarator(node, position, buffer)` — line 16757

### `whileStatement(node, position, buffer)` — line 16763

### `yieldExpression(node, position, buffer)` — line 16768

### `convertNode(parent, parentScope, position, buffer)` — line 16777

### `convertNodeList(parent, parentScope, position, buffer)` — line 16793

### `resetCacheToEncoded(cache)` — line 16933

### `resetSourcemapCache(map, sourcemapChain)` — line 16939

### `decodedSourcemap(map)` — line 16955

### `getId(m)` — line 16997

### `getOriginalLocation(sourcemapChain, location)` — line 17001

### `getAttributesFromImportExpression(node)` — line 17029

### `getAttributesFromImportExportDeclaration(attributes)` — line 17067

### `doAttributesDiffer(assertionA, assertionB)` — line 17072

### `getPersistedLabel(label, level)` — line 17079

### `timeStartImpl(label, level = 3)` — line 17095

### `timeEndImpl(label, level = 3)` — line 17114

### `getTimings()` — line 17124

### `getPluginWithTimers(plugin, index)` — line 17153

### `initialiseTimers(inputOptions)` — line 17183

### `getVariableForExportNameRecursive(target, name, importerForSideEffects, isExportAllSearch, searchedNamesAndModules = new Map()` — line 17200

### `getAndExtendSideEffectModules(variable, module)` — line 17220

### `foo()` — line 17921

### `setAlternativeExporterIfCyclic(variable, importer, reexporter)` — line 18175

### `createAddons(options, outputPluginDriver, chunk)` — line 18195

### `deconflictChunk(modules, dependenciesToBeDeconflicted, imports, usedNames, format, interop, preserveModules, externalLiveBindings, chunkByModule, externalChunkByModule, syntheticExports, exportNamesByVariable, accessedGlobalsByScope, includedNamespaces)` — line 18226

### `deconflictImportsEsmOrSystem(usedNames, imports, dependenciesToBeDeconflicted, _interop, preserveModules, _externalLiveBindings, chunkByModule, externalChunkByModule, syntheticExports)` — line 18237

### `deconflictImportsOther(usedNames, imports, { deconflictedDefault, deconflictedNamespace, dependencies }, interop, preserveModules, externalLiveBindings, chunkByModule, externalChunkByModule)` — line 18268

### `deconflictTopLevelVariables(usedNames, modules, includedNamespaces)` — line 18323

### `assignExportsToMangledNames(exports, exportsByName, exportNamesByVariable)` — line 18350

### `assignExportsToNames(exports, exportsByName, exportNamesByVariable)` — line 18368

### `getExportMode(chunk, { exports: exportMode, name, format }, facadeModuleId, log)` — line 18380

### `guessIndentString(code)` — line 18407

### `getIndentString(modules, options)` — line 18427

### `getStaticDependencies(chunk, orderedModules, chunkByModule, externalChunkByModule)` — line 18438

### `addStaticDependencies(module, staticDependencies, handledModules, chunk, chunkByModule, externalChunkByModule)` — line 18457

### `renderNamePattern(pattern, patternName, replacements)` — line 18561

### `makeUnique(name, { [lowercaseBundleKeys]: reservedLowercaseBundleKeys })` — line 18574

### `getGlobalName(chunk, globals, hasExports, log)` — line 18610

### `getChunkNameFromModule(module)` — line 19725

### `getPredefinedChunkNameFromModule(module)` — line 19728

### `getImportedBindingsPerDependency(renderedDependencies, resolveFileName)` — line 19731

### `getChunkAssignments(entries, manualChunkAliasByEntry, minChunkSize, log, isManualChunksFunctionForm, onlyExplicitManualChunks)` — line 19874

### `getChunkDefinitionsFromManualChunks(manualChunkAliasByEntry, isManualChunksFunctionForm, onlyExplicitManualChunks)` — line 19893

### `addStaticDependenciesToManualChunk(entry, manualChunkModules, modulesInManualChunks)` — line 19921

### `analyzeModuleGraph(entries, manualChunkModules, manualChunkModulesByModule)` — line 19933

### `getDynamicEntries(allEntriesAndManualChunks, dynamicEntryModules, dynamicImportModulesByEntry, awaitedDynamicEntryModules, awaitedDynamicImportModulesByEntry)` — line 20003

### `getDynamicImportsByEntry(dynamicImportModulesByEntry, entryIndexByModule)` — line 20027

### `getDynamicallyDependentEntriesByDynamicEntry(dependentEntriesByModule, dynamicEntries, allEntriesAndManualChunks, getDynamicImporters)` — line 20039

### `getChunksWithSameDependentEntries(moduleWithDependentEntries)` — line 20061

### `getStaticDependencyAtomsByEntry(entriesAndManualChunksCount, chunkAtoms)` — line 20089

### `getAlreadyLoadedAtomsByEntry(staticDependencyAtomsByEntry, dynamicallyDependentEntriesByDynamicEntry, dynamicImportsByEntry, allEntriesCount)` — line 20104

### `removeUnnecessaryDependentEntries(chunkAtoms, alreadyLoadedAtomsByEntry, awaitedAlreadyLoadedAtomsByEntry)` — line 20137

### `getChunksWithSameDependentEntriesAndCorrelatedAtoms(chunkAtoms, staticDependencyAtomsByEntry, alreadyLoadedAtomsByEntry, minChunkSize)` — line 20152

### `addChunkDependenciesAndGetExternalSideEffectAtoms(chunks, chunkByModule, nextAvailableAtomMask)` — line 20207

### `getOptimizedChunks(chunks, minChunkSize, sideEffectAtoms, sizeByAtom, log)` — line 20314

### `getPartitionedChunks(chunks, minChunkSize)` — line 20331

### `compareChunkSize({ size: sizeA }, { size: sizeB })` — line 20347

### `mergeChunks(chunkPartition, minChunkSize, sideEffectAtoms, sizeByAtom)` — line 20350

### `findBestMergeTarget(mergedChunk, { big, small }, sideEffectAtoms, sizeByAtom, smallestAdditionalSize)` — line 20385

### `getAdditionalSizeAfterMerge(mergedChunk, targetChunk, // The maximum additional unused code size allowed to be added by the merge, // taking dependencies into account, needs to be below this number
currentAdditionalSize, sideEffectAtoms, sizeByAtom)` — line 20409

### `getAdditionalSizeIfNoTransitiveDependencyOrNonCorrelatedSideEffect(dependentChunk, dependencyChunk, currentAdditionalSize, sideEffectAtoms, sizeByAtom)` — line 20419

### `getAtomsSizeIfBelowLimit(atoms, currentAdditionalSize, sizeByAtom)` — line 20442

### `getChunksInPartition(chunk, minChunkSize, chunkPartition)` — line 20458

### `commondir(files)` — line 20463

### `sortByExecutionOrder(units)` — line 20481

### `analyseModuleExecution(entryModules)` — line 20491

### `getCyclePath(module, parent, parents)` — line 20545

### `getGenerateCodeSnippets({ compact, generatedCode: { arrowFunctions, constBindings, objectShorthand, reservedNamesAsProps } })` — line 20560

### `getLinkMap(log)` — line 20722

### `linkMap(source, map)` — line 20723

### `getCollapsedSourcemap(id, originalCode, originalSourcemap, sourcemapChain, linkMap)` — line 20734

### `collapseSourcemaps(file, map, modules, bundleSourcemapChain, excludeContent, log)` — line 20749

### `collapseSourcemap(id, originalCode, originalSourcemap, sourcemapChain, log)` — line 20773

### `ensureBuffer(input)` — line 20791

### `renderChunks(chunks, bundle, pluginDriver, outputOptions, log)` — line 20807

### `reserveEntryChunksInBundle(chunks)` — line 20820

### `getChunkGraph(chunks)` — line 20828

### `transformChunk(magicString, fileName, usedModules, chunkGraph, options, outputPluginDriver, log)` — line 20834

### `transformChunksAndGenerateContentHashes(renderedChunks, chunkGraph, outputOptions, pluginDriver, getHash, log)` — line 20896

### `generateFinalHashes(renderedChunksByPlaceholder, hashDependenciesByPlaceholder, initialHashesByPlaceholder, placeholders, bundle, getHash)` — line 20950

### `addChunksToBundle(renderedChunksByPlaceholder, hashesByPlaceholder, bundle, nonHashedChunksWithPlaceholders, pluginDriver, options)` — line 20979

### `emitSourceMapAndGetComment(fileName, map, pluginDriver, { sourcemap, sourcemapBaseUrl })` — line 21011

### `calculateDebugIdAndGetComment(code, map)` — line 21030

### `validateOptionsForMultiChunkOutput(outputOptions, log)` — line 21209

### `getIncludedModules(modulesById)` — line 21219

### `getAbsoluteEntryModulePaths(includedModules, preserveModules)` — line 21229

### `getExternalChunkByModule(modulesById, outputOptions, inputBase)` — line 21238

### `addModuleToManualChunk(alias, module, manualChunkAliasByEntry)` — line 21247

### `isFileNameOutsideOutputDirectory(fileName)` — line 21254

### `validateOutputBundleFileNames(bundle)` — line 21263

### `flru(max)` — line 21277

### `keep(key, value)` — line 21281

### `reset(isPartial)` — line 21290

### `resolveIdViaPlugins(source, importer, pluginDriver, moduleLoaderResolveId, skip, customOptions, isEntry, attributes, importerAttributes)` — line 21337

### `resolveId(source, importer, preserveSymlinks, pluginDriver, moduleLoaderResolveId, skip, customOptions, isEntry, attributes, importerAttributes, fs)` — line 21368

### `addJsExtensionIfNecessary(file, preserveSymlinks, fs)` — line 21396

### `findFile(file, preserveSymlinks, fs)` — line 21401

### `stripBom(content)` — line 21419

### `asyncFlatten(array)` — line 21426

### `warnUnknownOptions(passedOptions, validOptions, optionType, log, ignoredKeys = /$./)` — line 21484

### `createPluginCache(cache)` — line 21591

### `getTrackedPluginCache(pluginCache, onUse)` — line 21615

### `uncacheablePluginError(pluginName)` — line 21647

### `getCacheForUncacheablePlugin(pluginName)` — line 21654

### `transform(source, module, pluginDriver, options)` — line 21671

### `transformReducer(previousCode, result, plugin)` — line 21683

### `normalizeRelativeExternalId(source, importer)` — line 22232

### `addChunkNamesToModule(module, { fileName, name }, isUserDefined)` — line 22239

### `isNotAbsoluteExternal(id, source, makeAbsoluteExternalsRelative)` — line 22248

### `waitForDependencyResolution(loadPromise)` — line 22253

### `generateAssetFileName(name, names, source, originalFileName, originalFileNames, sourceHash, outputOptions, bundle, inputOptions)` — line 22258

### `reserveFileNameInBundle(fileName, { bundle }, log)` — line 22284

### `hasValidType(emittedFile)` — line 22293

### `hasValidName(emittedFile)` — line 22297

### `getValidSource(source, emittedFile, fileReferenceId)` — line 22301

### `getAssetFileName(file, referenceId)` — line 22308

### `getChunkFileName(file, facadeChunkByModule)` — line 22314

### `getNamesFromAssets(consumedFiles)` — line 22635

### `getLogHandler(level, code, logger, pluginName, logLevel)` — line 22653

### `getPluginContext(plugin, pluginCache, graph, options, fileEmitter, existingPluginNames)` — line 22672

### `ensureArray(items)` — line 22729

### `getMatcherString(glob, cwd)` — line 22739

### `patternToIdFilter(pattern)` — line 22746

### `patternToCodeFilter(pattern)` — line 22763

### `normalizeFilter(filter)` — line 22787

### `createIdFilter(filter)` — line 22803

### `createCodeFilter(filter)` — line 22811

### `createFilterForId(filter)` — line 22819

### `createFilterForTransform(idFilter, codeFilter)` — line 22823

### `getSortedValidatedPlugins(hookName, plugins, validateHandler = validateFunctionPluginHandler)` — line 23078

### `validateFunctionPluginHandler(handler, hookName, plugin)` — line 23104

### `validateAddonPluginHandler(handler, hookName, plugin)` — line 23109

### `noReturn()` — line 23114

### `normalizeEntryModules(entryModules)` — line 23147

### `formatAction([pluginName, hookName, parameters])` — line 23339

### `catchUnfinishedHookActions(pluginDriver, callback)` — line 23363

### `initWasm()` — line 23391

### `getLogger(plugins, onLog, watchMode, logLevel)` — line 23394

### `normalizeInputOptions(config, watchMode)` — line 23433

### `sanitizeFileName(name)` — line 23602

### `normalizeOutputOptions(config, inputOptions, unsetInputOptions)` — line 23610

### `getExports(config, unsetOptions)` — line 23800

### `rollup(rawInputOptions)` — line 23892

### `rollupInternal(rawInputOptions, watcher)` — line 23895

### `getInputOptions(initialInputOptions, watchMode)` — line 23975

### `getProcessedInputOptions(inputOptions, watchMode)` — line 23984

### `normalizePlugins(plugins, anonymousPrefix)` — line 24004

### `handleGenerateWrite(isWrite, inputOptions, unsetInputOptions, rawOutputOptions, graph)` — line 24011

### `getOutputOptionsAndPluginDriver(rawOutputOptions, inputPluginDriver, inputOptions, unsetInputOptions)` — line 24028

### `getOutputOptions(inputOptions, unsetInputOptions, rawOutputOptions, outputPluginDriver)` — line 24040

### `createOutput(outputBundle)` — line 24050

### `getSortingFileType(file)` — line 24061

### `writeOutputFile(outputFile, outputOptions, { fs: { mkdir, writeFile } })` — line 24070

### `defineConfig(options)` — line 24082

### `requirePicocolors()` — line 24090

### `handleError(error, recover = false)` — line 24180

### `mergeOptions(config, watchMode, rawCommandOptions = EMPTY_COMMAND_OPTIONS, printLog)` — line 24236

### `getCommandOptions(rawCommandOptions)` — line 24270

### `mergeInputOptions(config, overrides, plugins, log, onLog)` — line 24289

### `mergeOutputOptions(config, overrides, log)` — line 24342

### `loadFsEvents()` — line 24404

### `getFsEvents()` — line 24413

### `watch(configs)` — line 24478

### `ensureTrailingSlash(path)` — line 24485

### `checkWatchConfig(config)` — line 24491

### `watchInternal(configs, emitter)` — line 24512

### `replacer(match)` — line 844

### `isAnyWellKnown(v)` — line 2018

### `isConcreteKey(v)` — line 2021

### `deoptimizeInteraction(interaction)` — line 2321

### `includeInteraction(interaction, context)` — line 2326

### `includeInteractionWithoutThis({ args }, context)` — line 2332

### `getPureFunctions({ treeshake })` — line 2363

### `startsWithDigit(value)` — line 2561

### `needsEscape(value)` — line 2562

### `doNothing()` — line 2702

### `isInteger(property)` — line 3751

### `getUnknownValue()` — line 4024

### `returnFalse()` — line 4025

### `returnTrue()` — line 4026

### `getWellKnownSymbol(symbol)` — line 4027

### `limitConcatenatedPathDepth(path1, path2)` — line 5063

### `parameters(this.params = params.map((parameter)` — line 6809

### `isDefaultAProperty(interopType, externalLiveBindings)` — line 7068

### `canDefaultBeTakenFromNamespace(interopType, externalLiveBindings)` — line 7077

### `getHelpersBlock(additionalHelpers, accessedGlobals, indent, snippets, liveBindings, freeze, symbols)` — line 7078

### `getDefaultLiveBinding({ _, getObject })` — line 7178

### `getDefaultStatic({ _, getPropertyAccess })` — line 7179

### `getIsCompatNamespace({ _ })` — line 7180

### `createNamespaceObject(t, index, snippets, liveBindings, freeze, symbols)` — line 7181

### `loopOverKeys(body, allowVariableLoopVariable, { _, cnst, getFunctionIntro, s })` — line 7193

### `loopOverNamespaces(body, useForEach, t, { _, cnst, getDirectReturnFunction, getFunctionIntro, n })` — line 7199

### `copyNonDefaultOwnPropertyLiveBinding(t, index, snippets)` — line 7216

### `copyOwnPropertyLiveBinding(t, index, { _, cnst, getDirectReturnFunction, n })` — line 7222

### `copyPropertyLiveBinding(t, index, { _, cnst, getDirectReturnFunction, n })` — line 7234

### `copyPropertyStatic(_t, index, { _, n })` — line 7248

### `getFrozen(freeze, fragment)` — line 7249

### `getWithToStringTag(symbols, fragment, { _, getObject })` — line 7250

### `getResolveUrl(path, asObject, URL = 'URL')` — line 7854

### `getRelativeUrlFromDocument(relativePath, asObject, umd = false)` — line 7855

### `getGenericImportMetaMechanism(getUrl)` — line 7856

### `getFileUrlFromFullPath(path, asObject)` — line 7864

### `getFileUrlFromRelativePath(path, asObject)` — line 7865

### `getUrlFromDocument(chunkId, umd = false)` — line 7866

### `getDynamicNamespaceVariable(namespace)` — line 8127

### `getDefineProperty(name, needsLiveBinding, t, { _, getDirectReturnFunction, n })` — line 8380

### `addInteropStatement(helperVariableName, helper, dependencyVariableName)` — line 8398

### `keypath(keypath, getPropertyAccess)` — line 8791

### `getStarExcludes({ dependencies, exports })` — line 9016

### `getStarExcludesBlock(starExcludes, t, { _, cnst, getObject, n })` — line 9029

### `getImportBindingsBlock(importBindings, t, { _, n })` — line 9042

### `getHoistedExportsBlock(exports, t, snippets)` — line 9043

### `getSyntheticExportsBlock(exports, t, snippets)` — line 9057

### `getMissingExportsBlock(exports, t, snippets)` — line 9060

### `scan(input, options)` — line 9536

### `eos()` — line 9564

### `peek()` — line 9565

### `advance()` — line 9566

### `expandRange(args, options)` — line 9908

### `syntaxError(type, char)` — line 9923

### `parseRepeatedExtglob(pattern, requireEnd = true)` — line 10043

### `analyzeRepeatedExtglob(body, options)` — line 10162

### `parse(input, options)` — line 10205

### `remaining()` — line 10295

### `consume(value = '', num = 0)` — line 10296

### `negate()` — line 10306

### `extglobOpen(type, value)` — line 10372

### `isIgnored()` — line 11332

### `matcher(input, returnObject = false)` — line 11338

### `getMatcher(id)` — line 11722

### `getIncludedPatternPath(destructuredInitPath)` — line 11852

### `getPropertyKey(property)` — line 17061

### `addSideEffectDependencies(possibleDependencies)` — line 17997

### `copyNameToModulesMap(searchedNamesAndModules)` — line 18189

### `sortExportedVariables([a], [b])` — line 18191

### `concatSeparator(out, next)` — line 18193

### `concatDblSeparator(out, next)` — line 18194

### `getHashPlaceholderGenerator()` — line 18484

### `replacePlaceholders(code, hashesByPlaceholder)` — line 18498

### `replaceSinglePlaceholder(code, placeholder, value)` — line 18499

### `replacePlaceholdersWithDefaultAndGetContainedPlaceholders(code, placeholders)` — line 18500

### `getOutputBundle(outputBundleBase)` — line 18516

### `removeUnreferencedAssets(outputBundle)` — line 18539

### `finalize(code)` — line 18744

### `resolveFileName(dependency)` — line 19750

### `compareExecIndex(unitA, unitB)` — line 20480

### `handleSyncLoadedModule(module, parent)` — line 20498

### `analyseModule(module)` — line 20508

### `getNonArrowFunctionIntro(parameters, { isAsync, name })` — line 20563

### `getDirectReturnFunction(parameters, { functionReturn, lineBreakIndent, name })` — line 20571

### `wrapIfNeeded(code, needsParens)` — line 20619

### `handleDependency(chunk, parent)` — line 21174

### `analyseChunk(chunk)` — line 21194

### `getOnLog(config, logLevel, printLog = defaultPrintLog)` — line 21433

### `getDefaultOnLog(printLog, onwarn)` — line 21449

### `addLogToString(log)` — line 21459

### `normalizeLog(log)` — line 21466

### `defaultPrintLog(level, { message })` — line 21471

### `objectifyOption(value)` — line 21562

### `objectifyOptionWithPresets(presets, optionName, urlSnippet, additionalValues)` — line 21563

### `getOptionWithPreset(value, presets, optionName, urlSnippet, additionalValues)` — line 21573

### `normalizePluginOption(plugins)` — line 21586

### `useCustomTransformCache()` — line 21680

### `handleChange(...parameters)` — line 23203

### `handleClose()` — line 23204

### `logger(level, log, skipped = EMPTY_SET)` — line 23397

### `getCache(config)` — line 23468

### `getIdMatcher(option)` — line 23471

### `getInput(config)` — line 23493

### `getJsx(config)` — line 23497

### `getMaxParallelFileOps(config)` — line 23537

### `getModuleContext(config, context)` — line 23546

### `getTreeshake(config)` — line 23560

### `getHasModuleSideEffects(moduleSideEffectsOption)` — line 23578

### `getFile(config, preserveModules, inputOptions)` — line 23686

### `getFormat(config)` — line 23697

### `getInlineDynamicImports(config, inputOptions)` — line 23724

### `getPreserveModules(config, inlineDynamicImports, inputOptions)` — line 23732

### `getPreserveModulesRoot(config)` — line 23744

### `getAmd(config)` — line 23751

### `getAddon(config, name)` — line 23779

### `getDir(config, file)` — line 23786

### `getEntryFileNames(config, unsetOptions)` — line 23793

### `getExternalImportAttributes(config, inputOptions)` — line 23810

### `getGeneratedCode(config)` — line 23816

### `getIndent(config, compact)` — line 23826

### `getInterop(config)` — line 23840

### `validateInterop(interop)` — line 23853

### `getManualChunks(config, inlineDynamicImports, preserveModules)` — line 23859

### `getMinifyInternalExports(config, format, compact)` — line 23871

### `getSourcemapFileNames(config, unsetOptions)` — line 23872

### `getSourcemapBaseUrl(config)` — line 23879

### `emitError()` — line 24042

### `formatter(open, close, replace = open)` — line 24098

### `replaceClose(string, close, replace, index)` — line 24104

### `createColors(enabled = isColorSupported)` — line 24114

### `stderr(...parameters)` — line 24179

### `getOption(name)` — line 24290

### `getExternal(config, overrides)` — line 24318

### `getObjectOption(config, overrides, name, objectifyValue = objectifyOption)` — line 24324

### `getWatch(config, overrides)` — line 24332

### `normalizeObjectOptionValue(optionValue, objectifyValue)` — line 24333

### `selfRemovingListener(...parameters)` — line 24454

### `handler(...parameters)` — line 17164
