# `module-runner.js`

> **Path:** `frontend/node_modules/vite/dist/node/module-runner.js`  
> **Last documented:** 2026-06-29 18:03

---

## Dependencies / Imports

- `${rawId}`

## Functions

### `unwrapId(id)` — line 5

### `slash(p)` — line 9

### `cleanUrl(url)` — line 13

### `isPrimitive(value)` — line 16

### `getAsyncFunctionDeclarationPaddingLineCount()` — line 22

### `promiseWithResolvers()` — line 30

### `normalizeWindowsPath(input = "")` — line 37

### `cwd()` — line 41

### `normalizeString(path, allowAboveRoot)` — line 53

### `encodePathChars(filepath)` — line 89

### `posixPathToFileHref(posixPath)` — line 94

### `toWindowsPath(path)` — line 99

### `decodeInteger(reader, relative)` — line 107

### `hasMoreVlq(reader, max)` — line 116

### `decode(mappings)` — line 134

### `sort(line)` — line 148

### `sortComparator(a, b)` — line 151

### `binarySearch(haystack, needle, low, high)` — line 156

### `upperBound(haystack, needle, index)` — line 165

### `lowerBound(haystack, needle, index)` — line 170

### `memoizedBinarySearch(haystack, needle, state, key)` — line 175

### `cast(map)` — line 186

### `decodedMappings(map)` — line 189

### `originalPositionFor(map, needle)` — line 193

### `OMapping(source, line, column, name)` — line 211

### `traceSegmentInternal(segments, memo, line, column, bias)` — line 214

### `memoizedState()` — line 234

### `getOriginalPosition(map, needle)` — line 241

### `normalizeModuleId(file)` — line 337

### `decline()` — line 382

### `analyzeImportedModDifference(mod, rawId, moduleType, metadata)` — line 525

### `reviveInvokeError(e)` — line 547

### `createHMRHandler(handler)` — line 736

### `createHMRHandlerForRunner(runner)` — line 761

### `getModulesByFile(runner, file)` — line 821

### `getModulesEntrypoints(runner, modules, visited = /* @__PURE__ */ new Set()` — line 825

### `findAllEntrypoints(runner, entrypoints = /* @__PURE__ */ new Set()` — line 841

### `resetInterceptor(runner, options)` — line 857

### `interceptStackTrace(runner, options = {})` — line 860

### `supportRelativeURL(file, url)` — line 863

### `getRunnerSourceMap(position)` — line 870

### `retrieveFile(path)` — line 882

### `retrieveSourceMapURL(source)` — line 887

### `retrieveSourceMap(source)` — line 896

### `mapSourcePosition(position)` — line 912

### `mapEvalOrigin(origin)` — line 950

### `CallSiteToString()` — line 963

### `cloneCallSite(frame)` — line 990

### `wrapCallSite(frame, state)` — line 999

### `prepareStackTrace(error, stack)` — line 1034

### `enableSourceMapSupport(runner)` — line 1041

### `exportAll(exports, sourceModule)` — line 1287

### `addToMap(map)` — line 400

### `removeFromMap(map)` — line 407

### `createInvokeableTransport(transport)` — line 555

### `getStack()` — line 1168

### `AsyncFunction()` — line 19

### `resolve(...arguments_)` — line 44

### `isAbsolute(p)` — line 83
