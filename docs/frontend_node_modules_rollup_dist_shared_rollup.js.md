# `rollup.js`

> **Path:** `frontend/node_modules/rollup/dist/shared/rollup.js`  
> **Last documented:** 2026-06-29 18:03

---

## Dependencies / Imports

- `estree`

## Functions

### `_interopNamespaceDefault(e)` — line 20

### `_mergeNamespaces(n, m)` — line 31

### `ensureBuffer(input)` — line 75

### `getOrCreate(map, key, init)` — line 86

### `getNewSet()` — line 95

### `getNewArray()` — line 98

### `toBase64(value)` — line 104

### `renderNamePattern(pattern, patternName, replacements)` — line 199

### `makeUnique(name, { [lowercaseBundleKeys]: reservedLowercaseBundleKeys })` — line 212

### `generateAssetFileName(name, names, source, originalFileName, originalFileNames, sourceHash, outputOptions, bundle, inputOptions)` — line 228

### `reserveFileNameInBundle(fileName, { bundle }, log)` — line 254

### `hasValidType(emittedFile)` — line 263

### `hasValidName(emittedFile)` — line 267

### `getValidSource(source, emittedFile, fileReferenceId)` — line 271

### `getAssetFileName(file, referenceId)` — line 278

### `getChunkFileName(file, facadeChunkByModule)` — line 284

### `getNamesFromAssets(consumedFiles)` — line 605

### `asyncFlatten(array)` — line 625

### `warnUnknownOptions(passedOptions, validOptions, optionType, log, ignoredKeys = /$./)` — line 683

### `getLogHandler(level, code, logger, pluginName, logLevel)` — line 787

### `createPluginCache(cache)` — line 808

### `getTrackedPluginCache(pluginCache, onUse)` — line 832

### `uncacheablePluginError(pluginName)` — line 864

### `getCacheForUncacheablePlugin(pluginName)` — line 871

### `getPluginContext(plugin, pluginCache, graph, options, fileEmitter, existingPluginNames)` — line 889

### `getDefaultExportFromCjs(x)` — line 946

### `getAugmentedNamespace(n)` — line 950

### `a()` — line 954

### `requireConstants()` — line 984

### `requireUtils()` — line 1177

### `requireScan()` — line 1258

### `requireParse()` — line 1657

### `requirePicomatch()` — line 3401

### `picomatch(glob, options, returnState = false)` — line 3408

### `patternToIdFilter(pattern)` — line 3433

### `patternToCodeFilter(pattern)` — line 3450

### `normalizeFilter(filter)` — line 3474

### `createIdFilter(filter)` — line 3490

### `createCodeFilter(filter)` — line 3498

### `createFilterForId(filter)` — line 3506

### `createFilterForTransform(idFilter, codeFilter)` — line 3510

### `getSortedValidatedPlugins(hookName, plugins, validateHandler = validateFunctionPluginHandler)` — line 3765

### `validateFunctionPluginHandler(handler, hookName, plugin)` — line 3791

### `validateAddonPluginHandler(handler, hookName, plugin)` — line 3796

### `noReturn()` — line 3801

### `getLogger(plugins, onLog, watchMode, logLevel)` — line 3804

### `mergeOptions(config, watchMode, rawCommandOptions = EMPTY_COMMAND_OPTIONS, printLog)` — line 3855

### `getCommandOptions(rawCommandOptions)` — line 3889

### `mergeInputOptions(config, overrides, plugins, log, onLog)` — line 3908

### `mergeOutputOptions(config, overrides, log)` — line 3967

### `requirePicocolors()` — line 4031

### `handleError(error, recover = false)` — line 4121

### `decodeInteger(reader, relative)` — line 4172

### `encodeInteger(builder, num, relative)` — line 4189

### `hasMoreVlq(reader, max)` — line 4200

### `decode(mappings)` — line 4259

### `sort(line)` — line 4301

### `sortComparator(a, b)` — line 4304

### `encode(decoded)` — line 4307

### `getBtoa()` — line 4526

### `guessIndent(code)` — line 4565

### `getRelativePath(from, to)` — line 4591

### `isObject(thing)` — line 4612

### `getLocator(source)` — line 4616

### `locate(index)` — line 4625

### `getReplacement(match, str)` — line 5524

### `matchAll(re, str)` — line 5538

### `treeshakeNode(node, code, start, end)` — line 5917

### `findFirstOccurrenceOutsideComment(code, searchString, start = 0)` — line 5924

### `findNonWhiteSpace(code, index)` — line 5944

### `findLastWhiteSpaceReverse(code, start, end)` — line 5950

### `findFirstLineBreakOutsideComment(code)` — line 5965

### `renderStatementList(statements, code, start, end, options)` — line 5982

### `getCommaSeparatedNodesWithBoundaries(nodes, code, start, end)` — line 6022

### `removeLineBreaks(code, start, end)` — line 6061

### `getSystemExportStatement(exportedVariables, { exportNamesByVariable, snippets: { _, getObject, getPropertyAccess } }, modifier = '')` — line 6071

### `renderSystemExportExpression(exportedVariable, expressionStart, expressionEnd, code, { exportNamesByVariable, snippets: { _ } })` — line 6088

### `renderSystemExportFunction(exportedVariables, expressionStart, expressionEnd, needsParens, code, options)` — line 6092

### `renderSystemExportSequenceAfterExpression(exportedVariable, expressionStart, expressionEnd, needsParens, code, options)` — line 6097

### `renderSystemExportSequenceBeforeExpression(exportedVariable, expressionStart, expressionEnd, needsParens, code, options, modifier)` — line 6105

### `is_reference(node, parent)` — line 6291

### `createInclusionContext()` — line 6336

### `createHasEffectsContext()` — line 6345

### `isFlagSet(flags, flag)` — line 6366

### `setFlag(flags, flag, value)` — line 6369

### `cacheObjectGetters(object, getterProperties)` — line 6605

### `isLegal(value)` — line 6674

### `makeLegal(value)` — line 6680

### `stringifyObjectKeyIfNeeded(key)` — line 6690

### `stringifyIdentifierIfNeeded(key)` — line 6699

### `markModuleAndImpureDependenciesAsExecuted(baseModule)` — line 6795

### `createChildNodeKeysForNode(esTreeNode)` — line 7110

### `onlyIncludeSelf()` — line 7113

### `onlyIncludeSelfNoDeoptimize()` — line 7118

### `doNotDeoptimize()` — line 7121

### `isObjectExpressionNode(node)` — line 7125

### `isPropertyNode(node)` — line 7128

### `isArrowFunctionExpressionNode(node)` — line 7131

### `isFunctionExpressionNode(node)` — line 7134

### `isCallExpressionNode(node)` — line 7137

### `isMemberExpressionNode(node)` — line 7140

### `isAwaitExpressionNode(node)` — line 7143

### `isIdentifierNode(node)` — line 7146

### `isExpressionStatementNode(node)` — line 7149

### `assembleMemberDescriptions(memberDescriptions, inheritedDescriptions = null)` — line 7153

### `getLiteralMembersForValue(value)` — line 7326

### `hasMemberEffectWhenCalled(members, memberName, interaction, context)` — line 7343

### `getMemberReturnExpressionWhenCalled(members, memberName)` — line 7349

### `getGlobalAtPath(path)` — line 9098

### `closestParentFunctionOrProgram(node)` — line 9511

### `if(name === 'eval' &&
                renderedParentType === parseAst_js.CallExpression &&
                isCalleeOfRenderedParent)` — line 9630

### `getSafeName(baseName, usedNames, forbiddenNames)` — line 9640

### `checkEffectForNodes(nodes, context)` — line 9787

### `isStaticBlock(statement)` — line 9937

### `addArgumentToBeDeoptimized(argument)` — line 10134

### `getDeclarationStart(code, start)` — line 11033

### `getFunctionIdInsertPosition(code, start)` — line 11036

### `escapeId(id)` — line 11144

### `getToStringTagValue(getObject)` — line 11351

### `getChainElementLiteralValueAtPath(element, object, path, recursionTracker, origin)` — line 11411

### `getResolvablePropertyKey(memberExpression)` — line 11425

### `getResolvableComputedPropertyKey(propertyKey)` — line 11430

### `getPathIfNotComputed(memberExpression)` — line 11436

### `getStringFromPath(path)` — line 11453

### `resolveNamespaceVariables(baseVariable, path, astContext)` — line 11822

### `formatAttributes(attributes, { getObject })` — line 12310

### `removeJsExtension(name)` — line 12321

### `getCompleteAmdId(options, chunkId)` — line 12325

### `getSingleDefaultExport(exports, dependencies, interop, externalLiveBindings, getPropertyAccess)` — line 12419

### `getReexportedImportName(moduleVariableName, imported, depNamedExportsMode, isChunk, defaultVariableName, namespaceVariableName, interop, moduleId, externalLiveBindings, getPropertyAccess)` — line 12431

### `getEsModuleValue(getObject)` — line 12453

### `getNamespaceMarkers(hasNamedExports, addEsModule, addNamespaceToStringTag, { _, getObject })` — line 12458

### `getInteropBlock(dependencies, interop, externalLiveBindings, freeze, symbols, accessedGlobals, indent, snippets)` — line 12491

### `throwOnPhase(outputFormat, chunkId, dependencies)` — line 12546

### `addJsExtension(name)` — line 12553

### `updateExtensionForRelativeAmdId(id, forceJsExtensionForImports)` — line 12560

### `warnOnBuiltins(log, dependencies)` — line 12685

### `amd(magicString, { accessedGlobals, dependencies, exports, hasDefaultExport, hasExports, id, indent: t, intro, isEntryFacade, isModuleFacade, namedExportsMode, log, outro, snippets }, { amd, esModule, externalLiveBindings, freeze, generatedCode: { symbols }, interop, reexportProtoFromExternal, strict })` — line 12694

### `cjs(magicString, { accessedGlobals, dependencies, exports, hasDefaultExport, hasExports, id, indent: t, intro, isEntryFacade, isModuleFacade, namedExportsMode, outro, snippets }, { compact, esModule, externalLiveBindings, freeze, interop, generatedCode: { symbols }, reexportProtoFromExternal, strict })` — line 12734

### `es(magicString, { accessedGlobals, indent: t, intro, outro, dependencies, exports, snippets }, { externalLiveBindings, freeze, generatedCode: { symbols }, importAttributesKey })` — line 12771

### `getImportBlock(dependencies, importAttributesKey, { _ })` — line 12786

### `getExportBlock(exports, { _, cnst })` — line 12869

### `setupNamespace(name, root, globals, { _, getPropertyAccess, s }, compact, log)` — line 12890

### `assignToDeepVariable(deepName, root, globals, assignment, { _, getPropertyAccess }, log)` — line 12912

### `trimEmptyImports(dependencies)` — line 12940

### `iife(magicString, { accessedGlobals, dependencies, exports, hasDefaultExport, hasExports, id, indent: t, intro, namedExportsMode, log, outro, snippets }, { compact, esModule, extend, freeze, externalLiveBindings, reexportProtoFromExternal, globals, interop, name, generatedCode: { symbols }, strict })` — line 12951

### `system(magicString, { accessedGlobals, dependencies, exports, hasExports, id, indent: t, intro, snippets, outro, usesTopLevelAwait }, { externalLiveBindings, freeze, name, generatedCode: { symbols }, strict, systemNullSetters })` — line 13011

### `analyzeDependencies(dependencies, exports, t, { _, cnst, getObject, getPropertyAccess, n })` — line 13059

### `getExportsBlock(exports, t, { _, n })` — line 13141

### `globalProperty(name, globalVariable, getPropertyAccess)` — line 13161

### `safeAccess(name, globalVariable, { _, getPropertyAccess })` — line 13166

### `umd(magicString, { accessedGlobals, dependencies, exports, hasDefaultExport, hasExports, id, indent: t, intro, namedExportsMode, log, outro, snippets }, { amd, compact, esModule, extend, externalLiveBindings, freeze, interop, name, generatedCode: { symbols }, globals, noConflict, reexportProtoFromExternal, strict })` — line 13173

### `extractAssignedNames(param)` — line 13300

### `isArray(arg)` — line 13307

### `ensureArray(thing)` — line 13310

### `normalizePath(filename)` — line 13319

### `getMatcherString(id, resolutionBase)` — line 13323

### `createFilter(include, exclude, options)` — line 13337

### `result(id)` — line 13354

### `getRenderedLiteralValue(value)` — line 13844

### `getSimplifiedNumber(value)` — line 13859

### `renderCallArguments(code, options, node)` — line 14013

### `tryCastLiteralValueToBoolean(literalValue)` — line 14338

### `hasLoopBodyEffects(context, body)` — line 14601

### `includeLoopBody(context, body, includeChildrenRecursively)` — line 14617

### `getChunkInfoWithPath(chunk)` — line 15107

### `getInteropHelper(resolution, exportMode, interop)` — line 15410

### `getRenderedJsxChildren(children)` — line 15617

### `getAndIncludeFactoryVariable(factory, preserve, importSource, node, context)` — line 15628

### `findFunctionScope(scope, thisVariable)` — line 17104

### `isReassignedExportsMember(variable, exportNamesByVariable)` — line 17324

### `gatherSystemExportsAndGetSingleExport(separatedNodes, options, aggregatedSystemExports)` — line 17486

### `convertProgram(buffer, parent, parentScope)` — line 17642

### `panicError(node, position, buffer)` — line 17840

### `parseError(node, position, buffer)` — line 17843

### `arrayExpression(node, position, buffer)` — line 17846

### `arrayPattern(node, position, buffer)` — line 17850

### `arrowFunctionExpression(node, position, buffer)` — line 17854

### `assignmentExpression(node, position, buffer)` — line 17866

### `assignmentPattern(node, position, buffer)` — line 17872

### `awaitExpression(node, position, buffer)` — line 17877

### `binaryExpression(node, position, buffer)` — line 17881

### `blockStatement(node, position, buffer)` — line 17887

### `breakStatement(node, position, buffer)` — line 17891

### `callExpression(node, position, buffer)` — line 17896

### `catchClause(node, position, buffer)` — line 17904

### `chainExpression(node, position, buffer)` — line 17912

### `classBody(node, position, buffer)` — line 17916

### `classDeclaration(node, position, buffer)` — line 17934

### `classExpression(node, position, buffer)` — line 17945

### `conditionalExpression(node, position, buffer)` — line 17955

### `continueStatement(node, position, buffer)` — line 17961

### `debuggerStatement()` — line 17966

### `decorator(node, position, buffer)` — line 17967

### `directive(node, position, buffer)` — line 17971

### `doWhileStatement(node, position, buffer)` — line 17976

### `emptyStatement()` — line 17981

### `exportAllDeclaration(node, position, buffer)` — line 17982

### `exportDefaultDeclaration(node, position, buffer)` — line 17990

### `exportNamedDeclaration(node, position, buffer)` — line 17994

### `exportSpecifier(node, position, buffer)` — line 18004

### `expressionStatement(node, position, buffer)` — line 18011

### `forInStatement(node, position, buffer)` — line 18015

### `forOfStatement(node, position, buffer)` — line 18021

### `forStatement(node, position, buffer)` — line 18029

### `functionDeclaration(node, position, buffer)` — line 18039

### `functionExpression(node, position, buffer)` — line 18053

### `identifier(node, position, buffer)` — line 18066

### `ifStatement(node, position, buffer)` — line 18069

### `importAttribute(node, position, buffer)` — line 18079

### `importDeclaration(node, position, buffer)` — line 18084

### `importDefaultSpecifier(node, position, buffer)` — line 18092

### `importExpression(node, position, buffer)` — line 18096

### `importNamespaceSpecifier(node, position, buffer)` — line 18105

### `importSpecifier(node, position, buffer)` — line 18109

### `jsxAttribute(node, position, buffer)` — line 18116

### `jsxClosingElement(node, position, buffer)` — line 18122

### `jsxClosingFragment()` — line 18126

### `jsxElement(node, position, buffer)` — line 18127

### `jsxEmptyExpression()` — line 18137

### `jsxExpressionContainer(node, position, buffer)` — line 18138

### `jsxFragment(node, position, buffer)` — line 18142

### `jsxIdentifier(node, position, buffer)` — line 18148

### `jsxMemberExpression(node, position, buffer)` — line 18151

### `jsxNamespacedName(node, position, buffer)` — line 18156

### `jsxOpeningElement(node, position, buffer)` — line 18161

### `jsxOpeningFragment(node)` — line 18168

### `jsxSpreadAttribute(node, position, buffer)` — line 18172

### `jsxSpreadChild(node, position, buffer)` — line 18176

### `jsxText(node, position, buffer)` — line 18180

### `labeledStatement(node, position, buffer)` — line 18184

### `literalBigInt(node, position, buffer)` — line 18189

### `literalBoolean(node, position, buffer)` — line 18194

### `literalNull(node)` — line 18199

### `literalNumber(node, position, buffer)` — line 18202

### `literalRegExp(node, position, buffer)` — line 18207

### `literalString(node, position, buffer)` — line 18214

### `logicalExpression(node, position, buffer)` — line 18219

### `memberExpression(node, position, buffer)` — line 18225

### `metaProperty(node, position, buffer)` — line 18233

### `methodDefinition(node, position, buffer)` — line 18238

### `newExpression(node, position, buffer)` — line 18248

### `objectExpression(node, position, buffer)` — line 18254

### `objectPattern(node, position, buffer)` — line 18258

### `privateIdentifier(node, position, buffer)` — line 18262

### `program(node, position, buffer)` — line 18265

### `property(node, position, buffer)` — line 18270

### `propertyDefinition(node, position, buffer)` — line 18281

### `restElement(node, position, buffer)` — line 18291

### `returnStatement(node, position, buffer)` — line 18295

### `sequenceExpression(node, position, buffer)` — line 18301

### `spreadElement(node, position, buffer)` — line 18305

### `staticBlock(node, position, buffer)` — line 18309

### `superElement()` — line 18313

### `switchCase(node, position, buffer)` — line 18314

### `switchStatement(node, position, buffer)` — line 18320

### `taggedTemplateExpression(node, position, buffer)` — line 18325

### `templateElement(node, position, buffer)` — line 18330

### `templateLiteral(node, position, buffer)` — line 18338

### `thisExpression()` — line 18343

### `throwStatement(node, position, buffer)` — line 18344

### `tryStatement(node, position, buffer)` — line 18348

### `unaryExpression(node, position, buffer)` — line 18357

### `updateExpression(node, position, buffer)` — line 18362

### `variableDeclaration(node, position, buffer)` — line 18369

### `variableDeclarator(node, position, buffer)` — line 18374

### `whileStatement(node, position, buffer)` — line 18380

### `yieldExpression(node, position, buffer)` — line 18385

### `convertNode(parent, parentScope, position, buffer)` — line 18394

### `convertNodeList(parent, parentScope, position, buffer)` — line 18410

### `resetCacheToEncoded(cache)` — line 18543

### `resetSourcemapCache(map, sourcemapChain)` — line 18549

### `decodedSourcemap(map)` — line 18565

### `getId(m)` — line 18607

### `getOriginalLocation(sourcemapChain, location)` — line 18611

### `getAttributesFromImportExpression(node)` — line 18639

### `getAttributesFromImportExportDeclaration(attributes)` — line 18677

### `doAttributesDiffer(assertionA, assertionB)` — line 18682

### `getPersistedLabel(label, level)` — line 18689

### `timeStartImpl(label, level = 3)` — line 18705

### `timeEndImpl(label, level = 3)` — line 18724

### `getTimings()` — line 18734

### `getPluginWithTimers(plugin, index)` — line 18763

### `initialiseTimers(inputOptions)` — line 18793

### `getVariableForExportNameRecursive(target, name, importerForSideEffects, isExportAllSearch, searchedNamesAndModules = new Map()` — line 18810

### `getAndExtendSideEffectModules(variable, module)` — line 18830

### `foo()` — line 19531

### `setAlternativeExporterIfCyclic(variable, importer, reexporter)` — line 19785

### `createAddons(options, outputPluginDriver, chunk)` — line 19805

### `deconflictChunk(modules, dependenciesToBeDeconflicted, imports, usedNames, format, interop, preserveModules, externalLiveBindings, chunkByModule, externalChunkByModule, syntheticExports, exportNamesByVariable, accessedGlobalsByScope, includedNamespaces)` — line 19836

### `deconflictImportsEsmOrSystem(usedNames, imports, dependenciesToBeDeconflicted, _interop, preserveModules, _externalLiveBindings, chunkByModule, externalChunkByModule, syntheticExports)` — line 19847

### `deconflictImportsOther(usedNames, imports, { deconflictedDefault, deconflictedNamespace, dependencies }, interop, preserveModules, externalLiveBindings, chunkByModule, externalChunkByModule)` — line 19878

### `deconflictTopLevelVariables(usedNames, modules, includedNamespaces)` — line 19933

### `assignExportsToMangledNames(exports, exportsByName, exportNamesByVariable)` — line 19960

### `assignExportsToNames(exports, exportsByName, exportNamesByVariable)` — line 19978

### `getExportMode(chunk, { exports: exportMode, name, format }, facadeModuleId, log)` — line 19990

### `guessIndentString(code)` — line 20017

### `getIndentString(modules, options)` — line 20037

### `getStaticDependencies(chunk, orderedModules, chunkByModule, externalChunkByModule)` — line 20048

### `addStaticDependencies(module, staticDependencies, handledModules, chunk, chunkByModule, externalChunkByModule)` — line 20067

### `getGlobalName(chunk, globals, hasExports, log)` — line 20106

### `getChunkNameFromModule(module)` — line 21221

### `getPredefinedChunkNameFromModule(module)` — line 21224

### `getImportedBindingsPerDependency(renderedDependencies, resolveFileName)` — line 21227

### `getChunkAssignments(entries, manualChunkAliasByEntry, minChunkSize, log, isManualChunksFunctionForm, onlyExplicitManualChunks)` — line 21370

### `getChunkDefinitionsFromManualChunks(manualChunkAliasByEntry, isManualChunksFunctionForm, onlyExplicitManualChunks)` — line 21389

### `addStaticDependenciesToManualChunk(entry, manualChunkModules, modulesInManualChunks)` — line 21417

### `analyzeModuleGraph(entries, manualChunkModules, manualChunkModulesByModule)` — line 21429

### `getDynamicEntries(allEntriesAndManualChunks, dynamicEntryModules, dynamicImportModulesByEntry, awaitedDynamicEntryModules, awaitedDynamicImportModulesByEntry)` — line 21499

### `getDynamicImportsByEntry(dynamicImportModulesByEntry, entryIndexByModule)` — line 21523

### `getDynamicallyDependentEntriesByDynamicEntry(dependentEntriesByModule, dynamicEntries, allEntriesAndManualChunks, getDynamicImporters)` — line 21535

### `getChunksWithSameDependentEntries(moduleWithDependentEntries)` — line 21557

### `getStaticDependencyAtomsByEntry(entriesAndManualChunksCount, chunkAtoms)` — line 21585

### `getAlreadyLoadedAtomsByEntry(staticDependencyAtomsByEntry, dynamicallyDependentEntriesByDynamicEntry, dynamicImportsByEntry, allEntriesCount)` — line 21600

### `removeUnnecessaryDependentEntries(chunkAtoms, alreadyLoadedAtomsByEntry, awaitedAlreadyLoadedAtomsByEntry)` — line 21633

### `getChunksWithSameDependentEntriesAndCorrelatedAtoms(chunkAtoms, staticDependencyAtomsByEntry, alreadyLoadedAtomsByEntry, minChunkSize)` — line 21648

### `addChunkDependenciesAndGetExternalSideEffectAtoms(chunks, chunkByModule, nextAvailableAtomMask)` — line 21703

### `getOptimizedChunks(chunks, minChunkSize, sideEffectAtoms, sizeByAtom, log)` — line 21810

### `getPartitionedChunks(chunks, minChunkSize)` — line 21827

### `compareChunkSize({ size: sizeA }, { size: sizeB })` — line 21843

### `mergeChunks(chunkPartition, minChunkSize, sideEffectAtoms, sizeByAtom)` — line 21846

### `findBestMergeTarget(mergedChunk, { big, small }, sideEffectAtoms, sizeByAtom, smallestAdditionalSize)` — line 21881

### `getAdditionalSizeAfterMerge(mergedChunk, targetChunk, // The maximum additional unused code size allowed to be added by the merge, // taking dependencies into account, needs to be below this number
currentAdditionalSize, sideEffectAtoms, sizeByAtom)` — line 21905

### `getAdditionalSizeIfNoTransitiveDependencyOrNonCorrelatedSideEffect(dependentChunk, dependencyChunk, currentAdditionalSize, sideEffectAtoms, sizeByAtom)` — line 21915

### `getAtomsSizeIfBelowLimit(atoms, currentAdditionalSize, sizeByAtom)` — line 21938

### `getChunksInPartition(chunk, minChunkSize, chunkPartition)` — line 21954

### `commondir(files)` — line 21959

### `sortByExecutionOrder(units)` — line 21977

### `analyseModuleExecution(entryModules)` — line 21987

### `getCyclePath(module, parent, parents)` — line 22041

### `getGenerateCodeSnippets({ compact, generatedCode: { arrowFunctions, constBindings, objectShorthand, reservedNamesAsProps } })` — line 22056

### `getLinkMap(log)` — line 22218

### `linkMap(source, map)` — line 22219

### `getCollapsedSourcemap(id, originalCode, originalSourcemap, sourcemapChain, linkMap)` — line 22230

### `collapseSourcemaps(file, map, modules, bundleSourcemapChain, excludeContent, log)` — line 22245

### `collapseSourcemap(id, originalCode, originalSourcemap, sourcemapChain, log)` — line 22269

### `renderChunks(chunks, bundle, pluginDriver, outputOptions, log)` — line 22283

### `reserveEntryChunksInBundle(chunks)` — line 22296

### `getChunkGraph(chunks)` — line 22304

### `transformChunk(magicString, fileName, usedModules, chunkGraph, options, outputPluginDriver, log)` — line 22310

### `transformChunksAndGenerateContentHashes(renderedChunks, chunkGraph, outputOptions, pluginDriver, getHash, log)` — line 22372

### `generateFinalHashes(renderedChunksByPlaceholder, hashDependenciesByPlaceholder, initialHashesByPlaceholder, placeholders, bundle, getHash)` — line 22426

### `addChunksToBundle(renderedChunksByPlaceholder, hashesByPlaceholder, bundle, nonHashedChunksWithPlaceholders, pluginDriver, options)` — line 22455

### `emitSourceMapAndGetComment(fileName, map, pluginDriver, { sourcemap, sourcemapBaseUrl })` — line 22487

### `calculateDebugIdAndGetComment(code, map)` — line 22506

### `validateOptionsForMultiChunkOutput(outputOptions, log)` — line 22685

### `getIncludedModules(modulesById)` — line 22695

### `getAbsoluteEntryModulePaths(includedModules, preserveModules)` — line 22705

### `getExternalChunkByModule(modulesById, outputOptions, inputBase)` — line 22714

### `addModuleToManualChunk(alias, module, manualChunkAliasByEntry)` — line 22723

### `isFileNameOutsideOutputDirectory(fileName)` — line 22730

### `validateOutputBundleFileNames(bundle)` — line 22739

### `flru(max)` — line 22753

### `keep(key, value)` — line 22757

### `reset(isPartial)` — line 22766

### `resolveIdViaPlugins(source, importer, pluginDriver, moduleLoaderResolveId, skip, customOptions, isEntry, attributes, importerAttributes)` — line 22813

### `resolveId(source, importer, preserveSymlinks, pluginDriver, moduleLoaderResolveId, skip, customOptions, isEntry, attributes, importerAttributes, fs)` — line 22844

### `addJsExtensionIfNecessary(file, preserveSymlinks, fs)` — line 22872

### `findFile(file, preserveSymlinks, fs)` — line 22877

### `stripBom(content)` — line 22895

### `transform(source, module, pluginDriver, options)` — line 22902

### `transformReducer(previousCode, result, plugin)` — line 22914

### `normalizeRelativeExternalId(source, importer)` — line 23463

### `addChunkNamesToModule(module, { fileName, name }, isUserDefined)` — line 23470

### `isNotAbsoluteExternal(id, source, makeAbsoluteExternalsRelative)` — line 23479

### `waitForDependencyResolution(loadPromise)` — line 23484

### `normalizeEntryModules(entryModules)` — line 23520

### `formatAction([pluginName, hookName, parameters])` — line 23712

### `catchUnfinishedHookActions(pluginDriver, callback)` — line 23736

### `initWasm()` — line 23764

### `normalizeInputOptions(config, watchMode)` — line 23770

### `sanitizeFileName(name)` — line 23939

### `normalizeOutputOptions(config, inputOptions, unsetInputOptions)` — line 23947

### `getExports(config, unsetOptions)` — line 24137

### `rollup(rawInputOptions)` — line 24229

### `rollupInternal(rawInputOptions, watcher)` — line 24232

### `getInputOptions(initialInputOptions, watchMode)` — line 24312

### `getProcessedInputOptions(inputOptions, watchMode)` — line 24321

### `normalizePlugins(plugins, anonymousPrefix)` — line 24341

### `handleGenerateWrite(isWrite, inputOptions, unsetInputOptions, rawOutputOptions, graph)` — line 24348

### `getOutputOptionsAndPluginDriver(rawOutputOptions, inputPluginDriver, inputOptions, unsetInputOptions)` — line 24365

### `getOutputOptions(inputOptions, unsetInputOptions, rawOutputOptions, outputPluginDriver)` — line 24377

### `createOutput(outputBundle)` — line 24387

### `getSortingFileType(file)` — line 24398

### `writeOutputFile(outputFile, outputOptions, { fs: { mkdir, writeFile } })` — line 24407

### `defineConfig(options)` — line 24419

### `getHashPlaceholderGenerator()` — line 122

### `replacePlaceholders(code, hashesByPlaceholder)` — line 136

### `replaceSinglePlaceholder(code, placeholder, value)` — line 137

### `replacePlaceholdersWithDefaultAndGetContainedPlaceholders(code, placeholders)` — line 138

### `getOutputBundle(outputBundleBase)` — line 154

### `removeUnreferencedAssets(outputBundle)` — line 177

### `doNothing()` — line 623

### `getOnLog(config, logLevel, printLog = defaultPrintLog)` — line 632

### `getDefaultOnLog(printLog, onwarn)` — line 648

### `addLogToString(log)` — line 658

### `normalizeLog(log)` — line 665

### `defaultPrintLog(level, { message })` — line 670

### `objectifyOption(value)` — line 761

### `objectifyOptionWithPresets(presets, optionName, urlSnippet, additionalValues)` — line 762

### `getOptionWithPreset(value, presets, optionName, urlSnippet, additionalValues)` — line 772

### `normalizePluginOption(plugins)` — line 785

### `scan(input, options)` — line 1308

### `eos()` — line 1336

### `peek()` — line 1337

### `advance()` — line 1338

### `expandRange(args, options)` — line 1680

### `syntaxError(type, char)` — line 1695

### `parseRepeatedExtglob(pattern, requireEnd = true)` — line 1815

### `analyzeRepeatedExtglob(body, options)` — line 1934

### `parse(input, options)` — line 1977

### `remaining()` — line 2067

### `consume(value = '', num = 0)` — line 2068

### `negate()` — line 2078

### `extglobOpen(type, value)` — line 2144

### `isIgnored()` — line 3104

### `matcher(input, returnObject = false)` — line 3110

### `logger(level, log, skipped = parseAst_js.EMPTY_SET)` — line 3807

### `getOption(name)` — line 3909

### `getExternal(config, overrides)` — line 3937

### `getObjectOption(config, overrides, name, objectifyValue = objectifyOption)` — line 3943

### `getWatch(config, overrides)` — line 3951

### `isWatchEnabled(optionValue)` — line 3952

### `normalizeObjectOptionValue(optionValue, objectifyValue)` — line 3958

### `formatter(open, close, replace = open)` — line 4039

### `replaceClose(string, close, replace, index)` — line 4045

### `createColors(enabled = isColorSupported)` — line 4055

### `stderr(...parameters)` — line 4120

### `replacer(match)` — line 4971

### `isAnyWellKnown(v)` — line 6129

### `isConcreteKey(v)` — line 6132

### `deoptimizeInteraction(interaction)` — line 6432

### `includeInteraction(interaction, context)` — line 6437

### `includeInteractionWithoutThis({ args }, context)` — line 6443

### `getPureFunctions({ treeshake })` — line 6474

### `startsWithDigit(value)` — line 6672

### `needsEscape(value)` — line 6673

### `isInteger(property)` — line 7860

### `getUnknownValue()` — line 8133

### `returnFalse()` — line 8134

### `returnTrue()` — line 8135

### `getWellKnownSymbol(symbol)` — line 8136

### `limitConcatenatedPathDepth(path1, path2)` — line 9172

### `parameters(this.params = params.map((parameter)` — line 10906

### `isDefaultAProperty(interopType, externalLiveBindings)` — line 11165

### `canDefaultBeTakenFromNamespace(interopType, externalLiveBindings)` — line 11174

### `getHelpersBlock(additionalHelpers, accessedGlobals, indent, snippets, liveBindings, freeze, symbols)` — line 11175

### `getDefaultLiveBinding({ _, getObject })` — line 11275

### `getDefaultStatic({ _, getPropertyAccess })` — line 11276

### `getIsCompatNamespace({ _ })` — line 11277

### `createNamespaceObject(t, index, snippets, liveBindings, freeze, symbols)` — line 11278

### `loopOverKeys(body, allowVariableLoopVariable, { _, cnst, getFunctionIntro, s })` — line 11290

### `loopOverNamespaces(body, useForEach, t, { _, cnst, getDirectReturnFunction, getFunctionIntro, n })` — line 11296

### `copyNonDefaultOwnPropertyLiveBinding(t, index, snippets)` — line 11313

### `copyOwnPropertyLiveBinding(t, index, { _, cnst, getDirectReturnFunction, n })` — line 11319

### `copyPropertyLiveBinding(t, index, { _, cnst, getDirectReturnFunction, n })` — line 11331

### `copyPropertyStatic(_t, index, { _, n })` — line 11345

### `getFrozen(freeze, fragment)` — line 11346

### `getWithToStringTag(symbols, fragment, { _, getObject })` — line 11347

### `getResolveUrl(path, asObject, URL = 'URL')` — line 11951

### `getRelativeUrlFromDocument(relativePath, asObject, umd = false)` — line 11952

### `getGenericImportMetaMechanism(getUrl)` — line 11953

### `getFileUrlFromFullPath(path, asObject)` — line 11961

### `getFileUrlFromRelativePath(path, asObject)` — line 11962

### `getUrlFromDocument(chunkId, umd = false)` — line 11963

### `getDynamicNamespaceVariable(namespace)` — line 12224

### `getDefineProperty(name, needsLiveBinding, t, { _, getDirectReturnFunction, n })` — line 12477

### `addInteropStatement(helperVariableName, helper, dependencyVariableName)` — line 12495

### `keypath(keypath, getPropertyAccess)` — line 12888

### `getStarExcludes({ dependencies, exports })` — line 13113

### `getStarExcludesBlock(starExcludes, t, { _, cnst, getObject, n })` — line 13126

### `getImportBindingsBlock(importBindings, t, { _, n })` — line 13139

### `getHoistedExportsBlock(exports, t, snippets)` — line 13140

### `getSyntheticExportsBlock(exports, t, snippets)` — line 13154

### `getMissingExportsBlock(exports, t, snippets)` — line 13157

### `getMatcher(id)` — line 13339

### `getIncludedPatternPath(destructuredInitPath)` — line 13469

### `getPropertyKey(property)` — line 18671

### `addSideEffectDependencies(possibleDependencies)` — line 19607

### `copyNameToModulesMap(searchedNamesAndModules)` — line 19799

### `sortExportedVariables([a], [b])` — line 19801

### `concatSeparator(out, next)` — line 19803

### `concatDblSeparator(out, next)` — line 19804

### `finalize(code)` — line 20240

### `resolveFileName(dependency)` — line 21246

### `compareExecIndex(unitA, unitB)` — line 21976

### `handleSyncLoadedModule(module, parent)` — line 21994

### `analyseModule(module)` — line 22004

### `getNonArrowFunctionIntro(parameters, { isAsync, name })` — line 22059

### `getDirectReturnFunction(parameters, { functionReturn, lineBreakIndent, name })` — line 22067

### `wrapIfNeeded(code, needsParens)` — line 22115

### `handleDependency(chunk, parent)` — line 22650

### `analyseChunk(chunk)` — line 22670

### `useCustomTransformCache()` — line 22911

### `handleChange(...parameters)` — line 23576

### `handleClose()` — line 23577

### `getCache(config)` — line 23805

### `getIdMatcher(option)` — line 23808

### `getInput(config)` — line 23830

### `getJsx(config)` — line 23834

### `getMaxParallelFileOps(config)` — line 23874

### `getModuleContext(config, context)` — line 23883

### `getTreeshake(config)` — line 23897

### `getHasModuleSideEffects(moduleSideEffectsOption)` — line 23915

### `getFile(config, preserveModules, inputOptions)` — line 24023

### `getFormat(config)` — line 24034

### `getInlineDynamicImports(config, inputOptions)` — line 24061

### `getPreserveModules(config, inlineDynamicImports, inputOptions)` — line 24069

### `getPreserveModulesRoot(config)` — line 24081

### `getAmd(config)` — line 24088

### `getAddon(config, name)` — line 24116

### `getDir(config, file)` — line 24123

### `getEntryFileNames(config, unsetOptions)` — line 24130

### `getExternalImportAttributes(config, inputOptions)` — line 24147

### `getGeneratedCode(config)` — line 24153

### `getIndent(config, compact)` — line 24163

### `getInterop(config)` — line 24177

### `validateInterop(interop)` — line 24190

### `getManualChunks(config, inlineDynamicImports, preserveModules)` — line 24196

### `getMinifyInternalExports(config, format, compact)` — line 24208

### `getSourcemapFileNames(config, unsetOptions)` — line 24209

### `getSourcemapBaseUrl(config)` — line 24216

### `emitError()` — line 24379

### `handler(...parameters)` — line 18774
