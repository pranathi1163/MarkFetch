# `parseAst.js`

> **Path:** `frontend/node_modules/rollup/dist/shared/parseAst.js`  
> **Last documented:** 2026-06-29 18:03

---

## Functions

### `rangeContains(range, index)` — line 21

### `getLocator(source, options = {})` — line 29

### `locator(search, index)` — line 50

### `locate(source, search, options)` — line 84

### `spaces(index)` — line 88

### `tabsToSpaces(value)` — line 94

### `getCodeFrame(source, line, column)` — line 100

### `isAbsolute(path)` — line 149

### `isRelative(path)` — line 152

### `normalize(path)` — line 156

### `printQuotedStringList(list, verbs)` — line 160

### `relative(from, to)` — line 173

### `getAliasName(id)` — line 194

### `relativeId(id)` — line 198

### `isPathFragment(name)` — line 203

### `getImportPath(importerId, targetPath, stripJsExtension, ensureFileName)` — line 208

### `isValidUrl(url)` — line 227

### `getRollupUrl(snippet)` — line 236

### `addTrailingSlashIfMissed(url)` — line 239

### `error(base)` — line 284

### `getRollupError(base)` — line 287

### `augmentCodeLocation(properties, pos, source, id)` — line 296

### `augmentLogMessage(log)` — line 316

### `logAddonNotGenerated(message, hook, plugin)` — line 338

### `logAlreadyClosed()` — line 345

### `logAmbiguousExternalNamespaces(binding, reexportingModule, usedModule, sources)` — line 351

### `logAnonymousPluginCache()` — line 360

### `logAssetNotFinalisedForFileName(name)` — line 366

### `logAssetReferenceIdNotFoundForSetSource(assetReferenceId)` — line 372

### `logAssetSourceAlreadySet(name)` — line 378

### `logNoAssetSourceSet(assetName)` — line 384

### `logBadLoader(id)` — line 390

### `logCannotCallNamespace(name)` — line 396

### `logCannotEmitFromOptionsHook()` — line 402

### `logChunkNotGeneratedForFileName(name)` — line 408

### `logChunkInvalid({ fileName, code }, { pos, message })` — line 414

### `logCircularDependency(cyclePath)` — line 422

### `logCircularChunk(cyclePath, isManualChunkConflict)` — line 429

### `logCircularReexport(exportName, exporter)` — line 438

### `logCyclicCrossChunkReexport(exportName, exporter, reexporter, importer, preserveModules)` — line 445

### `logDeprecation(deprecation, urlSnippet, plugin)` — line 454

### `logConstVariableReassignError()` — line 462

### `logDuplicateArgumentNameError(name)` — line 468

### `logDuplicateExportError(name)` — line 474

### `logDuplicateImportOptions()` — line 477

### `logDuplicatePluginName(plugin)` — line 483

### `logEmptyChunk(chunkName)` — line 489

### `logEval(id)` — line 496

### `logExternalSyntheticExports(id, importer)` — line 504

### `logFailAfterWarnings()` — line 511

### `logFileNameConflict(fileName)` — line 517

### `logFileNameOutsideOutputDirectory(fileName)` — line 523

### `logFileReferenceIdNotFoundForFilename(assetReferenceId)` — line 529

### `logFirstSideEffect(source, id, { line, column })` — line 535

### `logIllegalIdentifierAsName(name)` — line 541

### `logIllegalImportReassignment(name, importingId)` — line 548

### `logInconsistentImportAttributes(existingAttributes, newAttributes, source, importer)` — line 554

### `logInvalidAnnotation(comment, id, type)` — line 566

### `logInputHookInOutputPlugin(pluginName, hookName)` — line 574

### `logCannotAssignModuleToChunk(moduleId, assignToAlias, currentAlias)` — line 580

### `tweakStackMessage(error, oldMessage)` — line 586

### `logCannotBundleConfigAsEsm(originalError)` — line 593

### `logCannotLoadConfigAsCjs(originalError)` — line 602

### `logCannotLoadConfigAsEsm(originalError)` — line 611

### `logInvalidExportOptionValue(optionValue)` — line 620

### `logIncompatibleExportOptionValue(optionValue, keys, entryModule)` — line 627

### `logInternalIdCannotBeExternal(source, importer)` — line 634

### `logImportOptionsAreInvalid(importer)` — line 640

### `logImportAttributeIsInvalid(importer)` — line 646

### `logInvalidLogPosition(plugin)` — line 652

### `logInvalidOption(option, urlSnippet, explanation, value)` — line 658

### `logInvalidAddonPluginHook(hook, plugin)` — line 665

### `logInvalidFunctionPluginHook(hook, plugin)` — line 673

### `logInvalidRollupPhaseForChunkEmission()` — line 681

### `logInvalidSetAssetSourceCall()` — line 687

### `logInvalidFormatForTopLevelAwait(id, format)` — line 693

### `logMissingConfig()` — line 700

### `logMissingEntryExport(binding, exporter)` — line 707

### `logMissingExport(binding, importingModule, exporter, missingButExportExists)` — line 716

### `logMissingExternalConfig(file)` — line 736

### `logMissingGlobalName(externalId, guess)` — line 742

### `logImplicitDependantCannotBeExternal(unresolvedId, implicitlyLoadedBefore)` — line 751

### `logUnresolvedImplicitDependant(unresolvedId, implicitlyLoadedBefore)` — line 757

### `logImplicitDependantIsNotIncluded(module)` — line 763

### `logMissingJsxExport(name, exporter, importer)` — line 772

### `logMissingNameOptionForIifeExport()` — line 782

### `logMissingNameOptionForUmdExport()` — line 789

### `logMissingNodeBuiltins(externalBuiltins)` — line 796

### `logMissingFileOrDirOption()` — line 803

### `logMixedExport(facadeModuleId, name)` — line 810

### `logModuleLevelDirective(directive, id)` — line 818

### `logNamespaceConflict(binding, reexportingModuleId, sources)` — line 825

### `logNonExternalSourcePhaseImport(source, importer)` — line 834

### `logNoTransformMapOrAstWithoutCode(pluginName)` — line 841

### `logOnlyInlineSourcemapsForStdout()` — line 848

### `logOptimizeChunkStatus(chunks, smallChunks, pointInTime)` — line 854

### `logParseError(message, pos)` — line 862

### `logRedeclarationError(name)` — line 865

### `logReservedNamespace(namespace)` — line 871

### `logModuleParseError(error, moduleId)` — line 877

### `logPluginError(error, plugin, { hook, id } = {})` — line 893

### `logShimmedExport(id, binding)` — line 910

### `logSourcePhaseFormatUnsupported(outputFormat, chunkId, dependencyId)` — line 918

### `logSourcemapBroken(plugin)` — line 925

### `logConflictingSourcemapSources(filename)` — line 933

### `logInvalidSourcemapForError(error, id, column, line, pos)` — line 939

### `logSyntheticNamedExportsNeedNamespaceExport(id, syntheticNamedExportsOption)` — line 953

### `logThisIsUndefined()` — line 962

### `logUnexpectedNamedImport(id, imported, isReexport)` — line 969

### `logUnexpectedNamespaceReexport(id)` — line 978

### `logUnknownOption(optionType, unknownOptions, validOptions)` — line 986

### `logEntryCannotBeExternal(unresolvedId)` — line 992

### `logExternalModulesCannotBeIncludedInManualChunks(source)` — line 998

### `logExternalModulesCannotBeTransformedToModules(source)` — line 1004

### `logUnresolvedEntry(unresolvedId)` — line 1010

### `logUnresolvedImport(source, importer)` — line 1016

### `logUnresolvedImportTreatedAsExternal(source, importer)` — line 1024

### `logUnusedExternalImports(externalId, names, importers)` — line 1033

### `logFailedValidation(message)` — line 1045

### `warnDeprecation(deprecation, urlSnippet, activeDeprecation, options, plugin)` — line 1051

### `warnDeprecationWithOptions(deprecation, urlSnippet, activeDeprecation, log, strictDeprecations, plugin)` — line 1054

### `convertProgram(buffer)` — line 1188

### `panicError(position, buffer)` — line 1204

### `parseError(position, buffer)` — line 1212

### `arrayExpression(position, buffer)` — line 1220

### `arrayPattern(position, buffer)` — line 1228

### `arrowFunctionExpression(position, buffer)` — line 1236

### `assignmentExpression(position, buffer)` — line 1252

### `assignmentPattern(position, buffer)` — line 1262

### `awaitExpression(position, buffer)` — line 1271

### `binaryExpression(position, buffer)` — line 1279

### `blockStatement(position, buffer)` — line 1289

### `breakStatement(position, buffer)` — line 1297

### `callExpression(position, buffer)` — line 1306

### `catchClause(position, buffer)` — line 1319

### `chainExpression(position, buffer)` — line 1329

### `classBody(position, buffer)` — line 1337

### `classDeclaration(position, buffer)` — line 1345

### `classExpression(position, buffer)` — line 1358

### `conditionalExpression(position, buffer)` — line 1371

### `continueStatement(position, buffer)` — line 1381

### `debuggerStatement(position, buffer)` — line 1390

### `decorator(position, buffer)` — line 1397

### `directive(position, buffer)` — line 1405

### `doWhileStatement(position, buffer)` — line 1414

### `emptyStatement(position, buffer)` — line 1423

### `exportAllDeclaration(position, buffer)` — line 1430

### `exportDefaultDeclaration(position, buffer)` — line 1441

### `exportNamedDeclaration(position, buffer)` — line 1449

### `exportSpecifier(position, buffer)` — line 1462

### `expressionStatement(position, buffer)` — line 1473

### `forInStatement(position, buffer)` — line 1481

### `forOfStatement(position, buffer)` — line 1491

### `forStatement(position, buffer)` — line 1503

### `functionDeclaration(position, buffer)` — line 1517

### `functionExpression(position, buffer)` — line 1534

### `identifier(position, buffer)` — line 1551

### `ifStatement(position, buffer)` — line 1559

### `importAttribute(position, buffer)` — line 1570

### `importDeclaration(position, buffer)` — line 1579

### `importDefaultSpecifier(position, buffer)` — line 1591

### `importExpression(position, buffer)` — line 1599

### `importNamespaceSpecifier(position, buffer)` — line 1611

### `importSpecifier(position, buffer)` — line 1619

### `jsxAttribute(position, buffer)` — line 1630

### `jsxClosingElement(position, buffer)` — line 1640

### `jsxClosingFragment(position, buffer)` — line 1648

### `jsxElement(position, buffer)` — line 1655

### `jsxEmptyExpression(position, buffer)` — line 1666

### `jsxExpressionContainer(position, buffer)` — line 1673

### `jsxFragment(position, buffer)` — line 1681

### `jsxIdentifier(position, buffer)` — line 1691

### `jsxMemberExpression(position, buffer)` — line 1699

### `jsxNamespacedName(position, buffer)` — line 1708

### `jsxOpeningElement(position, buffer)` — line 1717

### `jsxOpeningFragment(position, buffer)` — line 1728

### `jsxSpreadAttribute(position, buffer)` — line 1737

### `jsxSpreadChild(position, buffer)` — line 1745

### `jsxText(position, buffer)` — line 1753

### `labeledStatement(position, buffer)` — line 1762

### `literalBigInt(position, buffer)` — line 1771

### `literalBoolean(position, buffer)` — line 1782

### `literalNull(position, buffer)` — line 1793

### `literalNumber(position, buffer)` — line 1802

### `literalRegExp(position, buffer)` — line 1812

### `literalString(position, buffer)` — line 1824

### `logicalExpression(position, buffer)` — line 1834

### `memberExpression(position, buffer)` — line 1844

### `metaProperty(position, buffer)` — line 1856

### `methodDefinition(position, buffer)` — line 1865

### `newExpression(position, buffer)` — line 1879

### `objectExpression(position, buffer)` — line 1890

### `objectPattern(position, buffer)` — line 1898

### `privateIdentifier(position, buffer)` — line 1906

### `program(position, buffer)` — line 1914

### `property(position, buffer)` — line 1925

### `propertyDefinition(position, buffer)` — line 1941

### `restElement(position, buffer)` — line 1955

### `returnStatement(position, buffer)` — line 1963

### `sequenceExpression(position, buffer)` — line 1972

### `spreadElement(position, buffer)` — line 1980

### `staticBlock(position, buffer)` — line 1988

### `superElement(position, buffer)` — line 1996

### `switchCase(position, buffer)` — line 2003

### `switchStatement(position, buffer)` — line 2013

### `taggedTemplateExpression(position, buffer)` — line 2022

### `templateElement(position, buffer)` — line 2031

### `templateLiteral(position, buffer)` — line 2044

### `thisExpression(position, buffer)` — line 2053

### `throwStatement(position, buffer)` — line 2060

### `tryStatement(position, buffer)` — line 2068

### `unaryExpression(position, buffer)` — line 2080

### `updateExpression(position, buffer)` — line 2090

### `variableDeclaration(position, buffer)` — line 2101

### `variableDeclarator(position, buffer)` — line 2110

### `whileStatement(position, buffer)` — line 2120

### `yieldExpression(position, buffer)` — line 2129

### `convertNode(position, buffer)` — line 2141

### `convertNodeList(position, buffer)` — line 2151

### `getAstBuffer(astBuffer)` — line 2163

### `formatAttributes(attributes)` — line 560

### `convertAnnotations(position, buffer)` — line 1169

### `convertAnnotation(position, buffer)` — line 1179

### `parseAst(input, { allowReturnOutsideFunction = false, jsx = false } = {})` — line 2184

### `parseAstAsync(input, { allowReturnOutsideFunction = false, jsx = false, signal } = {})` — line 2185
