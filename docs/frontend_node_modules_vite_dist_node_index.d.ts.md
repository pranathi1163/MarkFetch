# `index.d.ts`

> **Path:** `frontend/node_modules/vite/dist/node/index.d.ts`  
> **Last documented:** 2026-06-29 18:03

---

## Dependencies / Imports

- `../../types/customEvent.js`
- `../../types/hmrPayload.js`
- `../../types/internal/cssPreprocessorOptions.js`
- `../../types/internal/lightningcssOptions.js`
- `./moduleRunnerTransport.d-DJ_mE5sf.js`
- `esbuild`
- `foo`
- `node:events`
- `node:fs`
- `node:http`
- `node:http2`
- `node:https`
- `node:net`
- `node:stream`
- `node:tls`
- `node:url`
- `node:zlib`
- `postcss`
- `rollup`
- `vite/module-runner`

## Functions

### `createLogger(level?: LogLevel, options?: LoggerOptions)` — line 660

### `preview(inlineConfig?: InlineConfig)` — line 807

### `optimizeDeps(config: ResolvedConfig, force?: boolean | undefined, asCommand?: boolean)` — line 1031

### `createServerHotChannel()` — line 1304

### `createWebSocketStream(websocket: WebSocket, options?: DuplexOptions)` — line 1846

### `build(inlineConfig?: InlineConfig)` — line 2759

### `createBuilder(inlineConfig?: InlineConfig, useLegacyBuilder?: null | boolean)` — line 2804

### `createServer(inlineConfig?: InlineConfig | ResolvedConfig)` — line 3159

### `perEnvironmentPlugin(name: string, applyToEnvironment: (environment: PartialEnvironment)` — line 3446

### `preprocessCSS(code: string, filename: string, config: ResolvedConfig)` — line 3521

### `formatPostcssSourceMap(rawMap: ExistingRawSourceMap, file: string)` — line 3522

### `transformWithEsbuild(code: string, filename: string, options?: esbuild_TransformOptions, inMap?: object, config?: ResolvedConfig, watcher?: FSWatcher)` — line 3554

### `defineConfig(config: UserConfig)` — line 3642

### `resolveConfig(inlineConfig: InlineConfig, command: 'build' | 'serve', defaultMode?: string, defaultNodeEnv?: string, isPreview?: boolean)` — line 4048

### `sortUserPlugins(plugins: (Plugin | Plugin[])` — line 4051

### `loadConfigFromFile(configEnv: ConfigEnv, configFile?: string, configRoot?: string, logLevel?: LogLevel, customLogger?: Logger, configLoader?: 'bundle' | 'runner' | 'native')` — line 4052

### `createIdResolver(config: ResolvedConfig, options?: Partial<InternalResolveOptions>)` — line 4063

### `buildErrorMessage(err: RollupError, args?: string[], includeStack?: boolean)` — line 4065

### `createServerModuleRunner(environment: DevEnvironment, options?: ServerModuleRunnerOptions)` — line 4089

### `createRunnableDevEnvironment(name: string, config: ResolvedConfig, context?: RunnableDevEnvironmentContext)` — line 4091

### `isRunnableDevEnvironment(environment: Environment)` — line 4097

### `createFetchableDevEnvironment(name: string, config: ResolvedConfig, context: FetchableDevEnvironmentContext)` — line 4110

### `isFetchableDevEnvironment(environment: Environment)` — line 4111

### `fetchModule(environment: DevEnvironment, url: string, importer?: string, options?: FetchModuleOptions)` — line 4137

### `ssrTransform(code: string, inMap: SourceMap | {
    mappings: '';
} | null, url: string, originalCode: string, options?: ModuleRunnerTransformOptions)` — line 4144

### `splitVendorChunk(options?: {
    cache?: SplitVendorChunkCache;
})` — line 4167

### `splitVendorChunkPlugin()` — line 4173

### `normalizePath(id: string)` — line 4183

### `mergeAlias(a?: AliasOptions, b?: AliasOptions)` — line 4185

### `send(req: IncomingMessage, res: ServerResponse, content: string | Buffer, type: string, options: SendOptions)` — line 4195

### `searchForWorkspaceRoot(current: string, root?: string)` — line 4200

### `isFileServingAllowed(config: ResolvedConfig, url: string)` — line 4206

### `isFileLoadingAllowed(config: ResolvedConfig, filePath: string)` — line 4208

### `loadEnv(mode: string, envDir: string | false, prefixes?: string | string[])` — line 4210

### `resolveEnvPrefix({ envPrefix, }: UserConfig)` — line 4211
