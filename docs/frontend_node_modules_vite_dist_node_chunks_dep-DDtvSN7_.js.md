# `dep-DDtvSN7_.js`

> **Path:** `frontend/node_modules/vite/dist/node/chunks/dep-DDtvSN7_.js`  
> **Last documented:** 2026-06-29 18:03

---

## Dependencies / Imports

- `./dep-3RmXg9uo.js`
- `./dep-Dm0c1Wj2.js`
- `crypto`
- `fs`
- `path`
- `postcss`
- `util`

## Functions

### `_mergeNamespaces(n, m)` — line 9

### `setFileSystem(fs)` — line 39

### `getFileSystem()` — line 44

### `unquote(str)` — line 59

### `addImports(node, path)` — line 143

### `addExports(node)` — line 155

### `saveJSON(cssFile, json)` — line 371

### `arrayReduce(array, iteratee, accumulator, initAccum)` — line 550

### `asciiToArray(string)` — line 566

### `asciiWords(string)` — line 577

### `basePropertyOf(object)` — line 588

### `hasUnicode(string)` — line 611

### `hasUnicodeWord(string)` — line 622

### `stringToArray(string)` — line 633

### `unicodeToArray(string)` — line 646

### `unicodeWords(string)` — line 657

### `baseSlice(array, start, end)` — line 687

### `baseToString(value)` — line 716

### `castSlice(array, start, end)` — line 737

### `createCaseFirst(methodName)` — line 750

### `createCompounder(callback)` — line 777

### `isObjectLike(value)` — line 807

### `isSymbol(value)` — line 828

### `toString(value)` — line 854

### `capitalize(string)` — line 898

### `deburr(string)` — line 920

### `words(string, pattern, guard)` — line 963

### `dashesCamelCase(string)` — line 984

### `makeLocalsConventionReducer(localsConvention, inputFile)` — line 988

### `createError(node, graph)` — line 1169

### `walkGraph(node, graph, state, result, strict)` — line 1182

### `addImportToGraph(importId, parentId, graph, visited)` — line 1261

### `requireWasmHash()` — line 1454

### `requireXxhash64()` — line 1670

### `requireBatchedHash()` — line 1693

### `requireMd4()` — line 1771

### `requireBulkUpdateDecorator()` — line 1794

### `divmod32(uint32Array, divisor)` — line 1923

### `encodeBufferToBase(buffer, base, length)` — line 1933

### `createGenerator(pattern, options)` — line 2168

### `generate(localName, filepath)` — line 2182

### `gobbleHex(str)` — line 2234

### `unesc(str)` — line 2263

### `getProp(obj)` — line 2308

### `ensureObject(obj)` — line 2332

### `stripComments(str)` — line 2355

### `_defineProperties(target, props)` — line 2393

### `_createClass(Constructor, protoProps, staticProps)` — line 2394

### `cloneNode(obj, parent)` — line 2395

### `Node(opts)` — line 2421

### `remove()` — line 2431

### `replaceWith()` — line 2438

### `next()` — line 2447

### `prev()` — line 2450

### `clone(overrides)` — line 2453

### `appendToPropertyAndEscape(name, value, valueEscaped)` — line 2473

### `setPropertyAndEscape(name, value, valueEscaped)` — line 2495

### `setPropertyWithoutEscape(name, value)` — line 2510

### `isAtPosition(line, column)` — line 2522

### `stringifyProperty(name)` — line 2540

### `valueToString()` — line 2543

### `get()` — line 2551

### `set(raw)` — line 2558

### `_getRequireWildcardCache(nodeInterop)` — line 2619

### `_interopRequireWildcard(obj, nodeInterop)` — line 2620

### `_interopRequireDefault(obj)` — line 2621

### `_createForOfIteratorHelperLoose(o, allowArrayLike)` — line 2622

### `_unsupportedIterableToArray(o, minLen)` — line 2623

### `_arrayLikeToArray(arr, len)` — line 2624

### `_inheritsLoose(subClass, superClass)` — line 2627

### `_setPrototypeOf(o, p)` — line 2628

### `Container(opts)` — line 2631

### `append(selector)` — line 2640

### `prepend(selector)` — line 2645

### `at(index)` — line 2653

### `index(child)` — line 2656

### `removeChild(child)` — line 2662

### `removeAll()` — line 2675

### `empty()` — line 2683

### `insertAfter(oldNode, newNode)` — line 2686

### `insertBefore(oldNode, newNode)` — line 2705

### `_findChildAtPosition(line, col)` — line 2724

### `atPosition(line, col)` — line 2754

### `_inferEndPosition()` — line 2761

### `each(callback)` — line 2768

### `walk(callback)` — line 2795

### `walkAttributes(callback)` — line 2806

### `walkClasses(callback)` — line 2814

### `walkCombinators(callback)` — line 2822

### `walkComments(callback)` — line 2830

### `walkIds(callback)` — line 2838

### `walkNesting(callback)` — line 2846

### `walkPseudos(callback)` — line 2854

### `walkTags(callback)` — line 2862

### `walkUniversals(callback)` — line 2870

### `split(callback)` — line 2878

### `map(callback)` — line 2893

### `reduce(callback, memo)` — line 2896

### `every(callback)` — line 2899

### `some(callback)` — line 2902

### `filter(callback)` — line 2905

### `sort(callback)` — line 2908

### `Root(opts)` — line 2951

### `error(message, options)` — line 2965

### `Selector(opts)` — line 2999

### `merge(options, defaults)` — line 3019

### `cssesc(string, options)` — line 3037

### `ClassName(opts)` — line 3139

### `Comment(opts)` — line 3191

### `ID(opts)` — line 3218

### `Namespace()` — line 3254

### `qualifiedName(value)` — line 3258

### `Tag(opts)` — line 3334

### `String(opts)` — line 3361

### `Pseudo(opts)` — line 3388

### `unescapeValue(value)` — line 3435

### `handleDeprecatedContructorOpts(opts)` — line 3454

### `Attribute(opts)` — line 3477

### `getQuotedValue(options)` — line 3519

### `_determineQuoteMark(options)` — line 3528

### `setValue(value, options)` — line 3537

### `smartQuoteMark(options)` — line 3558

### `preferredQuoteMark(options)` — line 3595

### `_syncRawValue()` — line 3605

### `_handleEscapes(prop, value)` — line 3615

### `_spacesFor(name)` — line 3627

### `_stringFor(name, spaceName, concat)` — line 3636

### `offsetOf(name)` — line 3661

### `defaultAttrConcat(attrValue, attrSpaces)` — line 3860

### `Universal(opts)` — line 3878

### `Combinator(opts)` — line 3906

### `Nesting(opts)` — line 3933

### `sortAscending(list)` — line 3954

### `consumeWord(css, start)` — line 4059

### `consumeEscape(css, start)` — line 4081

### `tokenize(input)` — line 4112

### `unclosed(what, fix)` — line 4122

### `tokenStart(token)` — line 4305

### `tokenEnd(token)` — line 4311

### `getSource(startLine, startColumn, endLine, endColumn)` — line 4317

### `getTokenSource(token)` — line 4329

### `getTokenSourceSpan(startToken, endToken)` — line 4332

### `unescapeProp(node, prop)` — line 4338

### `indexesOf(array, item)` — line 4352

### `uniqs()` — line 4360

### `Parser(rule, options)` — line 4367

### `_errorGenerator()` — line 4402

### `attribute()` — line 4411

### `parseWhitespaceEquivalentTokens(stopPosition)` — line 4656

### `convertWhitespaceNodesToSpace(nodes, requiredSpace)` — line 4708

### `isNamedCombinator(position)` — line 4730

### `namedCombinator()` — line 4736

### `combinator()` — line 4756

### `comma()` — line 4844

### `comment()` — line 4861

### `missingBackslash()` — line 4873

### `missingParenthesis()` — line 4878

### `missingSquareBracket()` — line 4881

### `unexpected()` — line 4884

### `unexpectedPipe()` — line 4887

### `namespace()` — line 4890

### `nesting()` — line 4901

### `parentheses()` — line 4917

### `pseudo()` — line 4978

### `space()` — line 5007

### `string()` — line 5022

### `universal(namespace)` — line 5031

### `splitWord(namespace, firstCallback)` — line 5045

### `word(namespace)` — line 5119

### `loop()` — line 5127

### `parse(throwOnParenthesis)` — line 5134

### `expected(description, index, found)` — line 5191

### `requiredSpace(space)` — line 5206

### `optionalSpace(space)` — line 5209

### `lossySpace(space, required)` — line 5212

### `parseParenthesisToken(token)` — line 5219

### `newNode(node, namespace)` — line 5227

### `content(token)` — line 5244

### `locateNextMeaningfulToken(startPosition)` — line 5254

### `Processor(func, options)` — line 5300

### `noop()` — line 5301

### `_shouldUpdateSelector(rule, options)` — line 5306

### `_isLossy(options)` — line 5317

### `_root(rule, options)` — line 5328

### `_parseOptions(options)` — line 5335

### `_run(rule, options)` — line 5340

### `_runSync(rule, options)` — line 5366

### `ast(rule, options)` — line 5394

### `astSync(rule, options)` — line 5407

### `transform(rule, options)` — line 5418

### `transformSync(rule, options)` — line 5431

### `process(rule, options)` — line 5442

### `processSync(rule, options)` — line 5455

### `className(opts)` — line 5490

### `id(opts)` — line 5502

### `root(opts)` — line 5514

### `selector(opts)` — line 5518

### `tag(opts)` — line 5526

### `isNode(node)` — line 5551

### `isNodeType(type, node)` — line 5554

### `isPseudoElement(node)` — line 5581

### `isPseudoClass(node)` — line 5584

### `isContainer(node)` — line 5587

### `isNamespace(node)` — line 5590

### `parser(processor)` — line 5626

### `getIgnoreComment(node)` — line 5659

### `normalizeNodeArray(nodes)` — line 5676

### `localizeNode(rule, mode, localAliasMap)` — line 5697

### `localizeDeclNode(node, context)` — line 5928

### `localizeDeclarationValues(localize, declaration, context)` — line 5989

### `localizeDeclaration(declaration, context)` — line 6076

### `isNestedRule(rule)` — line 6375

### `getSingleLocalNamesForComposes(root, rule)` — line 6387

### `unescape(str)` — line 6450

### `exportScopedName(name, rawName, node)` — line 6478

### `traverseNode(node)` — line 6542

### `hash(str)` — line 6746

### `getDefaultPlugins({
  behaviour, generateScopedName, exportGlobals
})` — line 6935

### `isValidBehaviour(behaviour)` — line 6955

### `getDefaultScopeBehaviour(scopeBehaviour)` — line 6959

### `generateScopedNameDefault(name, filename, css)` — line 6963

### `getScopedNameGenerator(generateScopedName, hashPrefix)` — line 6970

### `isGlobalModule(globalModules, inputFile)` — line 7006

### `getDefaultPluginsList(opts, inputFile)` — line 7010

### `getLoader(opts, plugins)` — line 7031

### `isOurPlugin(plugin)` — line 7036

### `makePlugin(opts)` — line 7040

### `getDeclsObject(rule)` — line 122

### `createImports(imports, postcss, mode = "rule")` — line 196

### `createExports(exports, postcss, mode = "rule")` — line 229

### `traceKeySorter(a, b)` — line 1065

### `create(wasmModule, instancesPool, chunkSize, digestSize)` — line 1640

### `isSpacing(node)` — line 5645

### `isPureCheckDisabled(root)` — line 5647

### `isPureSelector(context, rule)` — line 6153

### `isNodeWithoutDeclarations(rule)` — line 6165

### `plugin(options = {})` — line 6466

### `fetcher(file, relativeTo, depTrace)` — line 7060
