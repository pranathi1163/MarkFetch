# `install.js`

> **Path:** `frontend/node_modules/esbuild/install.js`  
> **Last documented:** 2026-06-29 18:02

---

## Functions

### `pkgAndSubpathForCurrentPlatform()` — line 63

### `downloadedBinPath(pkg, subpath)` — line 83

### `validateBinaryVersion(...command)` — line 98

### `isYarn()` — line 139

### `fetch(url)` — line 146

### `extractFileFromTarGzip(buffer, subpath)` — line 159

### `installUsingNPM(pkg, subpath, binPath)` — line 179

### `removeRecursive(dir)` — line 199

### `applyManualBinaryPathOverride(overridePath)` — line 213

### `maybeOptimizePackage(binPath)` — line 223

### `downloadDirectlyFromNPM(pkg, subpath, binPath)` — line 236

### `checkAndPreparePackage()` — line 247

### `__copyProps(to, from, except, desc)` — line 8

### `__toESM(mod, isNodeMode, target)` — line 16

### `isValidBinaryPath(x)` — line 30

### `str(i, n)` — line 165
